---
milestone: v1.3
milestone_name: SuperAdmin Monitoring & TikTok Downloads
audited: 2026-05-13T16:00:00Z
status: tech_debt
scores:
  requirements: "14/18 satisfied, 4 partial, 0 unsatisfied"
  phases: 6/6
  integration: "6/6 wired (all E2E flows complete)"
  flows: 3/3
gaps:
  requirements:
    - id: "TKTOK-01"
      status: "partial"
      phase: "Phase 15"
      claimed_by_plans: ["15-01-PLAN.md", "15-02-PLAN.md"]
      completed_by_plans: ["15-01-SUMMARY.md", "15-02-SUMMARY.md"]
      verification_status: "human_needed"
      evidence: "Code path complete. harmonizer.py:374 now uses raw.asset_url or raw.creative_url (S3 URL wins — fixed 2026-05-13). Live-run UAT still pending."
    - id: "TKTOK-02"
      status: "partial"
      phase: "Phase 15"
      claimed_by_plans: ["15-01-PLAN.md"]
      completed_by_plans: ["15-01-SUMMARY.md"]
      verification_status: "human_needed"
      evidence: "Image download path wired; harmonizer precedence fix applied 2026-05-13. Live-run confirmation pending."
    - id: "INSTR-05"
      status: "partial"
      phase: "Phase 17"
      claimed_by_plans: ["17-02-PLAN.md", "17-05-PLAN.md"]
      completed_by_plans: ["17-06-SUMMARY.md"]
      verification_status: "passed"
      evidence: "sync_job_id stored in metadata_ and visible in References panel. brainsuite_job_id for scoring jobs stored in output (not metadata_) — accessible in scoring detail section but absent from References header. platform_sync_run_id declared in frontend KNOWN_EXTERNAL_ID_KEYS but never populated by backend."
    - id: "MON-07"
      status: "partial"
      phase: "Phase 19"
      claimed_by_plans: ["19-06-PLAN.md"]
      completed_by_plans: ["19-06-SUMMARY.md"]
      verification_status: "human_needed"
      evidence: "Internal job ID visible in header for all jobs. sync_job_id shows in References. brainsuite_job_id for scoring jobs is in output not metadata_ — not shown in References panel (shown in scoring section). platform_sync_run_id orphaned frontend key never set by backend."
  integration:
    - "WARNING: Alembic migration graph has 4 unmerged heads — 'alembic upgrade head' ambiguous on fresh install. Workaround: specify all heads explicitly. Affects JOBS-01 on fresh deploys."
    - "WARNING: sse-starlette==1.8.2 in requirements.txt vs 3.4.2 in milestone spec. Functional with current API usage. Needs boot confirmation."
    - "WARNING: brainsuite_job_id for scoring jobs stored in output JSONB, not metadata_ — References panel never displays it. Accessible in per-asset scoring section."
    - "WARNING: Dual heartbeat — app-level ping every 30s plus sse-starlette built-in ping every 15s. No functional break."
    - "INFO: platform_sync_run_id declared in frontend KNOWN_EXTERNAL_ID_KEYS, never set by any backend create_background_job() call."
  flows: []
tech_debt:
  - phase: "15-tiktok-asset-download"
    items:
      - "asset_url/video_source_url not in _upsert_records ON CONFLICT exclusion list — null window during re-sync (S3 file preserved; DB field temporarily null)"
      - "WR-03: TT_USER identity type not recognized as Spark ad — assets may download in violation of D-02"
      - "WR-05: asset download proceeds regardless of SystemConfig.scoring_enabled toggle"
  - phase: "18-sse-transport"
    items:
      - "sse-starlette==1.8.2 in requirements.txt; SUMMARY-02 narrative claimed 3.4.2 — version mismatch needs boot confirmation"
      - "Dual heartbeat: app-level ping every 30s + sse-starlette built-in ping=15 — redundant keepalives"
  - phase: "19-superadmin-monitoring-ui"
    items:
      - "CR-04: 403 tests call get_current_superadmin directly instead of going through list_jobs/delete_jobs endpoints — endpoint dependency wiring not verified"
      - "brainsuite_job_id for scoring jobs in output not metadata_ — References panel misses it; KNOWN_EXTERNAL_ID_KEYS includes platform_sync_run_id which backend never populates"
  - phase: "19.1-close-gap-null-token-eventsource-leak"
    items:
      - "WR-01: this.eventSource not nulled after close on null-token early return — stale closed-object reference persists; any liveness check via !== null is a false positive"
      - "VALIDATION.md nyquist_compliant: false — no frontend test runner; static grep validation only"
  - phase: "cross-phase"
    items:
      - "Alembic migration graph has 4 unmerged heads — alembic upgrade head ambiguous; fresh installs require specifying all 4 heads explicitly"
closed_since_last_audit:
  - "harmonizer.py:374 — raw.creative_url or raw.asset_url → raw.asset_url or raw.creative_url (S3 wins over CDN on re-sync). Closed 2026-05-13."
  - "DELETE /jobs — added _PROTECTED_STATUSES guard; returns 422 for RUNNING/PENDING. Closed 2026-05-13."
  - "JSON.parse(event.data) — wrapped in try/catch; malformed SSE frames discarded silently. Closed 2026-05-13."
  - "getJobs() — added encodeURIComponent for jobType and status params; consistent with clearJobs(). Closed 2026-05-13."
  - "DV360 instrumentation gap — integration checker confirmed all 4 platforms route through scheduler.py create_background_job at line 169. Prior audit finding was incorrect. Closed 2026-05-13."
nyquist:
  compliant_phases: [15, 16, 17, 18, 19]
  partial_phases: ["19.1"]
  missing_phases: []
  overall: "partial — 5/6 phases compliant; Phase 19.1 static-only (no frontend test runner configured)"
---

# v1.3 Milestone Audit: SuperAdmin Monitoring & TikTok Downloads

**Audited:** 2026-05-13 (second pass — post gap-closure fixes)
**Status:** tech_debt — all requirements have implementations; 4 partial (human UAT + display routing issues); no critical blockers; 10 deferred tech debt items; all 3 E2E flows complete

## Score Summary

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | 14/18 satisfied, 4 partial, 0 unsatisfied | TKTOK pending live UAT; INSTR-05/MON-07 brainsuite_job_id routing |
| Phases verified | 6/6 | 1 passed, 5 human_needed |
| Integration links | 6/6 wired | Prior DV360 gap was incorrect — confirmed wired |
| E2E flows | 3/3 complete | Sync→SSE, row click→detail panel, clear→reconnect |

## Changes Since Prior Audit (2026-05-13)

**Four gaps closed:**
1. **Harmonizer precedence** — `harmonizer.py:374` flipped to `raw.asset_url or raw.creative_url` (S3 URL wins over CDN on re-sync). Clears TKTOK-01/02 code-level blocker.
2. **DELETE /jobs status guard** — `_PROTECTED_STATUSES = {"RUNNING", "PENDING"}` added; endpoint returns 422 for protected statuses. Closes CR-03.
3. **JSON.parse try/catch** — malformed SSE frames now discarded silently. Closes WR-02.
4. **getJobs encodeURIComponent** — both jobType and status params now encoded, consistent with clearJobs(). Closes WR-03.

**One prior gap retracted:**
- DV360 instrumentation gap (INSTR-01, MON-01) — integration checker confirmed all 4 platforms (including DV360) route through `scheduler.py:_sync_connection_for_date()` which calls `create_background_job` at line 169 before branching to platform-specific fetch. Prior audit finding was incorrect.

**Three new findings from integration check:**
- 4-head Alembic migration graph (fresh install risk)
- sse-starlette version mismatch (1.8.2 vs 3.4.2)
- Dual heartbeat (app 30s + sse-starlette 15s)

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | VERIFICATION.md | SUMMARY | REQUIREMENTS.md | Final |
|--------|-----------------|---------|-----------------|-------|
| TKTOK-01 | human_needed (code ✓, UAT pending) | listed | [x] | **partial** — live run not confirmed |
| TKTOK-02 | human_needed (code ✓, UAT pending) | listed | [x] | **partial** — live run not confirmed |
| JOBS-01 | human_needed (10/10 static ✓) | listed | [ ] stale | **satisfied** |
| JOBS-02 | satisfied | listed | [ ] stale | **satisfied** |
| INSTR-01 | passed | listed | [ ] stale | **satisfied** — all 4 platforms confirmed via scheduler.py |
| INSTR-02 | passed | listed | [ ] stale | **satisfied** |
| INSTR-03 | passed | listed | [ ] stale | **satisfied** |
| INSTR-04 | passed | listed | [ ] stale | **satisfied** |
| INSTR-05 | passed | listed | [ ] stale | **partial** — brainsuite_job_id in output not metadata_; platform_sync_run_id orphaned |
| SSE-01 | human_needed (logic ✓) | listed | [ ] stale | **satisfied** |
| SSE-02 | human_needed (logic ✓) | listed | [ ] stale | **satisfied** |
| MON-01 | human_needed | listed | [ ] stale | **satisfied** — DV360 jobs visible (scheduler.py wired) |
| MON-02 | human_needed | listed | [ ] stale | **satisfied** |
| MON-03 | human_needed | listed | [ ] stale | **satisfied** |
| MON-04 | human_needed | listed | [ ] stale | **satisfied** |
| MON-05 | human_needed | listed | [ ] stale | **satisfied** |
| MON-06 | human_needed | listed | [ ] stale | **satisfied** |
| MON-07 | human_needed | listed | [ ] stale | **partial** — brainsuite_job_id routing + platform_sync_run_id orphan |

Note: REQUIREMENTS.md traceability checkboxes never updated post-execution. All `[ ] stale` rows have code implementation; stale checkbox is a doc debt.

## Phase Verification Summary

| Phase | Status | Score | Key Notes |
|-------|--------|-------|-----------|
| 15 — TikTok Asset Download | human_needed | 7/9 | Harmonizer precedence fixed; live-run UAT pending |
| 16 — Job Persistence Schema | human_needed | 10/10 | Migration + pytest green-run need human execution |
| 17 — Service Instrumentation | **passed** | 7/7 | — |
| 18 — SSE Transport | human_needed | 4/4 | 3 wall-clock tests need live stack |
| 19 — SuperAdmin Monitoring UI | human_needed | 7/7 | CR-04 test wiring; brainsuite_job_id routing |
| 19.1 — Null Token / EventSource Fix | human_needed | 5/5 | WR-01 stale ref; browser UAT pending |

## Integration Findings

### E2E Flows — All 3 Complete

**Flow 1: Sync run → SSE → UI progress bar**

`scheduler.py:169 create_background_job()` → DB insert + commit → `job_tracker.py:63 redis.publish("sse:job_updates")` → `jobs.py:sse_generator pubsub.get_message()` → DB fetch `BackgroundJob` → yield `job_update` event → `EventSource job_update listener` → try/catch JSON.parse → `NgZone.run → jobMap → jobsSubject.next()` → `jobs$ | async` → OnPush re-render → `mat-progress-bar` ✓

**Flow 2: Click job row → detail panel**

`selectJob(job)` → `selectedJobId` set → `[jobId] [isOpen]` bindings → `ngOnChanges → loadJobDetail()` → `getJobDetail()` → `GET /api/v1/jobs/{id}` → `db.get(BackgroundJob)` + enrichment → `JobDetail` JSONB → type-specific panel render (sync/download/autofill/scoring) + error traceback ✓

**Flow 3: Clear → backend delete → reconnect**

`clearJobs('COMPLETE', allJobs)` → `forkJoin(DELETE calls)` → `encodeURIComponent(jobType/status)` → `DELETE /api/v1/jobs?...` → `_PROTECTED_STATUSES` guard → db DELETE + commit → `clearJobMap()` + `connect()` → close old EventSource (null guard) → fresh SSE burst ✓

### Cross-Phase Links

| Boundary | Status | Notes |
|----------|--------|-------|
| 15 → Harmonizer | ✅ WIRED | `tiktok_sync.py` → `TikTokRawPerformance.asset_url` → `harmonizer.py:374 raw.asset_url or raw.creative_url` → `CreativeAsset.asset_url` |
| 16 → 17 | ✅ WIRED | `BackgroundJob` model imported by `job_tracker.py`; migration chain intact |
| 17 → 18 | ✅ WIRED | Redis PUBLISH after commit in both create/update helpers; SSE generator subscribes same channel |
| 18 → 19 | ✅ WIRED | Router mounted at `/jobs` in `__init__.py:14`; all 4 routes (stream, list, detail, delete) consumed by service |
| 19 frontend | ✅ WIRED | connect() null guard + close-before-open + JSON.parse try/catch + encodeURIComponent; panel bound in template |
| 19 → 19.1 | ✅ WIRED | All Phase 19.1 fixes confirmed in job-monitor.service.ts |

## Nyquist Compliance

| Phase | nyquist_compliant | wave_0_complete |
|-------|-------------------|-----------------|
| 15 | ✅ true | ✅ true |
| 16 | ✅ true | ✅ true |
| 17 | ✅ true | ✅ true |
| 18 | ✅ true | ✅ true |
| 19 | ✅ true | ✅ true |
| 19.1 | ⚠️ false | ⚠️ false |

Phase 19.1: no frontend test runner configured — static grep validation only (VALIDATION.md status: draft).

## Tech Debt (10 items across 5 areas)

**Phase 15**
- `asset_url`/`video_source_url` not in `_upsert_records` ON CONFLICT exclusion — null window during re-sync (S3 file preserved; DB field temporarily null between upsert and enrich pass)
- `TT_USER` identity type not recognized as Spark ad — affected ads may download in violation of D-02
- Asset download proceeds regardless of `SystemConfig.scoring_enabled` toggle

**Phase 18**
- `sse-starlette==1.8.2` in requirements.txt; milestone spec stated 3.4.2 — version mismatch needs boot confirmation
- Dual heartbeat: app-level ping every 30s + sse-starlette built-in `ping=15` — redundant keepalives

**Phase 19**
- CR-04: 403 tests call `get_current_superadmin` directly, not through `list_jobs`/`delete_jobs` — endpoint dependency wiring not verified by tests
- `brainsuite_job_id` for scoring jobs stored in `output` not `metadata_` — References panel misses it; `platform_sync_run_id` in `KNOWN_EXTERNAL_ID_KEYS` never populated by backend

**Phase 19.1**
- WR-01: `this.eventSource` not nulled after close on null-token early return — stale closed-object reference; `!== null` liveness checks are false positives

**Cross-phase**
- Alembic migration graph has 4 unmerged heads — `alembic upgrade head` is ambiguous on a fresh install; must specify all 4 heads explicitly

**Stale docs**
- REQUIREMENTS.md traceability table — all v1.3 requirements still show `[ ] Pending` (never updated post-execution)

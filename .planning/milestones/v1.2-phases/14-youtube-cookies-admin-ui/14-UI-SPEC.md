---
phase: 14
slug: youtube-cookies-admin-ui
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-24
---

# Phase 14 — UI Design Contract

> Visual and interaction contract for Phase 14: YouTube Cookies Admin UI.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Angular Material with custom CSS variables |
| Preset | not applicable |
| Component library | Angular Material (MDC) v16+ |
| Icon library | Bootstrap Icons (`bi-*` classes) |
| Font | Nunito Sans, 'Avenir Next', sans-serif |

Source: `frontend/src/styles.scss` — Angular Material dark theme configured with `mat.$orange-palette`, CSS custom properties established in `.dark-theme` / `.light-theme` blocks.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Compact element spacing, button gaps |
| md | 16px | Default element spacing, form field margin-bottom, grid gaps |
| lg | 24px | Section body padding, section header padding, page gap between cards |
| 2xl | 48px | Empty state vertical padding |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Cookie textarea row height: 6 rows (unrestricted pixel height — user-controlled)
- Touch targets for Reveal Cookie / Replace buttons: minimum 36px height (Angular Material default)
- Table row padding: 8px vertical, 8px horizontal (matches existing table `th, td { padding: 8px }` in RESEARCH.md code example)

Inherited implementation detail outside the design token system:
- `.page-container { padding: 28px }` — this 28px value is inherited from the existing global page container style and is not a new design token. Executors must not use 28px for any new element spacing; use `lg (24px)` or `2xl (48px)` instead.

Source: All spacing values verified against `brainsuite-apps.component.ts` inline styles (`.page-container`, `.section-body`, `.section-header`, `.form-full`).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label / secondary | 13px | 400 | 1.5 |
| Small / badge / label | 12px | 400 or 600 | 1.5 |
| Section heading | 16px | 600 | 1.2 |

Notes:
- Body 14px is the global `html, body { font-size: 14px }` baseline — all Admin sections inherit it.
- Section headings use `h2 { font-size: 16px; font-weight: 600 }` per `.section-header` rule in brainsuite-apps.component.ts.
- Badge text (health status labels: VALID / EXPIRED / MISSING) uses the 12px Small role at weight 600 uppercase with letter-spacing 0.5px — the uppercase transform and letter-spacing provide sufficient visual differentiation without introducing a 5th size. Inherits from global `.badge` class.
- Table header cells use 13px weight 400 (no separate bold declared in existing patterns — matches Angular Material MDC table default).

Source: `frontend/src/styles.scss` lines 99–105, `brainsuite-apps.component.ts` lines 357–358.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg-primary)` = `#1B1B1B` (dark) / `#F5F6FA` (light) | Page background, admin container background |
| Secondary (30%) | `var(--bg-card)` = `#2C2C2C` (dark) / `#FFFFFF` (light) | config-section card backgrounds, cookie slot cards, table surfaces |
| Accent (10%) | `#FF7700` (`var(--accent)`) | Save button background, focused form field border, Reveal Cookie/Replace button text color on hover |
| Success semantic | `#2ECC71` (`var(--success)`) | VALID badge background-tint + text |
| Warning semantic | `#F39C12` (`var(--warning)`) | EXPIRED badge background-tint + text |
| Neutral semantic | `var(--border)` = `#404040` (dark) / `#C2CDE2` (light) | MISSING badge background |
| Destructive | `#E74C3C` (`var(--error)`) | Not used for actions in Phase 14; error state text only |
| Border | `var(--border)` | Card borders, table row dividers, cookie slot card borders |
| Text secondary | `var(--text-secondary)` = `#A0A0A0` (dark) / `#4A5568` (light) | Descriptive text, table header labels, empty state body |
| Text muted | `var(--text-muted)` = `#707070` (dark) / `#718096` (light) | Placeholder text, masked cookie display `••••••••••` |

Accent reserved for:
- "Save Cookie" button fill (primary CTA per cookie slot)
- "Promote to SuperAdmin" button fill (primary CTA in SuperAdmin section)
- Focused/active form field outline border
- Hovered stroked button (`Reveal Cookie`, `Replace`, `Change secret`) text color

No accent on: table headers, section headings, nav label text, badge text.

Primary focal point: the cookie health badge (VALID / EXPIRED / MISSING) at the top-right of each cookie slot card. This badge is the first element the eye should locate on page load and after a save action; all other slot elements are subordinate to it.

Source: `frontend/src/styles.scss` `:root`, `.dark-theme`, `.light-theme` blocks; `.badge` class; `brainsuite-apps.component.ts` RESEARCH.md Example 5 badge color inline styles.

### Health Badge Semantics

| Status | CSS Class | Background tint | Text color | Label |
|--------|-----------|-----------------|------------|-------|
| valid | `badge-success` | `rgba(46,204,113,0.15)` | `#2ECC71` | VALID |
| expired | `badge-warning` | `rgba(243,156,18,0.15)` | `#F39C12` | EXPIRED |
| missing | `badge-neutral` | `var(--border)` | `var(--text-secondary)` | MISSING |

Badge text: 12px weight 600 uppercase letter-spacing 0.5px (Small role with uppercase modifier). Use existing global `.badge` class + status modifier. Do not create new badge styles.

---

## Component Inventory

### Reuse (zero new styles needed)

| Component | Angular Material | CSS Class | Usage in Phase 14 |
|-----------|-----------------|-----------|-------------------|
| Section card | — | `.config-section` | Wraps each of the three Admin sections |
| Section header | — | `.section-header` | Title + description row at top of each card |
| Section body | — | `.section-body` | Content area with 24px padding |
| Primary button | `mat-flat-button` | `.save-btn` (accent fill) | "Save Cookie", "Promote to SuperAdmin" |
| Stroked button | `mat-stroked-button` | — | "Reveal Cookie", "Hide Cookie", "Replace", "Discard" |
| Snack bar | `MatSnackBar` | — | Success and error feedback toasts |
| Spinner | `MatProgressSpinner` diameter=16 | — | Inline save/load loading states |
| Badge | — | `.badge .badge-{status}` | Cookie health status labels |
| Masked display | `<input type="password">` or monospace span | `.masked` | `••••••••••••••••` cookie display |
| Table | HTML `<table>` | — | SuperAdmin list, Org list |
| Form field | `MatFormField appearance="outline"` | `.w-full` | Email input for promote, cookie textarea |

### New Elements

| Element | Implementation | Notes |
|---------|---------------|-------|
| Cookie slot card | `<div class="cookie-card">` nested inside `.config-section` | Inner card with `var(--bg-secondary)` bg, border `var(--border)`, border-radius 6px, padding 12px; matches `.card-sm` pattern |
| Cookie textarea | `<textarea>` native | Appears only in Replace mode; 6 rows, full width, padding 8px, border `var(--border)`, border-radius `var(--border-radius)` 8px |
| "Admin" sidebar nav item | `<a routerLink>` in sidebar | Follows existing nav item pattern; visible only when `currentUser?.is_superuser === true` |

---

## Layout Specification

### Admin Page Layout

```
/configuration/admin

┌─────────────────────────────────────┐
│ .page-container (padding: 28px,     │
│   max-width: 900px, gap: 24px)      │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ .config-section                 │ │
│ │ ┌─ .section-header ────────────┐│ │
│ │ │ h2: YouTube Cookies      16/600││
│ │ │ p: Manage DV360 cookie…  13/400││
│ │ └──────────────────────────────┘│ │
│ │ ┌─ .section-body ─────────────┐ │ │
│ │ │ ┌─ .cookie-card ──────────┐ │ │ │
│ │ │ │ .slot-header: "Primary" │ │ │ │
│ │ │ │ + health badge [FOCAL]  │ │ │ │
│ │ │ │ ─────────────────────── │ │ │ │
│ │ │ │ [masked display or      │ │ │ │
│ │ │ │  textarea in edit mode] │ │ │ │
│ │ │ │ [Reveal Cookie][Replace]│ │ │ │
│ │ │ └─────────────────────────┘ │ │ │
│ │ │ ┌─ .cookie-card ──────────┐ │ │ │
│ │ │ │ .slot-header: "Backup"  │ │ │ │
│ │ │ │ + health badge [FOCAL]  │ │ │ │
│ │ │ │ [masked display or      │ │ │ │
│ │ │ │  textarea in edit mode] │ │ │ │
│ │ │ │ [Reveal Cookie][Replace]│ │ │ │
│ │ │ └─────────────────────────┘ │ │ │
│ │ └─────────────────────────────┘ │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ .config-section                 │ │
│ │ SuperAdmin Management           │ │
│ │ [table: email | name | joined]  │ │
│ │ [email input] [Promote] button  │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ .config-section                 │ │
│ │ Organizations (read-only)       │ │
│ │ [table: name | slug | users |   │ │
│ │  created]                       │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### Cookie Slot States (State Machine)

**State A — Masked (default after cookie saved):**
```
[ Primary Cookie ]                    • VALID
[••••••••••••••••••••]  [Reveal Cookie]  [Replace]
```
- `••••••••••••••••••••` uses monospace font, `var(--text-muted)` color
- `Reveal Cookie` button: `mat-stroked-button`, switches to State B
- `Replace` button: `mat-stroked-button`, switches to State C

**State B — Revealed:**
```
[ Primary Cookie ]                    • VALID
[yt_cookie_value plaintext…]  [Hide Cookie]  [Replace]
```
- Plain text in same field, normal `var(--text-primary)` color
- `Hide Cookie` button collapses back to State A

**State C — Replace mode (editing):**
```
[ Primary Cookie ]                    • VALID
┌────────────────────────────────────────┐
│ Paste Netscape cookie text here        │  ← textarea, 6 rows
│                                        │
└────────────────────────────────────────┘
[Discard]                          [Save Cookie]
```
- `Discard` button: `mat-stroked-button`, cancels edit, returns to State A
- `Save Cookie` button: `mat-flat-button` with accent fill; triggers PUT, shows inline spinner, then updates badge status and returns to State A on success

**State D — MISSING (no cookie ever saved):**
```
[ Primary Cookie ]                    • MISSING
No cookie saved.
                                   [Add Cookie]
```
- `Add Cookie` button: `mat-stroked-button`, opens State C (textarea pre-empty)

---

## Interaction Contracts

### Cookie Section

| Interaction | Trigger | Response | Feedback |
|-------------|---------|----------|----------|
| Page load | `ngOnInit` | GET `/api/v1/super-admin/youtube-cookies` | Skeleton shimmer on badge while loading; badge updates on resolve |
| Reveal Cookie | Click `Reveal Cookie` | Toggle `showPrimaryContent` / `showBackupContent` | Button label switches to "Hide Cookie"; masked text replaced with plain text |
| Hide Cookie | Click `Hide Cookie` | Toggle reveal state off | Button label returns to "Reveal Cookie"; plain text replaced with masked |
| Replace | Click `Replace` | Set `editingPrimary` / `editingBackup` = true | Textarea appears; masked display hidden; Discard + Save Cookie buttons appear |
| Discard | Click `Discard` | Clear textarea content; set editing = false | Returns to masked display (State A) |
| Save Cookie | Click `Save Cookie` | PUT `/api/v1/super-admin/youtube-cookies` | Inline spinner on button; button disabled during request; on success: snack "Cookie updated" 3s; badge refreshes; editing mode closes; on error: snack "Failed to save cookie" stays open until dismissed |

### SuperAdmin Management Section

| Interaction | Trigger | Response | Feedback |
|-------------|---------|----------|----------|
| Page load | `ngOnInit` | GET `/api/v1/super-admin/users` | List renders; if empty see empty state |
| Promote | Click `Promote to SuperAdmin` with non-empty email | POST `/api/v1/super-admin/users/promote` | Button spinner during request; on success: snack "User promoted to SuperAdmin" 3s + list refreshes + input clears; on 404: snack "No user found with that email"; on 409: snack "User is already a SuperAdmin" |
| Promote with empty email | Click button with blank input | No API call | Input field shows validation hint "Email required" |

### Organization List Section

| Interaction | Trigger | Response | Feedback |
|-------------|---------|----------|----------|
| Page load | `ngOnInit` | GET `/api/v1/super-admin/organizations` | Table renders read-only; no user actions |

### Navigation (Admin nav item)

| Interaction | Trigger | Response |
|-------------|---------|----------|
| Admin nav appears | `currentUser?.is_superuser === true` after login | "Admin" item renders in Configuration sidebar below existing links |
| Non-SuperAdmin visits `/configuration/admin` | Route activated | `isSuperAdminGuard` redirects to `/` (home) |
| Bell click on COOKIE_FAILED notification | Click notification | Router navigates to `/configuration/admin` (deeplink from `data.deeplink` if Phase 10 handler supports it; otherwise message text visible with manual navigation) |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Admin nav item | Admin |
| Page / section breadcrumb | Configuration > Admin |
| YouTube Cookies section heading | YouTube Cookies |
| YouTube Cookies section description | Manage system-wide DV360 cookie credentials used for video asset downloads. |
| Primary CTA — save primary slot | Save Cookie |
| Primary CTA — save backup slot | Save Cookie |
| Reveal button | Reveal Cookie |
| Hide button | Hide Cookie |
| Replace button | Replace |
| Add cookie button (MISSING state) | Add Cookie |
| Discard button | Discard |
| MISSING slot label | No cookie saved. |
| Empty state — no cookies set | No cookies configured. Add a primary cookie to enable DV360 video downloads. |
| Error state — save failed | Failed to save cookie. Check your connection and try again. |
| Error state — promote failed generic | Failed to promote user. Check your connection and try again. |
| Error state — user not found (404) | No user found with that email address. |
| Error state — already SuperAdmin (409) | This user is already a SuperAdmin. |
| Success — cookie saved | Cookie updated successfully. |
| Success — user promoted | User promoted to SuperAdmin. |
| SuperAdmin Management section heading | SuperAdmin Management |
| SuperAdmin Management description | Manage platform administrators. SuperAdmins have access to system configuration. |
| Promote input placeholder | Email address to promote |
| Promote button label | Promote to SuperAdmin |
| Promote input validation hint | Enter a valid email address |
| SuperAdmin list — empty state | No SuperAdmins found. Use the form below to promote a user. |
| Organizations section heading | Organizations |
| Organizations section description | Read-only list of all organizations on the platform. |
| Organizations list — empty state | No organizations found. |
| COOKIE_FAILED notification title | YouTube cookies failed |
| COOKIE_FAILED notification body | yt-dlp download failed — all cookie slots are exhausted or expired. Update cookies in Admin settings. |

---

## Table Specifications

### SuperAdmin List Table

| Column | Data | Width | Notes |
|--------|------|-------|-------|
| Email | `admin.email` | flex 1 | Left-aligned, `var(--text-primary)` |
| Name | `admin.full_name` | 160px | Left-aligned |
| Joined | `admin.created_at` pipe `date:'mediumDate'` | 120px | Left-aligned, `var(--text-secondary)` |

### Organization List Table

| Column | Data | Width | Notes |
|--------|------|-------|-------|
| Name | `org.name` | flex 1 | Left-aligned, `var(--text-primary)` |
| Slug | `org.slug` | 160px | `<code>` element, monospace, `var(--text-muted)` |
| Users | `org.user_count` | 80px | Right-aligned number |
| Created | `org.created_at` pipe `date:'mediumDate'` | 120px | Left-aligned, `var(--text-secondary)` |

Table shared styles: `border-collapse: collapse`, header row `background: var(--bg-hover)`, `th` and `td` padding `8px`, `border-bottom: 1px solid var(--border)`.

---

## Loading & Error States

| State | Component | Implementation |
|-------|-----------|----------------|
| Loading cookie health on page open | Badge area | Show `.skeleton` shimmer block (existing global class) in place of badge while GET resolves |
| Loading SuperAdmin list | Table body | Single skeleton row while GET resolves |
| Loading Org list | Table body | Single skeleton row while GET resolves |
| Saving cookie | Save Cookie button | `<mat-spinner diameter="16">` inline + button `[disabled]="saving"` + label changes to "Saving…" |
| Promoting user | Promote button | Same spinner pattern + disabled |
| GET error (cookie health) | Section body | Inline error text: "Could not load cookie status. Refresh to retry." in `var(--error)` color — no snack bar for page-load errors |

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|---------------|
| Health badge must convey status without color alone | Badge text is always uppercase label text (VALID / EXPIRED / MISSING) — color is supplementary |
| Textarea must have label | `<label>` or `aria-label="Cookie content"` associated with each textarea |
| Promote input must have label | `mat-label` "Email address to promote" inside `MatFormField` |
| Icon-only buttons disallowed | All icon usages in this phase accompany text labels |
| Reveal/Hide toggle announces to screen readers | `aria-pressed` on the Reveal Cookie/Hide Cookie button; `aria-label` changes to "Hide cookie" when revealed |
| Admin nav item | Standard `<a>` with `routerLink`; no aria additions needed beyond the link text "Admin" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Angular Material (official) | MatButtonModule, MatFormFieldModule, MatInputModule, MatProgressSpinnerModule, MatSnackBarModule | not required — already installed |
| Bootstrap Icons (official) | `bi-check-circle`, `bi-x-circle`, `bi-person-badge`, `bi-key`, `bi-building` | not required — already installed |
| shadcn | none | not applicable — no shadcn in project |
| Third-party | none | not applicable |

No new packages required for Phase 14. All UI is built with existing Angular Material + Bootstrap Icons already in the project.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

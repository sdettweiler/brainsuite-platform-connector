---
phase: 04-dashboard-polish-reliability
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, REL-01, REL-02, REL-03]

must_haves:
  truths:
    - "All 8 phase requirements verified in working application"
    - "No regressions in existing functionality"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of all Phase 4 changes.

Purpose: Confirm all dashboard polish and reliability features work together before marking the phase complete.
Output: Human-verified confirmation that all 8 requirements are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-polish-reliability/04-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of all Phase 4 requirements</name>
  <files></files>
  <action>
    Run the full backend test suite and present results. Then guide the user through manual verification of all 8 requirements.

    Run: `docker-compose exec backend pytest backend/tests/ -x -q`
    Run: `cd frontend && npx ng build --configuration=development`

    Then present the verification checklist below to the user.
  </action>
  <verify>
    <automated>cd /Users/sebastian.dettweiler/Claude\ Code/platform-connector/brainsuite-platform-connector && docker-compose exec backend pytest backend/tests/ -x -q</automated>
  </verify>
  <read_first>
    - .planning/phases/04-dashboard-polish-reliability/04-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - User confirms all DASH and REL requirements pass visual/functional inspection
    - No console errors in browser dev tools during testing
    - No Python exceptions in backend logs during testing
    - Backend test suite passes
    - Frontend builds without errors
  </acceptance_criteria>
  <done>User has approved all 8 Phase 4 requirements as working</done>
  <what-built>
    Complete Phase 4 implementation:
    - Score range slider in dashboard filter bar (DASH-05)
    - Video thumbnail fallback with platform icon + VIDEO tag (DASH-01)
    - Score sort with NULLS LAST on both directions (DASH-04)
    - sort_by=total_score fix (DASH-04)
    - score_min/score_max backend filter params (DASH-05)
    - Health badge per platform connection (REL-01)
    - Reconnect button for expired/failed connections (REL-02)
    - Relative time for last sync (REL-01)
    - token_expiry exposed in platform connections response (REL-01)
    - Phase 3 carryforward: score badge (DASH-02), CE tab (DASH-03), scheduler guard (REL-03)
  </what-built>
  <how-to-verify>
    Start the application: `docker compose up`

    **DASH-01 -- Thumbnails:**
    1. Open the Dashboard page
    2. Verify image creatives show their thumbnail
    3. Look for a video creative with no thumbnail -- should show dark background (#111) with platform icon and "VIDEO" tag in bottom-right

    **DASH-02 -- Score badge:**
    4. Verify scored creatives show a numeric score badge overlay on their tile

    **DASH-03 -- Dimension breakdown:**
    5. Double-click a scored creative to open the detail dialog
    6. Click the "Creative Effectiveness" tab
    7. Verify dimension scores are displayed

    **DASH-04 -- Sort:**
    8. Select "ACE Score" from the Sort dropdown
    9. Verify scored creatives appear first (highest scores at top in desc)
    10. Click the sort direction toggle (ascending)
    11. Verify scored creatives appear first (lowest scores at top), unscored still at bottom

    **DASH-05 -- Score range filter:**
    12. Locate the "Score range" slider in the filter bar
    13. If no scored assets: verify slider is disabled and shows "No scored creatives yet" on hover
    14. If scored assets exist: drag left handle to 50 -- verify only creatives with score >= 50 appear after a brief delay
    15. Reset handles to 0-100 -- verify all creatives return

    **REL-01 -- Platform health:**
    16. Navigate to Configuration > Platform Connections
    17. Verify each connection row shows a health badge (Connected/Token expired/Sync failed)
    18. Verify "Last Synced" column shows relative time (e.g., "2 hours ago")

    **REL-02 -- Reconnect prompt:**
    19. If any connection has expired token or failed sync, verify "Reconnect Account" button is visible
    20. (Optional) Click "Reconnect Account" and verify it initiates the OAuth flow

    **REL-03 -- Scheduler guard:**
    21. Verify SCHEDULER_ENABLED env var is documented and used (code-level check, no UI needed)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 4, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human visual and functional verification of all 8 requirements.
</verification>

<success_criteria>
- User approves all Phase 4 requirements as working
- Application runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-polish-reliability/04-04-SUMMARY.md`
</output>

---
phase: 09-ai-metadata-auto-fill
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-04-02T10:31:18.054Z
---

<current_state>
Plan 03 in progress. Task 1 (wire auto-fill into sync pipeline) partially started:
harmonizer.py has ONLY the import changes applied (import uuid + Tuple added to line 1-5).
ALL method signature and body changes still pending. Context ran out mid-edit.

4 session-fix files are uncommitted (should go in same commit as 09-03 work).
</current_state>

<completed_work>

- Plan 01: Backend foundation (model, migration, service, tests) ✓ commit f6ddfa5
- Plan 02-T1: Metadata config auto-fill toggle + type selector ✓ commit e27a78a
- Plan 02-T2: Asset detail dialog inference badge + rescore toast ✓ commit 911ca77
- Plan 02-T3: Visual verification ✓ user approved
- Session fix: default_metadata_values name→UUID normalization in dashboard.py ✓ uncommitted
- Session fix: platforms.component.ts keying by field.id not field.name ✓ uncommitted
- Session fix: removed debug console.logs from dashboard.component.ts ✓ uncommitted
- Plan 03 started: harmonizer.py got `import uuid` and `Tuple` added to imports only
</completed_work>

<remaining_work>

**Task 1 (09-03-t1): Wire auto-fill into harmonizer + scheduler**

KEY ARCHITECTURAL FACTS:
- CreativeAsset records are created in harmonizer.py `_ensure_asset()` — NOT in the 4 sync services
- harmonizer flushes but does NOT commit — commit is in scheduler.py
- ai_autofill.py opens its OWN session (get_session_factory()) — needs committed data
- Must fire asyncio.create_task AFTER scheduler's await db.commit()

IMPLEMENTATION PLAN:
1. harmonizer.py — add `_new_asset_ids: Optional[List[Tuple[uuid.UUID, uuid.UUID]]] = None` to:
   - `harmonize_connection(self, db, connection, date_from=None, date_to=None, _new_asset_ids=None)`
   - `_harmonize_meta(self, db, connection, org_currency, date_from, date_to, _new_asset_ids=None)`
   - `_harmonize_tiktok(self, db, connection, org_currency, date_from, date_to, _new_asset_ids=None)`
   - `_harmonize_google_ads(self, db, connection, org_currency, date_from, date_to, _new_asset_ids=None)`
   - `_harmonize_dv360(self, db, connection, org_currency, date_from, date_to, _new_asset_ids=None)`
   - `_ensure_asset(self, db, connection, platform, ad_id, _new_asset_ids=None, **kwargs)`
2. harmonizer.py — in `_ensure_asset` new-asset branch, AFTER `await db.flush()` and score_stmt:
   `if _new_asset_ids is not None: _new_asset_ids.append((asset.id, asset.organization_id))`
3. harmonizer.py — thread `_new_asset_ids=_new_asset_ids` through all internal calls
4. harmonizer.py — `harmonize_connection` calls each `_harmonize_*` with `_new_asset_ids=_new_asset_ids`
5. scheduler.py — update `_harmonize_with_deadlock_retry` to:
   - Create `new_assets: List = []` at start
   - Pass `_new_asset_ids=new_assets` to `harmonizer.harmonize_connection`
   - Return `(count, new_assets)` instead of just count
6. scheduler.py — add import: `from app.services.ai_autofill import run_autofill_for_asset`
7. scheduler.py — update all 8 call sites (lines ~167, 259, 449, 538, 664, 741, 875, 951):
   - `harmonized, new_assets = await _harmonize_with_deadlock_retry(...)`
   - After `await db.commit()`: `for aid, oid in new_assets: asyncio.create_task(run_autofill_for_asset(asset_id=aid, org_id=oid))`

**Task 2 (09-03-t2): Scoring reset + ai_inference_status**

In assets.py update_asset_metadata():
- Add `from app.models.scoring import CreativeScoreResult`
- Before commit: query CreativeScoreResult, set scoring_status = "UNSCORED" if not already

In assets.py get_asset_detail (GET /{asset_id}):
- Add `from app.models.ai_inference import AIInferenceTracking`
- Query AIInferenceTracking.ai_inference_status for the asset
- Include in response

In schemas/creative.py if typed response: add `ai_inference_status: Optional[str] = None`
</remaining_work>

<decisions_made>

- Auto-fill hook: harmonizer._ensure_asset (not sync services) — sync services don't touch CreativeAsset
- Fire timing: scheduler.py after db.commit() — ai_autofill needs committed data
- _harmonize_with_deadlock_retry returns (count, new_assets) tuple
- default_metadata_values fix: backend normalizes name→UUID on read (no DB migration needed)
</decisions_made>

<blockers>
None currently.
</blockers>

<context>
Plan 03 is the final piece of Phase 09. Once done, auto-fill runs automatically whenever a new
creative asset is synced from any of the 4 platforms. The metadata_values bug (name vs UUID keys)
was diagnosed and fixed this session — that was the original blocker from the previous handoff.

All 4 uncommitted files should be committed together with the 09-03 changes in one atomic commit.
</context>

<next_action>
Start with: harmonizer.py — add _new_asset_ids param to harmonize_connection (line ~54), then thread through _harmonize_meta, _harmonize_tiktok, _harmonize_google_ads, _harmonize_dv360, _ensure_asset. Append new asset IDs in _ensure_asset new-asset branch. Then scheduler.py changes. Then assets.py Task 2.
</next_action>

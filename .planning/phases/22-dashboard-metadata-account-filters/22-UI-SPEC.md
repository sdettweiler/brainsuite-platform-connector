---
phase: 22
slug: dashboard-metadata-account-filters
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-15
---

# Phase 22 — UI Design Contract

> Visual and interaction contract for Phase 22: Dashboard Metadata + Account Filters.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Angular Material (already initialized) |
| Preset | not applicable |
| Component library | Angular Material 17 (mat-menu, mat-autocomplete, mat-chips) |
| Icon library | Bootstrap Icons (`bi` classes via `bootstrap-icons`) |
| Font | Nunito Sans — loaded from Google Fonts; fallback `'Avenir Next', sans-serif` |

Source: `frontend/src/styles.scss` — Angular Material dark theme + Bootstrap Icons already imported; no shadcn applicable (Angular project).

---

## Spacing Scale

Declared values (multiples of 4, drawn from the allowed set {4, 8, 16, 24, 32, 48, 64}):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, chip inner padding, badge padding |
| sm | 8px | Toolbar group gaps, dropdown option vertical padding, toolbar outer gap, chip row gap, group-header horizontal padding |
| lg | 16px | Toolbar margin-bottom, section padding, filter chip row margin-top, dropdown option horizontal padding |
| xl | 24px | Page horizontal padding (inherited from page shell) |
| 2xl | 32px | Major vertical breaks |
| 3xl | 48px | Page-level vertical spacing |

Exceptions:
- `tbd-trigger` height is 36px (not a spacing multiple) — matches existing filter buttons exactly; do not change.
- `mat-chip` touch target must be minimum 32px tall — satisfies usability without deviation from existing scale.
- `tbd-group-header` in account dropdown: 4px vertical padding top/bottom, 8px horizontal — uses `xs` and `sm` from the standard scale for visual alignment with `tbd-option` horizontal padding (`lg = 16px`).

Source: `dashboard.component.ts` styles (lines 525–632 extracted).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / option text | 13px | 500 (medium) | 1.4 |
| Label / chip text | 13px | 500 (medium) | 1.4 |
| Badge / micro label | 9px | 700 (bold) | 1.0 |
| Heading / agg value | 20px | 700 (bold) | 1.2 |

Notes:
- 13px is the canonical toolbar/filter text size in this project (confirmed from `.tbd-trigger`, `.tbd-option`, `.tbd-check`).
- `tbd-badge` and overlay labels use 9px at weight 700 — use this for the platform group headers (`tbd-group-header`) in the account dropdown and for the chip count badge on the Metadata button.
- Do NOT introduce a new font size for Phase 22. Use 13px for all new interactive text; 9px for platform section headers and count badges.
- Weights are restricted to 500 (interactive elements) and 700 (badges, emphasis). No 400/600 for new Phase 22 elements.

Source: `dashboard.component.ts` styles + `frontend/src/styles.scss`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg-primary)` = `#1B1B1B` dark / `#F5F6FA` light | Page background, toolbar background |
| Secondary (30%) | `var(--bg-card)` = `#2C2C2C` dark / `#FFFFFF` light | Dropdown panels (`tbd-panel`), chip background, autocomplete panel |
| Accent (10%) | `var(--accent)` = `#FF7700` | Active filter state only (see reserved-for list below) |
| Destructive | `var(--error)` = `#E74C3C` | Not used in Phase 22 — no destructive actions |

Accent (`#FF7700`) reserved for:
1. The `×` dismiss icon on active metadata filter chips (on hover: full chip background becomes `var(--accent-light)` = `rgba(255,119,0,0.15)`)
2. The check mark (`tbd-check.checked`) inside the account dropdown when an account is selected
3. Border highlight on `tbd-trigger` hover (`:hover { border-color: var(--accent) }`) — existing rule, already applies to Metadata button
4. Matched characters in autocomplete suggestion list (optional: wrap matched prefix in `<mark>` styled with `color: var(--accent); background: transparent; font-weight: 700`)

Text colors:
- Primary text: `var(--text-primary)` = `#FFFFFF` dark / `#1B1B1B` light
- Secondary / placeholder text: `var(--text-secondary)` = `#A0A0A0` dark / `#4A5568` light
- Muted / group headers: `var(--text-muted)` = `#707070` dark / `#718096` light

Platform group headers in account dropdown use `var(--text-muted)` at 9px weight 700 uppercase — visually subordinate to account names.

Chip background: `var(--bg-card)` with `1px solid var(--border)`. On hover: background transitions to `var(--accent-light)`.

Source: `frontend/src/styles.scss` `:root` + `.dark-theme` / `.light-theme` blocks.

---

## Visual Focal Points

Primary new visual element: the active-filter chip row (`metadata-chip-row`) — it is the most prominent new surface introduced in Phase 22, appearing directly below the toolbar when filters are applied.

Secondary anchor: the accent-colored border on the Metadata button (`tbd-trigger`) when ≥ 1 filter is active (`border-color: var(--accent)`) — draws the eye to the filter's active state without competing with the chip row.

All other additions (account dropdown platform grouping, step 2 autocomplete) are subordinate to these two anchors. Do not introduce any additional visual anchors.

---

## Component Inventory

### New: Metadata Filter Button + Two-Step Dropdown (DASH-01)

**Step 1 — Field Selector:**
- Element: `<div class="tbd">` wrapping `<button class="tbd-trigger">` + `<div class="tbd-panel">` (matches existing Format/Sort/Account pattern exactly)
- Button label: `"Metadata"` when no filters active; `"Metadata (N)"` when N filters active (count badge inline in button text, not a separate badge element)
- Caret: `<span class="tbd-arrow">` with Bootstrap Icon `bi-chevron-down` (12px, `var(--text-secondary)`)
- Panel: `min-width: 200px; max-height: 280px; overflow-y: auto` — wider than Sort dropdown to accommodate field labels
- Each field: `<div class="tbd-option">` — no `tbd-check` (selection is implicit, moves to step 2)
- Empty field list: show single disabled `tbd-option` with text "No metadata fields configured" in `var(--text-muted)`

**Step 2 — Value Autocomplete (inline, replaces Step 1 panel):**
- Element: autocomplete input appears inside the same `tbd-panel` div, replacing the field list after a field is selected
- Back arrow: `<button class="tbd-back">` with `bi-arrow-left` (13px) + field label text — lets user return to step 1 without closing
- Input: `<input [(ngModel)]="metadataValueInput" placeholder="Type to search…">` styled to fill panel width; `background: var(--bg-hover); border: 1px solid var(--border); border-radius: 6px; padding: 8px 16px; font-size: 13px; color: var(--text-primary)`
- Suggestion list: rendered below input inside same panel; each suggestion is a `<div class="tbd-option">` — prefix match highlighted with `<mark>` (accent color, transparent bg)
- Empty suggestions: show single `tbd-option` "No values found" in `var(--text-muted)`
- On suggestion click: closes panel, appends chip, calls `onFilterChange()`

### New: Active Filter Chip Row (DASH-01)

- Container: `<div class="metadata-chip-row">` — `display: flex; flex-wrap: wrap; align-items: center; gap: 8px; margin-top: 8px; margin-bottom: 8px`
- Chip: `<div class="metadata-chip">` — `display: inline-flex; align-items: center; gap: 4px; padding: 4px 8px; border-radius: 16px; background: var(--bg-card); border: 1px solid var(--border); font-size: 13px; font-weight: 500; color: var(--text-primary); white-space: nowrap`
- Chip label: `"<FieldLabel>: <value>"` — FieldLabel from `MetadataField.label`, value is exact string
- Dismiss button: `<button class="chip-dismiss">` — `bi-x` icon at 13px; no border, transparent background; color `var(--text-secondary)`; on hover: color `var(--accent)`; `aria-label="Remove <FieldLabel>: <value> filter"`; `title="Remove filter"` for tooltip on hover (browser-native tooltip is sufficient — no custom overlay required)
- "Clear all filters" button: shown only when ≥ 2 chips active; `<button class="chip-clear-all">` — plain text "Clear all filters" at 13px weight 500, `color: var(--text-muted)`; on hover: `color: var(--accent)`. Placed at end of chip row.
- The chip row is hidden (height 0, no layout space) when `activeMetadataFilters.length === 0`

### Modified: Ad Account Dropdown — Platform Grouping (DASH-02)

- Platform section headers inside `mat-menu`: `<div class="tbd-group-header">META</div>` — not clickable; `padding: 4px 8px; font-size: 9px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.8px; color: var(--text-muted); pointer-events: none`
- Platform order: META → TIKTOK → GOOGLE ADS → DV360
- Flat list fallback: if only one platform is connected, no section headers rendered (conditional `*ngIf`)
- "All Accounts" option stays at the top of the list, before any platform group headers
- A thin `1px solid var(--border)` horizontal rule (`<div class="tbd-group-divider">`) separates each platform group from the next

---

## Interaction States

### Metadata Filter Button
| State | Visual |
|-------|--------|
| Default (no filters) | `tbd-trigger` — standard border `var(--border)`, bg `var(--bg-card)` |
| Active (≥1 filter) | `tbd-trigger` — border-color `var(--accent)`, bg `var(--accent-light)` |
| Hover | border-color `var(--accent)`, bg `var(--bg-hover)` |
| Open (step 1) | panel visible; button has `aria-expanded="true"` |
| Open (step 2) | same panel, field list replaced by autocomplete |

### Metadata Chip
| State | Visual |
|-------|--------|
| Default | bg `var(--bg-card)`, border `var(--border)` |
| Hover | bg `var(--accent-light)`, border-color `var(--accent)`; dismiss icon color `var(--accent)` |
| Focus (keyboard) | `outline: 2px solid var(--accent); outline-offset: 2px` |

### Autocomplete Input
| State | Visual |
|-------|--------|
| Default | border `var(--border)`, bg `var(--bg-hover)` |
| Focus | border-color `var(--accent)`, no box-shadow |
| Loading values | spinner (`bi-arrow-clockwise` animated rotation) at 13px inside panel, replacing suggestion list |
| No results | single `tbd-option` with "No values found" in `var(--text-muted)` |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Metadata button — default | `Metadata` |
| Metadata button — active (N filters) | `Metadata (N)` — e.g. `Metadata (2)` |
| Step 1 panel — empty field list | `No metadata fields configured` |
| Step 2 — input placeholder | `Type to search…` |
| Step 2 — no suggestions | `No values found` |
| Step 2 — back button | `← <FieldLabel>` (e.g. `← Language`) |
| Chip label format | `<FieldLabel>: <value>` (e.g. `Language: Indonesian`) |
| Clear all button | `Clear all filters` |
| Account dropdown — "All Accounts" option | `All Accounts` (unchanged from existing) |
| Account dropdown — platform group header examples | `META`, `TIKTOK`, `GOOGLE ADS`, `DV360` |
| Empty state (no results after filter) | `No creatives match your filters.` (existing empty state copy — no change) |
| Error — values failed to load | `Couldn't load values. Try again.` — shown as single `tbd-option` in panel; clicking it retries the fetch |

Destructive actions in Phase 22: none. The dismiss chip (×) is not destructive — it is reversible by re-adding the filter. No confirmation dialogs required.

---

## Accessibility Contract

- `tbd-trigger` for Metadata button: `aria-haspopup="true"`, `aria-expanded="false|true"`, `aria-label="Metadata filter"`
- Step 1 panel: `role="listbox"`, `aria-label="Select metadata field"`
- Step 2 input: `aria-label="Search <FieldLabel> values"`, `aria-autocomplete="list"`, `aria-controls="meta-value-listbox"`
- Step 2 suggestion list: `role="listbox" id="meta-value-listbox"`; each suggestion: `role="option"`
- Metadata chips: `role="listitem"`; dismiss button: `aria-label="Remove <FieldLabel>: <value> filter"` + `title="Remove filter"` (tooltip on hover)
- Clear all button: `aria-label="Clear all metadata filters"`
- Platform group headers in account dropdown: `role="separator"` or `aria-disabled="true"` — not focusable
- Keyboard: Escape closes any open panel and returns focus to trigger button

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — Angular project |
| third-party | none | not applicable |

No new packages are required for Phase 22. `MatAutocompleteModule` and `MatChipsModule` are already part of `@angular/material` (already in `package.json`). They simply need to be added to the component imports array.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---
phase: 22-dashboard-metadata-account-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/e8f9a0b1c2d3_phase22_metadata_filter_index.py
  - backend/app/api/v1/endpoints/dashboard.py
  - backend/tests/test_metadata_filter.py
  - backend/tests/test_metadata_migration.py
  - backend/tests/test_asset_grid_filters.py
autonomous: true
requirements: [DASH-01, DASH-02]
tags: [dashboard, filters, metadata, alembic, fastapi]

must_haves:
  truths:
    - "D-04: A user types 2+ characters in the metadata filter input and only sees DISTINCT values from their own organization's assets"
    - "D-02: GET /dashboard/metadata-fields returns only is_active=true fields scoped to the caller's organization_id, ordered by sort_order then label"
    - "D-04: GET /dashboard/metadata-fields/{field_id}/values returns DISTINCT non-null values for an org-owned field, and returns 404 when the field belongs to another org"
    - "D-10: GET /dashboard/assets accepts repeated metadata_filter=field_name:value query params (List[str]) and applies one AND-composed aliased JOIN per filter"
    - "D-07: D-08: Multiple metadata_filter params return only assets that match ALL filters (AND logic via aliased JOINs)"
    - "D-13: Composite index idx_asset_metadata_values_field_value exists on asset_metadata_values(field_id, value)"
  artifacts:
    - path: "backend/alembic/versions/e8f9a0b1c2d3_phase22_metadata_filter_index.py"
      provides: "Composite index migration"
      contains: "idx_asset_metadata_values_field_value"
    - path: "backend/app/api/v1/endpoints/dashboard.py"
      provides: "Two new endpoints + metadata_filter param on /assets"
      exports: ["get_metadata_fields", "get_metadata_field_values"]
    - path: "backend/tests/test_metadata_filter.py"
      provides: "Org-isolation + endpoint contract tests"
    - path: "backend/tests/test_metadata_migration.py"
      provides: "Composite index existence assertion"
    - path: "backend/tests/test_asset_grid_filters.py"
      provides: "AND composition + repeated query param tests"
  key_links:
    - from: "GET /dashboard/metadata-fields/{field_id}/values"
      to: "MetadataField + AssetMetadataValue + CreativeAsset"
      via: "DISTINCT JOIN with organization_id guard on both MetadataField and CreativeAsset"
      pattern: "MetadataField.organization_id == current_user.organization_id"
    - from: "GET /dashboard/assets with metadata_filter"
      to: "asset_metadata_values + metadata_fields"
      via: "aliased JOIN per filter entry with org_id guard"
      pattern: "metadata_filter: Optional[List[str]] = Query(default=None)"
---

<objective>
Add the backend foundation for the metadata autocomplete filter (DASH-01) and the API substrate needed by DASH-02's frontend polish:

1. Alembic migration adding composite index `idx_asset_metadata_values_field_value` on `asset_metadata_values(field_id, value)` per D-13
2. New endpoint `GET /dashboard/metadata-fields` — lists active org-scoped metadata fields (D-02)
3. New endpoint `GET /dashboard/metadata-fields/{field_id}/values` — returns DISTINCT asset values for an org-owned field (D-04)
4. Extend `GET /dashboard/assets` with `metadata_filter: Optional[List[str]] = Query(default=None)` and apply AND-composed aliased JOINs (D-07, D-08, D-10)
5. New pytest files asserting org isolation, AND composition, and migration index presence

Purpose: Phase 22's frontend (Plan 02) requires these endpoints + the param contract. Every query MUST include `MetadataField.organization_id == current_user.organization_id` to prevent cross-org data leakage (security requirement T-22-01).

Output: 5 files committed; alembic upgrade head succeeds; all new tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-dashboard-metadata-account-filters/22-CONTEXT.md
@.planning/phases/22-dashboard-metadata-account-filters/22-RESEARCH.md
@.planning/phases/22-dashboard-metadata-account-filters/22-PATTERNS.md
@backend/app/api/v1/endpoints/dashboard.py
@backend/app/models/creative.py
@backend/app/models/metadata.py
@backend/tests/test_dashboard_filters.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/models/metadata.py (lines 9-37):
```python
class MetadataField(Base):
    __tablename__ = "metadata_fields"
    id: Mapped[uuid.UUID]                        # PK
    organization_id: Mapped[uuid.UUID]           # REQUIRED in every query (org guard)
    name: Mapped[str]                            # API filter key (e.g. "language")
    label: Mapped[str]                           # UI display label
    field_type: Mapped[str]                      # SELECT | TEXT | NUMBER
    is_active: Mapped[bool] = True
    sort_order: Mapped[int] = 0
```

From backend/app/models/creative.py (lines 27-77, 96-112):
```python
class CreativeAsset(Base):
    __tablename__ = "creative_assets"
    id: Mapped[uuid.UUID]
    organization_id: Mapped[uuid.UUID]
    ad_account_id: Mapped[str]
    # ... (existing fields)

class AssetMetadataValue(Base):
    __tablename__ = "asset_metadata_values"
    id: Mapped[uuid.UUID]
    asset_id: Mapped[uuid.UUID]                  # FK -> creative_assets.id
    field_id: Mapped[uuid.UUID]                  # FK -> metadata_fields.id
    value: Mapped[str]                           # nullable, String(500)
    __table_args__ = (UniqueConstraint("asset_id", "field_id"),)
```

From backend/app/api/v1/endpoints/dashboard.py (existing endpoint signature, lines 184-201):
```python
@router.get("/assets", response_model=dict)
async def get_dashboard_assets(
    date_from: date = Query(default=None),
    date_to: date = Query(default=None),
    platforms: Optional[str] = Query(default=None),
    formats: Optional[str] = Query(default=None),
    objectives: Optional[str] = Query(default=None),
    project_id: Optional[uuid.UUID] = Query(default=None),
    sort_by: str = Query(default="spend"),
    sort_order: str = Query(default="desc"),
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=50, ge=1, le=250),
    score_min: Optional[float] = Query(default=None, ge=0, le=100),
    score_max: Optional[float] = Query(default=None, ge=0, le=100),
    ad_account_ids: Optional[str] = Query(default=None),
    # NEW PARAM TO ADD:
    metadata_filter: Optional[List[str]] = Query(default=None),
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
```

Alembic head target: `d2e3f4a5b6c7` (background_jobs_schema — most recent v1.3 head). Project has multiple heads (DEBT-01) but this is the post-Phase 19.3 forward path. New migration's `down_revision = "d2e3f4a5b6c7"`.

Alembic structure analog: backend/alembic/versions/a9b1c2d3e5f6_add_proxy_config.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffolds (RED) — metadata endpoints, AND composition, migration index</name>
  <files>backend/tests/test_metadata_filter.py, backend/tests/test_metadata_migration.py, backend/tests/test_asset_grid_filters.py</files>
  <read_first>
    - backend/tests/test_dashboard_filters.py (the project's pytest convention: AsyncMock db + MagicMock user + `current_user.organization_id = uuid.uuid4()`; see test_score_range_filter_min for query-construction inspection style)
    - backend/app/api/v1/endpoints/dashboard.py (signature of get_dashboard_assets and where the metadata_filter param will live)
    - backend/app/models/metadata.py (MetadataField fields)
    - backend/app/models/creative.py (AssetMetadataValue + CreativeAsset shape)
    - .planning/phases/22-dashboard-metadata-account-filters/22-VALIDATION.md (the Per-Task Verification Map names the exact pytest selectors that must exist)
  </read_first>
  <behavior>
    Each test starts RED until Task 2/3 lands. Test names must match the validation map exactly.

    test_metadata_filter.py:
    - test_org_scoped_fields: GET /dashboard/metadata-fields returns ONLY MetadataField rows where organization_id == current_user.organization_id AND is_active is True; ordered by sort_order then label
    - test_org_scoped_values: GET /dashboard/metadata-fields/{field_id}/values returns 404 when the field belongs to another organization (db.get(MetadataField, id) returns a field whose organization_id != current_user.organization_id)
    - test_values_distinct_org_scoped: when field belongs to caller org, response["values"] is sorted, distinct, and excludes NULLs
    - test_values_no_cross_org_leakage: an AssetMetadataValue row whose CreativeAsset.organization_id is a different org MUST NOT appear in the response

    test_metadata_migration.py:
    - test_composite_index_present: after running `op.create_index(...)` in upgrade(), querying pg_indexes (or inspecting the migration source) confirms the index named `idx_asset_metadata_values_field_value` is declared on columns (field_id, value); unit-level: import the migration module and assert the upgrade() function body invokes op.create_index with name='idx_asset_metadata_values_field_value', table='asset_metadata_values', columns=['field_id', 'value']

    test_asset_grid_filters.py:
    - test_metadata_filter_single: GET /dashboard/assets?metadata_filter=language:Indonesian applies one aliased JOIN through asset_metadata_values + metadata_fields with both org_id guards (CreativeAsset.organization_id AND MetadataField.organization_id)
    - test_metadata_filter_multi_and_composition: two metadata_filter params produce two separate aliased JOINs (amv_0/mf_0, amv_1/mf_1) such that an asset must satisfy both to appear (AND logic)
    - test_multi_account_filter: ad_account_ids=acc1,acc2 keeps the existing CreativeAsset.ad_account_id.in_([...]) clause; metadata + account filters compose with AND (both clauses present in the final query)
    - test_metadata_filter_malformed_value: a malformed metadata_filter entry missing ':' returns HTTP 400 (or is silently skipped — implementer decides; assert one explicit behavior)
  </behavior>
  <action>
    Create three new pytest files. Mirror the conventions in test_dashboard_filters.py: module docstring referencing "Phase 22 Plan 01", pytest fixtures `mock_db` (AsyncMock) and `mock_user` (MagicMock with `organization_id`), test functions named exactly as listed in the &lt;behavior&gt; block (also matching .planning/phases/22-dashboard-metadata-account-filters/22-VALIDATION.md row IDs 22-01-01 through 22-01-05 and 22-02-01).

    Each test body should:
    - Construct a unittest.mock AsyncMock chain that returns the synthetic rows
    - Call into the function under test (e.g. `from app.api.v1.endpoints.dashboard import get_metadata_fields, get_metadata_field_values, get_dashboard_assets`)
    - Assert query-construction behavior via inspecting the compiled SQL (`str(query.compile(compile_kwargs={"literal_binds": True}))`) for the presence of the required substrings — keep assertions on substrings, not exact strings: e.g. `assert "metadata_fields.organization_id" in compiled_sql` and `assert "amv_0" in compiled_sql` for the aliased JOIN test

    test_metadata_migration.py imports the migration module by path (`importlib.util.spec_from_file_location`) and inspects the upgrade() function source via `inspect.getsource(module.upgrade)` for required substrings: `'idx_asset_metadata_values_field_value'`, `'asset_metadata_values'`, `'field_id'`, `'value'`.

    Every test that exercises the endpoint must include an org-id assertion: the produced SQL contains `current_user.organization_id`'s bind parameter, and a DIFFERENT org's rows are filtered out.

    Use placeholders only for the endpoint imports — these will RED-fail until Task 2 adds the functions. Do NOT skip tests with pytest.skip; they must fail with ImportError / AttributeError until Task 2 lands (that is the RED signal).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_metadata_filter.py tests/test_metadata_migration.py tests/test_asset_grid_filters.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_metadata_filter.py exists and contains exactly these test function names: test_org_scoped_fields, test_org_scoped_values, test_values_distinct_org_scoped, test_values_no_cross_org_leakage
    - File backend/tests/test_metadata_migration.py exists and contains test function test_composite_index_present
    - File backend/tests/test_asset_grid_filters.py exists and contains test function names: test_metadata_filter_single, test_metadata_filter_multi_and_composition, test_multi_account_filter, test_metadata_filter_malformed_value
    - `pytest tests/test_metadata_filter.py tests/test_metadata_migration.py tests/test_asset_grid_filters.py --collect-only -q` exits 0 (collection succeeds — files are syntactically valid Python)
    - Running these tests (without --collect-only) FAILS with ImportError or AttributeError on `get_metadata_fields` / `get_metadata_field_values` / `metadata_filter` symbol because Task 2 has not yet added them (RED state confirmed)
    - Each test file has a module docstring starting with "Phase 22 Plan 01" matching the convention in test_dashboard_filters.py
    - Every endpoint test asserts at least one of: `"metadata_fields.organization_id"` or `"creative_assets.organization_id"` appears in the compiled SQL substring check
  </acceptance_criteria>
  <done>
    Three new test files committed; collection passes; tests RED on actual run (intentional). The validation map's pytest selectors (22-VALIDATION.md rows 22-01-01 through 22-02-01) all resolve to real test functions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration — composite index on asset_metadata_values(field_id, value)</name>
  <files>backend/alembic/versions/e8f9a0b1c2d3_phase22_metadata_filter_index.py</files>
  <read_first>
    - backend/alembic/versions/a9b1c2d3e5f6_add_proxy_config.py (most recent migration structure analog; copy module docstring + revision header pattern)
    - backend/alembic/versions/d2e3f4a5b6c7_background_jobs_schema.py (the down_revision target; verify revision = "d2e3f4a5b6c7" matches)
    - backend/app/models/creative.py (lines 96-112, AssetMetadataValue table — confirm column names field_id and value)
    - .planning/phases/22-dashboard-metadata-account-filters/22-CONTEXT.md §D-13 (decision that Phase 22 owns this migration; the index was NOT actually added in Phase 20 despite STATE.md mention)
  </read_first>
  <action>
    Create new migration file backend/alembic/versions/e8f9a0b1c2d3_phase22_metadata_filter_index.py with:

    - revision = "e8f9a0b1c2d3"
    - down_revision = "d2e3f4a5b6c7" (per D-13 — chains onto the most recent v1.3 head; project still has DEBT-01 multi-heads which is deferred)
    - branch_labels = None
    - depends_on = None
    - Module docstring: "Phase 22: add composite index on asset_metadata_values(field_id, value) for metadata autocomplete (DASH-01)"
    - upgrade(): op.create_index('idx_asset_metadata_values_field_value', 'asset_metadata_values', ['field_id', 'value'], unique=False)
    - downgrade(): op.drop_index('idx_asset_metadata_values_field_value', table_name='asset_metadata_values')

    Run alembic upgrade head against the local dev DB to confirm the new revision applies cleanly. If multi-head warning appears (DEBT-01), pass `--head d2e3f4a5b6c7` or use `alembic upgrade e8f9a0b1c2d3` to target this specific path; document the workaround in the migration docstring.

    After migration runs, verify the index exists by selecting from pg_indexes:
    `SELECT indexname FROM pg_indexes WHERE tablename = 'asset_metadata_values' AND indexname = 'idx_asset_metadata_values_field_value';`
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_metadata_migration.py::test_composite_index_present -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File backend/alembic/versions/e8f9a0b1c2d3_phase22_metadata_filter_index.py exists
    - `grep -E "^revision = .e8f9a0b1c2d3." backend/alembic/versions/e8f9a0b1c2d3_phase22_metadata_filter_index.py` returns 1 match
    - `grep -E "^down_revision = .d2e3f4a5b6c7." backend/alembic/versions/e8f9a0b1c2d3_phase22_metadata_filter_index.py` returns 1 match
    - `grep "idx_asset_metadata_values_field_value" backend/alembic/versions/e8f9a0b1c2d3_phase22_metadata_filter_index.py | grep -v '^#'` returns ≥ 2 matches (one in upgrade, one in downgrade)
    - upgrade() body contains: op.create_index with name='idx_asset_metadata_values_field_value', table='asset_metadata_values', columns ['field_id', 'value']
    - `pytest tests/test_metadata_migration.py::test_composite_index_present -x -q` exits 0 (the Wave-0 test from Task 1 now GREEN)
    - `cd backend && alembic upgrade e8f9a0b1c2d3` exits 0 against the dev DB
    - `psql ... -c "SELECT indexname FROM pg_indexes WHERE indexname = 'idx_asset_metadata_values_field_value';"` returns exactly one row
  </acceptance_criteria>
  <done>
    Migration committed; revision e8f9a0b1c2d3 chains onto d2e3f4a5b6c7; alembic upgrade succeeds; pg_indexes confirms the index; Task 1's test_composite_index_present is GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Two new endpoints + metadata_filter param on /dashboard/assets (GREEN all Wave-0 tests)</name>
  <files>backend/app/api/v1/endpoints/dashboard.py</files>
  <read_first>
    - backend/app/api/v1/endpoints/dashboard.py (lines 1-30 imports; 184-340 existing get_dashboard_assets) — the metadata_filter param is inserted into this signature and the JOIN block sits with the other filter clauses near line 300
    - backend/app/models/metadata.py (MetadataField fields: id, organization_id, name, label, field_type, is_active, sort_order)
    - backend/app/models/creative.py (AssetMetadataValue: asset_id, field_id, value)
    - backend/app/api/v1/deps.py (the get_current_user dependency that provides current_user.organization_id)
    - .planning/phases/22-dashboard-metadata-account-filters/22-PATTERNS.md §"Endpoint signature & parameter pattern" and §"Filter application pattern — org-scoped query" (lines 44-103) — the exact aliased JOIN per filter implementation
    - .planning/phases/22-dashboard-metadata-account-filters/22-CONTEXT.md §D-01–D-13 (the locked decisions all 13 must be honored)
  </read_first>
  <behavior>
    After this task:
    - Task 1's tests test_org_scoped_fields, test_org_scoped_values, test_values_distinct_org_scoped, test_values_no_cross_org_leakage, test_metadata_filter_single, test_metadata_filter_multi_and_composition, test_multi_account_filter, test_metadata_filter_malformed_value — all GREEN
    - GET /dashboard/metadata-fields returns {"fields": [{id, name, label, field_type}, ...]} ordered by sort_order then label
    - GET /dashboard/metadata-fields/{field_id}/values returns {"values": [str, ...]} ordered ascending, distinct, non-null; 404 on cross-org access
    - GET /dashboard/assets?metadata_filter=language:Indonesian&metadata_filter=market:US returns only assets that have BOTH metadata values, scoped to the caller's org
    - Existing dashboard tests (test_dashboard_filters.py) remain GREEN — no regression
  </behavior>
  <action>
    Modify backend/app/api/v1/endpoints/dashboard.py:

    1. Imports — add `aliased` and `distinct` to the existing `from sqlalchemy import ...` line (line 10). Add `from sqlalchemy.orm import aliased` near the existing `from sqlalchemy.orm import selectinload` (line 12).

    2. Endpoint A — insert after the existing `score_trend` endpoint and before `get_dashboard_assets`:
       - Path: GET /metadata-fields
       - Returns the org's MetadataField rows where is_active is True, ordered by sort_order then label
       - Each row serialized as {id: str, name: str, label: str, field_type: str}
       - Org guard via `MetadataField.organization_id == current_user.organization_id`

    3. Endpoint B — insert immediately after Endpoint A:
       - Path: GET /metadata-fields/{field_id}/values where field_id is uuid.UUID
       - First: `field = await db.get(MetadataField, field_id)`; if `field is None or field.organization_id != current_user.organization_id`: raise HTTPException(404, "Field not found") (per D-04 + T-22-01)
       - Then: select distinct(AssetMetadataValue.value), JOIN MetadataField + JOIN CreativeAsset, WHERE MetadataField.id == field_id AND MetadataField.organization_id == current_user.organization_id AND CreativeAsset.organization_id == current_user.organization_id AND AssetMetadataValue.value IS NOT NULL, ORDER BY AssetMetadataValue.value asc
       - Return {"values": [row[0] for row in result.all()]}

    4. Modify get_dashboard_assets (line 184):
       - Add to signature after `ad_account_ids` and before `current_user`: `metadata_filter: Optional[List[str]] = Query(default=None),` (per D-10)
       - After the existing `if account_id_list:` clause (~line 301), insert the metadata_filter handler:
         - Iterate `for i, meta_filter_str in enumerate(metadata_filter or []):`
         - If `':' not in meta_filter_str`: raise HTTPException(400, f"Invalid metadata_filter format; expected field_name:value (got: {meta_filter_str})")  — chooses explicit 400 over silent skip; documents the behavior decision in code comment referencing test_metadata_filter_malformed_value
         - Else split once: `field_name, filter_value = meta_filter_str.split(':', 1)`
         - Build aliased tables: `amv = aliased(AssetMetadataValue, name=f"amv_{i}")`, `mf = aliased(MetadataField, name=f"mf_{i}")`
         - `query = query.join(amv, and_(amv.asset_id == CreativeAsset.id, amv.value == filter_value)).join(mf, and_(mf.id == amv.field_id, mf.name == field_name, mf.organization_id == current_user.organization_id))`
       - This produces one inner JOIN per filter → AND composition by construction (per D-07, D-08)

    5. NEVER log filter_value or field_name at any level — they may contain PII. Pass straight to the query bind parameters.

    Validation: run all three Task-1 test files; they must transition from RED to GREEN. Run test_dashboard_filters.py to confirm zero regression.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_metadata_filter.py tests/test_metadata_migration.py tests/test_asset_grid_filters.py tests/test_dashboard_filters.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "@router\.get\(.\/metadata-fields\".\)" backend/app/api/v1/endpoints/dashboard.py | grep -v '^#'` returns exactly 1 match (Endpoint A)
    - `grep -nE "@router\.get\(.\/metadata-fields/\{field_id\}/values\".\)" backend/app/api/v1/endpoints/dashboard.py | grep -v '^#'` returns exactly 1 match (Endpoint B)
    - `grep -n "metadata_filter: Optional\[List\[str\]\]" backend/app/api/v1/endpoints/dashboard.py | grep -v '^#'` returns exactly 1 match (param added to get_dashboard_assets)
    - `grep -c "current_user.organization_id" backend/app/api/v1/endpoints/dashboard.py` >= 5 (org guard present in old + 2 new endpoints + metadata_filter JOIN)
    - `grep -n "aliased(AssetMetadataValue" backend/app/api/v1/endpoints/dashboard.py | grep -v '^#'` returns ≥ 1 match (aliased JOIN pattern present)
    - `grep -n "aliased(MetadataField" backend/app/api/v1/endpoints/dashboard.py | grep -v '^#'` returns ≥ 1 match
    - `grep -nE "HTTPException\(status_code=404,\s*detail=.Field not found" backend/app/api/v1/endpoints/dashboard.py | grep -v '^#'` returns ≥ 1 match (cross-org 404 guard)
    - `grep -nE "HTTPException\(.*400.*Invalid metadata_filter" backend/app/api/v1/endpoints/dashboard.py | grep -v '^#'` returns ≥ 1 match (malformed-input rejection)
    - `pytest tests/test_metadata_filter.py tests/test_metadata_migration.py tests/test_asset_grid_filters.py -x -q` exits 0 (all 9 new tests GREEN)
    - `pytest tests/test_dashboard_filters.py -x -q` exits 0 (zero regression on existing tests)
    - `curl -s -H "Authorization: Bearer $VALID_TOKEN" http://localhost:8000/api/v1/dashboard/metadata-fields | jq '.fields | type'` returns "array"
    - `curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $OTHER_ORG_TOKEN" http://localhost:8000/api/v1/dashboard/metadata-fields/{own_org_field_id}/values` returns 404 (cross-org access blocked)
  </acceptance_criteria>
  <done>
    Endpoint A + Endpoint B + metadata_filter param all implemented; 9 Wave-0 tests + existing dashboard tests GREEN; org isolation enforced in every query path; malformed input returns 400; no proxy/credential/PII logging introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /dashboard/metadata-fields | User-controlled JWT; org_id read from token claims; untrusted query string |
| Browser → /dashboard/metadata-fields/{field_id}/values | UUID path param controlled by user; can target arbitrary field IDs |
| Browser → /dashboard/assets?metadata_filter=... | Repeated user-controlled string params; raw values flow into SQL bind params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | I (Information Disclosure) | GET /dashboard/metadata-fields/{field_id}/values | mitigate | Two-layer org guard: (1) `await db.get(MetadataField, field_id)` followed by `if field.organization_id != current_user.organization_id: raise 404`; (2) JOIN-level filter `MetadataField.organization_id == current_user.organization_id` AND `CreativeAsset.organization_id == current_user.organization_id` in the DISTINCT values query. Verified by test_org_scoped_values and test_values_no_cross_org_leakage. |
| T-22-02 | T (Tampering) | metadata_filter query param | mitigate | All filter values passed only as SQLAlchemy bind parameters (`amv.value == filter_value`, `mf.name == field_name`) — never string-interpolated into raw SQL. No `text()` usage in this code path. |
| T-22-03 | I | GET /dashboard/metadata-fields | mitigate | Single WHERE clause `MetadataField.organization_id == current_user.organization_id AND is_active is True`. Field labels can contain custom org content but are scoped to caller. Verified by test_org_scoped_fields. |
| T-22-04 | D (Denial of Service) | metadata_filter with N parameters → N JOINs | accept | FastAPI's default query parser does not cap repeated params, but per D-09 the frontend caps active filters at a UX-reasonable count (chip row). PostgreSQL handles arbitrary JOIN counts up to its planner limit. No production risk at expected ≤ 10 filters per request. |
| T-22-05 | I | Filter values logged at error level | mitigate | Action explicitly forbids logging filter_value/field_name. The 400 error message echoes the value but is returned only to the authenticated caller; not logged server-side. |
| T-22-06 | E (Elevation of Privilege) | UUID guessing for field_id | mitigate | `db.get(MetadataField, field_id)` + 404 on wrong-org match — attacker cannot enumerate field UUIDs across orgs (response is identical for "doesn't exist" and "belongs to other org"). |
</threat_model>

<verification>
Run all new + existing dashboard tests:

```bash
cd backend && pytest tests/test_metadata_filter.py tests/test_metadata_migration.py tests/test_asset_grid_filters.py tests/test_dashboard_filters.py -q --tb=short
```

Apply migration and confirm index:

```bash
cd backend && alembic upgrade e8f9a0b1c2d3
psql "$DATABASE_URL" -c "SELECT indexname, tablename FROM pg_indexes WHERE indexname = 'idx_asset_metadata_values_field_value';"
```

Smoke test endpoints against running dev backend (requires a logged-in JWT):

```bash
# Endpoint A — list fields
curl -s -H "Authorization: Bearer $JWT" http://localhost:8000/api/v1/dashboard/metadata-fields | jq

# Endpoint B — list values for a field belonging to caller's org
curl -s -H "Authorization: Bearer $JWT" http://localhost:8000/api/v1/dashboard/metadata-fields/$FIELD_ID/values | jq

# Endpoint B negative — field belongs to another org
curl -s -o /dev/null -w "%{http_code}\n" -H "Authorization: Bearer $OTHER_JWT" \
  http://localhost:8000/api/v1/dashboard/metadata-fields/$FIELD_ID/values
# Must print 404

# Assets endpoint with metadata filter
curl -s -H "Authorization: Bearer $JWT" \
  "http://localhost:8000/api/v1/dashboard/assets?metadata_filter=language:Indonesian&metadata_filter=market:US&page=1&page_size=10" | jq '.total'
```
</verification>

<success_criteria>
- All 9 new pytest cases GREEN (test_metadata_filter.py 4 + test_metadata_migration.py 1 + test_asset_grid_filters.py 4)
- All existing tests in test_dashboard_filters.py still GREEN (zero regression)
- alembic upgrade head (or `alembic upgrade e8f9a0b1c2d3`) exits 0
- pg_indexes confirms idx_asset_metadata_values_field_value exists on asset_metadata_values(field_id, value)
- GET /dashboard/metadata-fields returns 200 with shape `{fields: [{id, name, label, field_type}]}`
- GET /dashboard/metadata-fields/{field_id}/values returns 200 with shape `{values: [str, ...]}` for own-org field; returns 404 for cross-org field_id
- GET /dashboard/assets?metadata_filter=field:value applies the AND-composed aliased JOIN; multiple filters compose with AND
- No proxy/PII/credential strings appear anywhere in this PR's diff (grep gates documented above all pass)
</success_criteria>

<output>
After completion, create `.planning/phases/22-dashboard-metadata-account-filters/22-01-SUMMARY.md` documenting:
- Files changed
- Wave-0 → GREEN test transitions
- Migration revision ID and head topology note
- DEBT-01 implication: multi-heads still present; this plan adds one more revision off d2e3f4a5b6c7
- Endpoint contract reference for Plan 02's frontend
</output>

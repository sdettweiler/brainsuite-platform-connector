---
phase: 19.1-close-gap-null-token-eventsource-leak
verified: 2026-05-13T12:45:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Navigate to /configuration/jobs as SuperAdmin with a valid session"
    expected: "SSE badge shows 'Live' within 2 seconds of page load; no 'Disconnected' flash"
    why_human: "Token availability on first connect() call depends on Angular initialization order — cannot verify statically"
  - test: "Trigger a job (e.g. manual sync), then navigate away and back to /configuration/jobs"
    expected: "Second connect() does not double-register a server-side pubsub subscription; server logs show only one active SSE client per browser tab"
    why_human: "Server-side subscription count requires live runtime observation"
  - test: "Let the auth token expire mid-session while watching /configuration/jobs"
    expected: "SSE badge transitions to 'Disconnected' cleanly; no ?token=null request appears in browser network tab"
    why_human: "Token expiry is a time-based runtime condition"
---

# Phase 19.1: Close Gap — Null Token + EventSource Leak — Verification Report

**Phase Goal:** Fix null token race and EventSource leak in job-monitor.service.ts so that the SSE connection to /configuration/jobs reliably establishes on page load and does not leak server-side pubsub subscriptions
**Verified:** 2026-05-13T12:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | connect() closes any existing EventSource before opening a new one | VERIFIED | `this.eventSource?.close()` at line 41, `new EventSource(url)` at line 54 — close precedes open; grep gate 1 count = 2, gate 2 confirms ordering |
| 2 | connect() reads the token before constructing the URL | VERIFIED | `const token = this.authService.getAccessToken()` at line 44; `const url` constructed at line 53 after the guard — token read precedes URL build; gate 3 count = 1 |
| 3 | connect() returns early with 'disconnected' status when token is null | VERIFIED | `if (!token) { this.statusSubject.next('disconnected'); return; }` at lines 45-48; gate 4 count = 1 |
| 4 | connect() resets reconnectAttempts to 0 before opening a new EventSource | VERIFIED | `this.reconnectAttempts = 0` at line 51, before `new EventSource` at line 54; gate 5 count = 2 (connect reset + onopen reset) |
| 5 | The URL is never constructed with a null token | VERIFIED | Null guard at line 45 returns before line 53; gate 6 count = 0 (no `token=null` in source); gate 7 count = 1 (single url declaration after guard) |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/app/core/services/job-monitor.service.ts` | Fixed connect() method with close-before-open + null guard + counter reset | VERIFIED | File exists, 103 lines, commit 2726ef3 confirmed; method ordering matches plan specification exactly: close (L41) → token read (L44) → null guard (L45-48) → counter reset (L51) → url build (L53) → new EventSource (L54) → event listeners (L56-72) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| connect() — close | connect() — null guard | close must precede guard so old EventSource is freed even on early return | VERIFIED | L41 `this.eventSource?.close()` precedes L45 `if (!token)` — ordering confirmed by line numbers |
| connect() — token read | connect() — URL construction | token variable declared at top, consumed in URL template string | VERIFIED | `const token` at L44 consumed in `\`/api/v1/jobs/stream?token=${token}\`` at L53 — single declaration, no duplication |

### Data-Flow Trace (Level 4)

Not applicable. This phase modifies a service method's control flow (close/guard/reset sequence), not a data-rendering component. No dynamic data rendering path to trace.

### Behavioral Spot-Checks

Step 7b: SKIPPED — no frontend test runner is configured for this project (confirmed in 19.1-VALIDATION.md). The phase is a pure static code change; the 7 grep gates serve as the programmatic verification layer. Runtime behavior requires human testing (see Human Verification Required below).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SSE-01 | 19.1-01-PLAN.md | Backend exposes a SSE endpoint streaming job-updated events in real time | PARTIAL — not owned by this phase | SSE-01 implementation lives in Phase 18 (backend endpoint). Phase 19.1 fixes the frontend client connection reliability that makes SSE-01 observable from the browser, but the endpoint itself is unchanged. REQUIREMENTS.md traceability maps SSE-01 to Phase 18. The claim in SUMMARY.md frontmatter (`requirements_verified: [SSE-01, SSE-02]`) overstates ownership — this phase does not implement SSE-01, it improves client-side connection reliability. This is a documentation scoping issue only; the code change is correct. |
| SSE-02 | 19.1-01-PLAN.md | SSE connections include keepalive heartbeats and are cleaned up on client disconnect | PARTIAL — not owned by this phase | SSE-02 implementation (keepalive heartbeats, server-side cleanup) lives in Phase 18. Phase 19.1 improves client-side EventSource lifecycle (close-before-open), which contributes to SSE-02's "cleaned up on client disconnect" aspect, but does not implement keepalives. Same scoping note as SSE-01 above. |

**Requirement scoping note:** SSE-01 and SSE-02 are mapped to Phase 18 in REQUIREMENTS.md traceability. Phase 19.1's contribution is a frontend bug fix that improves the end-to-end reliability of the SSE transport these requirements describe. The plan claiming these requirement IDs is technically justified (the fix is necessary for the requirements to be fully satisfied) but creates a misleading implication that Phase 19.1 satisfies them from scratch. The REQUIREMENTS.md checkboxes remain unchecked — this is correct and should stay that way until the full v1.3 milestone is validated end-to-end.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `job-monitor.service.ts` | 41 | `this.eventSource?.close()` on null-token early return path does not null the reference (WR-01 from REVIEW.md) | WARNING | `this.eventSource` field holds a stale closed-object reference after early return. Any future code checking `this.eventSource !== null` as a liveness proxy will get a false positive. Current codebase does not appear to use this check — no immediate behavioral impact, but is a correctness gap. Pre-existing in disconnect() design; not introduced by this phase. |
| `job-monitor.service.ts` | 58 | `JSON.parse(event.data)` without try/catch in ngZone.run() (WR-02 from REVIEW.md) | WARNING | Malformed SSE frame throws uncaught exception in Angular zone. Pre-existing; not introduced by this phase. |
| `job-monitor.service.ts` | 87-88 | `getJobs()` appends query params without encodeURIComponent (WR-03 from REVIEW.md) | WARNING | Inconsistent with clearJobs() which does encode. Pre-existing; not introduced by this phase. |

All three warnings are pre-existing issues surfaced during code review, not introduced by Phase 19.1. None block the phase goal. WR-01 is the most directly related: the `this.eventSource = null` line is missing after the close on the early-return path, but the phase plan did not require it, and the primary blocker goals (preventing null token URL + preventing leak on reconnect) are both achieved.

### Human Verification Required

#### 1. SSE badge shows Live on initial page load

**Test:** Open a fresh browser tab, log in as SuperAdmin, navigate directly to /configuration/jobs.
**Expected:** The SSE badge shows "Live" (not "Disconnected") within 2 seconds. No network request with `?token=null` appears in the browser DevTools Network tab.
**Why human:** The null-token guard depends on Angular's DI initialization completing before connect() is called. Static analysis confirms the guard exists; whether the auth token is available at the exact moment connect() fires on ngOnInit is a runtime timing question that cannot be verified without executing the app.

#### 2. No duplicate server-side subscriptions on reconnect

**Test:** Navigate to /configuration/jobs, then navigate to another page, then return to /configuration/jobs. Repeat 3 times. Check server-side logs or Docker stats for active SSE client count.
**Expected:** Server-side active SSE connection count never exceeds 1 per browser session. Each navigation away cleanly closes the previous EventSource.
**Why human:** Server-side subscription count is a runtime metric not visible from static code analysis.

#### 3. Null-token early return on token expiry

**Test:** Simulate token expiry (e.g., clear localStorage token, or wait for natural expiry) while /configuration/jobs is open.
**Expected:** SSE badge transitions to "Disconnected". No request with `?token=null` appears in network tab.
**Why human:** Token expiry is a time-dependent runtime condition.

### Gaps Summary

No blocking gaps found. All 5 must-have truths are verified in the codebase. The phase goal is achieved: BLOCKER-02 (null token in EventSource URL) and BLOCKER-03 (EventSource leak on reconnect) are both closed by the four-line insertion in connect() at the correct ordering.

Three warnings exist (WR-01, WR-02, WR-03) from the code review, all pre-existing and none blocking the phase goal. WR-01 (missing `this.eventSource = null` after early-return close) is the only one directly adjacent to the fix and is worth addressing in a follow-up, but does not prevent the blockers from being closed.

Status is `human_needed` because runtime browser testing is required to confirm the timing of connect() vs. Angular DI initialization on first load — the core scenario that motivated BLOCKER-02.

---

_Verified: 2026-05-13T12:45:00Z_
_Verifier: Claude (gsd-verifier)_

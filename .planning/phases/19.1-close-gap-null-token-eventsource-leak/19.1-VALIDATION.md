---
phase: 19.1
slug: close-gap-null-token-eventsource-leak
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-13
---

# Phase 19.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — no frontend test runner configured in this project |
| **Config file** | none |
| **Quick run command** | n/a — static code review only |
| **Full suite command** | n/a |
| **Estimated runtime** | n/a |

---

## Sampling Rate

- **After every task commit:** Code review against the ordering constraints in RESEARCH.md

---

## Wave 0 — Test Scaffolds

None required — no test framework available. Validation is static code review.

---

## Validation Checklist

| Check | Method | Acceptance |
|-------|--------|-----------|
| `eventSource?.close()` is first line of `connect()` | grep/read `job-monitor.service.ts` | Line 1 of `connect()` body |
| Token null-guard before URL construction | Code review | `if (!token) { ... return; }` before `const url = ...` |
| `this.reconnectAttempts = 0` reset present | grep `reconnectAttempts = 0` | Present after null-guard, before `new EventSource()` |
| URL only built when token is non-null | Code review: URL line is after the null-guard return | No path reaches URL build with null token |
| No second call site of `connect()` introduced | `grep -n "connect()" frontend/src/app/features/configuration/pages/job-monitor/job-monitor.component.ts` | Still exactly 2 call sites |
| No other files modified | `git diff --name-only` | Only `job-monitor.service.ts` |

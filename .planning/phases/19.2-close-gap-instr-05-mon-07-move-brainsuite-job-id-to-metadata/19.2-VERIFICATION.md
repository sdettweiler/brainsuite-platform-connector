---
phase: 19.2-close-gap-instr-05-mon-07-move-brainsuite-job-id-to-metadata
verified: 2026-05-13T00:00:00Z
status: human_needed
score: 3/3 must-haves verified (automated checks)
overrides_applied: 0
human_verification:
  - test: "Open a completed scoring job in the SuperAdmin job detail panel at /configuration/admin"
    expected: "References section shows a 'brainsuite_job_id' row with the BrainSuite UUID value (not empty, not absent)"
    why_human: "getExternalIds() reads from BackgroundJob.metadata_ at runtime; requires a real scoring job created after this phase's deployment — cannot verify via grep that the DB record has the key populated"
---

# Phase 19.2: Close INSTR-05/MON-07 Gaps Verification Report

**Phase Goal:** brainsuite_job_id for scoring jobs moves from output JSONB to metadata_ so the References panel in the job detail view displays it; orphaned platform_sync_run_id removed from KNOWN_EXTERNAL_ID_KEYS
**Verified:** 2026-05-13
**Status:** human_needed — all automated checks pass; one human UI test remains
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | After a scoring job completes, the job detail References panel shows brainsuite_job_id | ? UNCERTAIN (needs human) | `getExternalIds()` reads `metadata_` filtered by `KNOWN_EXTERNAL_ID_KEYS`; `brainsuite_job_id` is in that array (line 39 of component); `scoring_job.py` lines 519-523 write it to `metadata_` via merge call. Full round-trip requires a live job created post-deployment. |
| 2 | KNOWN_EXTERNAL_ID_KEYS in the frontend no longer includes platform_sync_run_id | ✓ VERIFIED | Line 39: `const KNOWN_EXTERNAL_ID_KEYS = ['brainsuite_job_id', 'sync_job_id'];` — zero grep matches for `platform_sync_run_id` across all .py/.ts/.html source files |
| 3 | Existing scoring job records are unaffected; only new scoring jobs write brainsuite_job_id to metadata_ | ✓ VERIFIED | No migration or backfill added; update_background_job merge only executes on new job execution paths |
| 4 | brainsuite_job_id remains in BackgroundJob.output (backward compat) | ✓ VERIFIED | scoring_job.py line 570: `"brainsuite_job_id": str(job_id)` in the COMPLETE update's output dict is unchanged |

**Score:** 3/3 automated truths verified; 1 truth requires human UI validation

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/app/services/sync/job_tracker.py` | update_background_job() with optional metadata merge parameter | ✓ VERIFIED | Line 77: `metadata: Optional[dict] = None`; line 115: `job.metadata_ = {**(job.metadata_ or {}), **metadata}` |
| `backend/app/services/sync/scoring_job.py` | _process_asset() calls update_background_job with brainsuite_job_id in metadata after BrainSuite API returns | ✓ VERIFIED | Lines 519-523: `if bg_job_id is not None: await update_background_job(bg_job_id, metadata={"brainsuite_job_id": str(job_id)})` — placed after CreativeScoreResult commit (line 514), before logger.info (line 525) |
| `frontend/src/app/features/configuration/pages/job-monitor/job-detail-panel/job-detail-panel.component.ts` | KNOWN_EXTERNAL_ID_KEYS without platform_sync_run_id | ✓ VERIFIED | Line 39: `const KNOWN_EXTERNAL_ID_KEYS = ['brainsuite_job_id', 'sync_job_id'];` |
| `backend/tests/services/test_instrumentation.py` | test_scoring_output_schema asserts brainsuite_job_id present in metadata dict | ✓ VERIFIED | Lines 506-518: conditional for-loop over `update_metadata_calls` asserts `"brainsuite_job_id" in meta_passed` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `scoring_job.py` | `job_tracker.py` | `update_background_job(bg_job_id, metadata={"brainsuite_job_id": ...})` after line 514 | ✓ WIRED | Line 39 import: `from app.services.sync.job_tracker import create_background_job, update_background_job`; call at lines 520-523 |
| `job_tracker.py` | `BackgroundJob.metadata_` | JSONB merge: `{**(job.metadata_ or {}), **metadata}` | ✓ WIRED | Line 115: exact merge expression preserving existing keys (asset_id, creative_score_result_id) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `job-detail-panel.component.ts` | `KNOWN_EXTERNAL_ID_KEYS` filtered against `jobDetail.metadata_` | `getExternalIds()` reads `metadata_` JSONB from GET /jobs/{id}; `metadata_` populated via `update_background_job(metadata={"brainsuite_job_id": ...})` in scoring_job.py | Yes — real BrainSuite UUID written from API response; not hardcoded | ✓ FLOWING (code path verified; runtime behavior needs human test) |

### Behavioral Spot-Checks

Step 7b: SKIPPED for the live-stack component test (requires running server + real BrainSuite job). Static code path verified via grep above.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| metadata merge param exists in update_background_job | `grep -n "metadata: Optional\[dict\]" job_tracker.py` | line 77 match | ✓ PASS |
| JSONB merge expression present | `grep -n "job\.metadata_ = {" job_tracker.py` | line 115: `{**(job.metadata_ or {}), **metadata}` | ✓ PASS |
| New metadata call in scoring_job.py | `grep -n 'metadata={"brainsuite_job_id"' scoring_job.py` | line 522 | ✓ PASS |
| brainsuite_job_id in output unchanged | `grep -n '"brainsuite_job_id"' scoring_job.py` | lines 522 (metadata) + 570 (output) | ✓ PASS |
| platform_sync_run_id absent from all source | `grep -rn "platform_sync_run_id" backend/ frontend/` | zero matches | ✓ PASS |
| KNOWN_EXTERNAL_ID_KEYS has exactly 2 items | `grep -n "KNOWN_EXTERNAL_ID_KEYS" job-detail-panel.component.ts` | line 39: `['brainsuite_job_id', 'sync_job_id']` | ✓ PASS |
| brainsuite_job_id metadata assertion in test | `grep -n "brainsuite_job_id.*meta" test_instrumentation.py` | lines 506-518 | ✓ PASS |
| Task commits exist in git log | `git log --oneline bcfdec5 dc6e1d6` | both hashes confirmed with correct messages | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| INSTR-05 | 19.2-01-PLAN.md | Every job record includes internal job ID and any external API job IDs (e.g. BrainSuite job ID, platform sync run ID) | ✓ SATISFIED | brainsuite_job_id now written to BackgroundJob.metadata_ via update_background_job merge; getExternalIds() surfaces it in References panel |
| MON-07 | 19.2-01-PLAN.md | Every job detail view displays the internal job ID and any external API job IDs | ✓ SATISFIED | KNOWN_EXTERNAL_ID_KEYS reduced to `['brainsuite_job_id', 'sync_job_id']` — platform_sync_run_id (never populated) removed; References panel will display brainsuite_job_id from metadata_ |

No orphaned requirements: REQUIREMENTS.md traceability maps INSTR-05 to Phase 17 and MON-07 to Phase 19; phase 19.2 is the gap-close sub-phase for both. Both IDs are claimed in the plan `requirements` field and both are addressed by the implementation.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| test_instrumentation.py | 178, 468 | `pass` in `except Exception` | Info | Intentional — test harness swallowing unmocked BrainSuite HTTP errors; documented inline; not a stub |

No blockers found.

### Human Verification Required

#### 1. References Panel Displays brainsuite_job_id for Scoring Jobs

**Test:** In a running stack, trigger a scoring job for any asset (or locate a scoring job completed after this phase's deployment). Open the job detail panel from /configuration/admin. Expand or scroll to the References section.

**Expected:** A row with key `brainsuite_job_id` and a non-empty UUID value appears in the References section.

**Why human:** The full round-trip — scoring job execution writing to BackgroundJob.metadata_, API endpoint returning metadata_ in the job detail response, Angular component calling getExternalIds() — can only be observed end-to-end in a live stack. All three layers are individually verified in code, but their composition under real runtime conditions requires browser-level observation. No smoke test can substitute for checking the rendered UI panel.

### Gaps Summary

No gaps. All four artifacts are substantive and wired. Both commits (bcfdec5, dc6e1d6) exist with correct messages. The one outstanding item is a human UI walkthrough to confirm the data-flow works end-to-end in a live stack — it is not a gap in the implementation but a validation gate that cannot be automated.

---

_Verified: 2026-05-13_
_Verifier: Claude (gsd-verifier)_

---
phase: 7
slug: score-trend-performer-highlights-performance-tab
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-30
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Angular Material + custom CSS variables |
| Preset | not applicable |
| Component library | Angular Material MDC (already installed) |
| Icon library | Bootstrap Icons (already installed: `bootstrap-icons`) |
| Font | Nunito Sans (already installed via Google Fonts) |

Source: `frontend/src/styles.scss` — no shadcn/tailwind present; existing Angular project.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, chip margins |
| sm | 8px | Grid card inner gaps, pillar-card gaps, badge padding |
| md | 16px | Default element spacing, section padding, tile internal padding |
| lg | 24px | Section padding, major gaps between dashboard panels |
| xl | 32px | Layout column gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:

| Location | Value | Justification |
|----------|-------|---------------|
| Score trend chart container height | 200px fixed | Scannable at a glance; does not crowd the grid |
| `.perf-mini-tiles` gap | 12px | Three-column compact layout; 8px is too tight, 16px wastes horizontal space in a two-tile row. Accepted as a one-off layout exception. |

---

## Typography

Exactly 4 sizes, 2 weights: 400 regular, 600 semibold:

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Label / metric name / category header | 12px | 600 | 1.2 |
| Body | 14px | 400 | 1.5 |
| Heading (section titles, tile headers) | 16px | 600 | 1.2 |
| Display (large metric values) | 20px | 600 | 1.2 |

Source: `frontend/src/styles.scss` — `html` base is 14px/1.5; existing heading and display elements use weight variants that are consolidated here to 400/600 only. Visual hierarchy is established through font-size contrast (20px vs 14px vs 12px), not weight variation.

Component-internal sizing notes (these are NOT new type sizes — they reference the 12px label token):
- Performer badge text: references the **12px label token** — render at 12px / 600 / uppercase / letter-spacing 0.5px. Do not declare a separate 10px token.
- Mini data tile labels ("Spend", "Impressions"): references the **12px label token** — render at 12px / 600 / uppercase. Do not declare a separate 11px token.
- ECharts axis labels: set `axisLabel.fontSize: 12` (ECharts internal config — closest standard token; overrides ECharts default without introducing a new design token).
- Metric name/value rows: use the **14px body token** (400 for names, 600 for values). The former 13px spec is absorbed — 1px difference is imperceptible.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--bg-primary: #1B1B1B` (dark) / `#F5F6FA` (light) | Page background, chart canvas background |
| Secondary (30%) | `--bg-card: #2C2C2C` (dark) / `#FFFFFF` (light) | All tile/card surfaces, dialog background, score trend panel background |
| Accent (10%) | `--accent: #FF7700` | Score trend line color, active tab underline, date range picker focus ring |
| Success | `--success: #2ECC71` | "Top Performer" badge background tint (`rgba(46,204,113,0.15)`) and text |
| Error | `--error: #E74C3C` | "Below Average" badge background tint (`rgba(231,76,60,0.15)`) and text |
| Warning | `--warning: #F39C12` | Engagement category icon color |
| Destructive | `--error: #E74C3C` | Not applicable to this phase — no destructive actions |

Source: `frontend/src/styles.scss` — all CSS variables confirmed present.

Accent reserved for:
- Score trend ECharts line stroke
- Date range picker focus border
- Active mat-tab underline indicator

Metric category icon colors (Claude's discretion, consistent with existing semantic palette):
- Delivery: `#4285F4` (blue — matches `.platform-google-ads`)
- Engagement: `--warning` (`#F39C12` orange/amber)
- Conversions: `--success` (`#2ECC71` green)
- Video: `#9C27B0` (purple — not yet used; introduce as one-off inline style only)
- Platform-specific: `--text-secondary` (`#A0A0A0` grey)

---

## Component Inventory

### 1. Aggregate Score Trend Panel (new — above dashboard grid)

**Location:** `dashboard.component.ts` — insert above the existing creative grid, below the filter bar.

**Structure:**
```
.score-trend-panel (.card)
  ├── .score-trend-header
  │   ├── h4 "Average BrainSuite Score"
  │   └── app-date-range-picker (reuse existing, 30-day default)
  └── div.echart-box [echarts] [options]="scoreTrendOptions"
```

**Dimensions:** full-width container; chart height 200px.

**ECharts config contract:**
- Chart type: `LineChart`
- X-axis: date strings (YYYY-MM-DD), `axisLabel.fontSize: 12`
- Y-axis: 0–100 domain, `axisLabel.fontSize: 12`
- Line color: `#FF7700` (accent)
- Line width: 2px, no area fill
- Tooltip: show date + score value (format: "Score: {value}")
- `DataZoomComponent`: not needed at 30-day window; omit for phase 7
- No legend (single series)

**Empty state (fewer than 2 data points):**
```
div.score-trend-empty
  i.bi.bi-graph-up (32px, --text-muted)
  p "Not enough data yet"
  p.text-sm "Score trend appears after the first two scoring runs"
```
Center-aligned, container height 120px.

---

### 2. Performer Badge Overlay (existing `.tile-tag` — reposition to bottom-left)

**Current position:** unknown exact offset — score badge is bottom-right, performer pill moves to bottom-left.

**CSS contract:**
```scss
.tile-tag {
  position: absolute;
  bottom: 8px;
  left: 8px;        // moved from current position to bottom-left
  font-size: 12px;  // 12px label token — no separate 10px token
  font-weight: 600;
  padding: 4px 8px; // 4px vertical (xs token), 8px horizontal (sm token)
  border-radius: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  &.tag-top   { background: rgba(46,204,113,0.15); color: #2ECC71; }
  &.tag-below { background: rgba(231,76,60,0.15);  color: #E74C3C; }
}
```

Labels:
- Top 10%: "Top Performer" (green pill)
- Bottom 10%: "Below Average" (red pill)
- Middle 80%: no badge rendered

Guard: if fewer than 10 scored assets in org → backend returns `null` for all `performer_tag` fields → no badges shown in grid.

---

### 3. Performance Tab Redesign (replaces `.kpi-table` section)

**Tab order after this phase:** Performance | Creative Effectiveness (per D-14 — no Score Trend tab).

**Top row — two-column grid:**
```
.perf-top-row (display: grid; grid-template-columns: 1fr 1fr; gap: 16px)
  ├── .perf-kpi-tile (.card style — bg-card, border, border-radius 8px, padding 16px)
  │   ├── h4 "Performance Over Time"
  │   ├── [existing KPI multi-select checkboxes: Spend, CTR, ROAS, etc.]
  │   └── div.echart-box [echarts] [options]="kpiChartOptions" (height: 200px)
  └── .perf-asset-tile (.card style — bg-card, border, border-radius 8px, padding 12px)
      ├── .perf-asset-header (display: flex; justify-content: space-between; align-items: center)
      │   ├── span.perf-asset-label "Creative Asset" (12px / 600 / text-secondary)
      │   └── span.tile-tag [tag-top or tag-below] (if performer_tag not null)
      ├── .perf-preview (thumbnail img or video tag; max-height 140px; object-fit: cover; border-radius 4px)
      ├── .perf-asset-meta (display: flex; justify-content: space-between; margin-top 8px)
      │   ├── span.perf-filename (14px / 400; text-overflow: ellipsis)
      │   └── span.perf-duration (12px / 400 / text-secondary; video only — omit for images)
      └── .perf-mini-tiles (display: flex; gap: 12px; margin-top 8px)
          ├── .perf-mini-tile (flex: 1; bg-primary; border-radius 6px; padding 8px)
          │   ├── span "Spend" (12px / 600 / text-secondary / uppercase — 12px label token)
          │   └── span (14px / 600) formatted value
          └── .perf-mini-tile (same structure for "Impressions")
```

Note: `.perf-asset-tile` uses uniform `padding: 12px` (md/1 token) on all sides, matching the `.ce-pillar-card` pattern. The 12px gap in `.perf-mini-tiles` is a declared spacing exception (see Spacing Scale table).

**Performance Summary section (below top row, full width):**

Section header: "Performance Summary" — h4, margin-bottom 12px.

Each metric category renders as a group:
```
.perf-summary-group (margin-bottom 16px)
  ├── .perf-category-header (display: flex; align-items: center; gap: 8px; margin-bottom 8px)
  │   ├── i.bi.[icon] (16px; color = category color)
  │   └── span (12px / 600 / uppercase / letter-spacing 0.5px; color = category color)
  └── .perf-metrics-grid (display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px)
      └── .perf-metric-row (display: flex; justify-content: space-between; align-items: center;
                            padding: 8px 0; border-bottom: 1px solid var(--border))
          ├── span.metric-name (14px / 400 / text-secondary)
          └── span.metric-value (14px / 600 / text-primary)
```

Category definitions:
| Category | Icon | Color | Metrics |
|----------|------|-------|---------|
| Delivery | `bi-send` | `#4285F4` | spend, impressions, CPM, reach, clicks, frequency, CPP, CPC |
| Engagement | `bi-hand-thumbs-up` | `#F39C12` | CTR, outbound CTR, unique CTR, inline link click CTR, outbound clicks, unique clicks, inline link clicks, post engagements, likes, comments, shares, follows |
| Conversions | `bi-arrow-repeat` | `#2ECC71` | ROAS, purchase ROAS, CVR, conversions, cost per result, conversion value, cost per conversion, purchases, purchase value, leads, cost per lead, app installs, cost per install, in-app purchases, in-app purchase value |
| Video | `bi-play-circle` | `#9C27B0` | video views, VTR, video completions, video plays, video 3-sec watched, video 30-sec watched, video p25/p50/p75/p100, video completion rate, thruplay, cost per thruplay, trueview views, focused view, cost per focused view, cost per view |
| Platform-specific | `bi-grid` | `--text-secondary` | subscribe, offline purchases, offline purchase value, messaging conversations started, estimated ad recallers, estimated ad recall rate |

Null/zero metric handling: **omit entirely** (do not render greyed-out rows). Zero spend is a meaningful edge case — show it. All other zero/null values are omitted from the rendered grid.

---

### 4. "Used in X campaigns" Section (below Performance Summary, full width)

Section header: "Used in {N} campaign{s}" — h4, margin-bottom 12px.

```
.perf-campaigns-list
  └── .perf-campaign-row (display: flex; align-items: center; justify-content: space-between;
                          padding: 8px 0; border-bottom: 1px solid var(--border))
      ├── span.campaign-name (14px / 400)
      └── a.campaign-link [href]="getCampaignUrl(campaign)" target="_blank" rel="noopener noreferrer"
                          [attr.aria-label]="'Open campaign in ' + campaign.platform + ' Ads Manager'"
          └── i.bi.bi-box-arrow-up-right (14px; color: text-secondary; hover: accent)
```

Campaign URL patterns by platform:
| Platform | URL Pattern |
|----------|------------|
| meta | `https://www.facebook.com/adsmanager/manage/campaigns?act={account_id}&campaign_ids={campaign_id}` |
| tiktok | `https://ads.tiktok.com/i18n/account/campaigns?keyword={campaign_id}` |
| google_ads | `https://ads.google.com/aw/campaigns?campaignId={campaign_id}` |
| dv360 | `https://displayvideo.google.com/#ng_nav/p/{account_id}/c/{campaign_id}` |

Empty state (no campaigns): render nothing (section omitted if `campaigns` array is empty).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Score trend panel heading | "Average BrainSuite Score" |
| Score trend date picker label | (inherited from existing DateRangePickerComponent — no change) |
| Score trend empty state heading | "Not enough data yet" |
| Score trend empty state body | "Score trend appears after the first two scoring runs" |
| Score trend loading state | (skeleton — no text label needed) |
| Performer badge — top 10% | "Top Performer" |
| Performer badge — bottom 10% | "Below Average" |
| Performance tab — KPI tile heading | "Performance Over Time" |
| Performance tab — asset tile label | "Creative Asset" |
| Performance tab — spend mini-tile label | "Spend" |
| Performance tab — impressions mini-tile label | "Impressions" |
| Performance Summary section heading | "Performance Summary" |
| Campaigns section heading | "Used in {N} campaign" / "Used in {N} campaigns" (pluralised) |
| Error state (score trend API fails) | "Could not load score data. Refresh to try again." |
| Error state (asset detail API fails) | (existing pattern — no change for this phase) |
| Primary CTA | None — this phase is display-only. No primary action button introduced. |
| Destructive actions | None in this phase. |

---

## Interaction Contract

### Score Trend Panel
- Date range change fires a new API request; chart re-renders with fade transition (ECharts built-in).
- Loading state: show skeleton shimmer in the chart container (`.skeleton` class from `styles.scss`).
- No click/drill-down on data points for phase 7 (display only).

### Performer Badges
- Static display on hover of the badge itself: `MatTooltip` with text "Top 10% of your scored creatives" (top) or "Bottom 10% of your scored creatives" (below average).
- No click interaction on the badge itself.

### Performance Tab
- KPI chart: existing multi-select checkbox behavior unchanged.
- Campaign link: opens in new tab (`target="_blank"`). `aria-label` declared in component inventory above.
- Metric rows: non-interactive (no hover, no click).
- Asset thumbnail: non-interactive (no click to expand in this tab).

---

## States and Guards

| State | Component | Behaviour |
|-------|-----------|-----------|
| Fewer than 2 trend data points | Score Trend Panel | Show empty state component |
| Fewer than 10 scored assets in org | Performer Badges | Backend returns `null`; no badges rendered |
| Score trend API error | Score Trend Panel | Show inline error text; hide chart |
| No campaigns | Campaigns Section | Section omitted entirely |
| Video metrics on image asset | Video category group | Omit entire Video group (all values null) |
| Null spend | Spend mini-tile | Show "$0.00" (zero spend is meaningful) |
| Null impressions | Impressions mini-tile | Show "0" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — Angular project |
| third-party | none | not applicable |

No new npm packages introduced. All components use existing installed libraries:
- `ngx-echarts ^17.2.0` (already installed)
- `@angular/material` (already installed)
- `bootstrap-icons` (already installed)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---
phase: 12-credentials-app-name-settings-ui
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - frontend/src/app/features/configuration/pages/brainsuite-apps.component.ts
autonomous: false
requirements: [BSCFG-01, BSCFG-02, BSCFG-03, BSCFG-04, VSAF-01, VSAF-02]
must_haves:
  truths:
    - "Settings page has a BrainSuite Credentials section card above the existing app list"
    - "Admin can enter Client ID and Client Secret and save them"
    - "Client Secret field is masked and read-only when secret exists; Change button enables editing"
    - "Discard changes button in secret edit mode reverts to read-only placeholder without API call"
    - "Credentials section auto-collapses when credentials exist AND test connection passed"
    - "Collapsed summary row shows truncated Client ID and Connection verified status with Edit button"
    - "Test Connection button is disabled until credentials exist in DB"
    - "Test Connection shows spinner during request and colored inline status block with result"
    - "Each app row has an accordion chevron that expands an inline panel with BrainSuite API App Name input"
    - "Saving app name or clicking away closes the accordion panel"
    - "When saving credentials or app name and values changed, a re-score MatDialog appears if org has scored assets"
    - "Re-score dialog has Keep existing scores and Re-score all assets buttons"
    - "Re-score all assets button fires POST /brainsuite-config/rescore-all and shows toast"
  artifacts:
    - path: "frontend/src/app/features/configuration/pages/brainsuite-apps.component.ts"
      provides: "BrainSuite credentials section, app name accordion, re-score dialog, all interactions"
      min_lines: 500
  key_links:
    - from: "frontend/brainsuite-apps.component.ts"
      to: "/api/v1/brainsuite-config/credentials"
      via: "ApiService GET/PUT calls"
      pattern: "api\\.(get|put).*brainsuite-config/credentials"
    - from: "frontend/brainsuite-apps.component.ts"
      to: "/api/v1/brainsuite-config/test-connection"
      via: "ApiService POST call"
      pattern: "api\\.post.*brainsuite-config/test-connection"
    - from: "frontend/brainsuite-apps.component.ts"
      to: "/api/v1/brainsuite-config/rescore-all"
      via: "ApiService POST call from dialog callback"
      pattern: "api\\.post.*brainsuite-config/rescore-all"
    - from: "frontend/brainsuite-apps.component.ts"
      to: "/api/v1/brainsuite-config/apps/{app_id}/system-app-name"
      via: "ApiService PATCH call"
      pattern: "api\\.patch.*brainsuite-config/apps"
---

<objective>
Modify `brainsuite-apps.component.ts` to add: (A) BrainSuite Credentials section card with save, secret masking, test connection, and auto-collapse; (B) App row accordion for system_app_name editing; (C) Re-score MatDialog triggered on config changes when org has scored assets.

Purpose: This delivers all Phase 12 frontend requirements (BSCFG-01 through BSCFG-04, VSAF-01, VSAF-02) — the admin-facing UI for configuring BrainSuite credentials and app names.
Output: Fully functional brainsuite-apps.component.ts with credentials section, accordion, and dialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-credentials-app-name-settings-ui/12-CONTEXT.md
@.planning/phases/12-credentials-app-name-settings-ui/12-PATTERNS.md
@.planning/phases/12-credentials-app-name-settings-ui/12-RESEARCH.md
@.planning/phases/12-credentials-app-name-settings-ui/12-UI-SPEC.md
@.planning/phases/12-credentials-app-name-settings-ui/12-02-SUMMARY.md

<interfaces>
<!-- Backend API contracts created in Plan 02. -->

GET /api/v1/brainsuite-config/credentials
Response: { client_id: string | null, has_secret: boolean, has_scored_assets: boolean }

PUT /api/v1/brainsuite-config/credentials
Request: { client_id: string, client_secret: string }  // empty client_secret = keep existing
Response: { changed: boolean, has_scored_assets: boolean }

POST /api/v1/brainsuite-config/test-connection
Request: {} (empty body)
Response: { success: boolean, message: string }

PATCH /api/v1/brainsuite-config/apps/{app_id}/system-app-name
Request: { system_app_name: string | null }
Response: { changed: boolean, has_scored_assets: boolean }

POST /api/v1/brainsuite-config/rescore-all
Request: {} (empty body)
Response: { reset_count: number }

<!-- Existing BrainsuiteApp list endpoint (already consumed by component) -->
GET /api/v1/platforms/brainsuite-apps
Response: BrainsuiteApp[] — now includes system_app_name: string | null (from Plan 01 schema extension)

<!-- Angular patterns from existing component (brainsuite-apps.component.ts) -->
```typescript
// Existing state (keep all of this):
apps: BrainsuiteApp[] = [];
loading = true;
saving = false;
showForm = false;
editingApp: BrainsuiteApp | null = null;
appForm?: FormGroup;

// Existing constructor DI:
constructor(
  private api: ApiService,
  private fb: FormBuilder,
  private snackBar: MatSnackBar,
) {}
```

<!-- MatDialog pattern from organization.component.ts -->
```typescript
// Inline dialog component pattern:
@Component({
  standalone: true,
  imports: [CommonModule, FormsModule, MatButtonModule, MatDialogModule],
  template: `...`,
  styles: [`...`],
})
export class ChangeRoleDialogComponent {
  constructor(
    public dialogRef: MatDialogRef<ChangeRoleDialogComponent>,
    @Inject(MAT_DIALOG_DATA) public data: { ... },
  ) {}
}

// Dialog open pattern:
const ref = this.dialog.open(ChangeRoleDialogComponent, {
  data: { ... },
  width: '320px',
});
ref.afterClosed().subscribe((result) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RescoreDialogComponent + credentials section + accordion + all interactions</name>
  <files>frontend/src/app/features/configuration/pages/brainsuite-apps.component.ts</files>
  <read_first>
    frontend/src/app/features/configuration/pages/brainsuite-apps.component.ts
    frontend/src/app/features/configuration/pages/organization.component.ts
    frontend/src/app/core/services/api.service.ts
    .planning/phases/12-credentials-app-name-settings-ui/12-UI-SPEC.md
    .planning/phases/12-credentials-app-name-settings-ui/12-CONTEXT.md
    .planning/phases/12-credentials-app-name-settings-ui/12-PATTERNS.md
  </read_first>
  <action>
This is a large modification to a single file. The component file must be rewritten to include all new functionality while preserving all existing functionality (app list, add/edit form, delete, info section).

### STEP 1: Add imports at top of file

Add these imports to the existing import block (lines 1-11):
```typescript
import { MatDialogModule, MatDialog, MatDialogRef, MAT_DIALOG_DATA } from '@angular/material/dialog';
import { Inject } from '@angular/core';
```
Add `MatDialogModule` to the component's `imports` array.

### STEP 2: Create inline RescoreDialogComponent BEFORE BrainsuiteAppsComponent

Place this BEFORE the `@Component` decorator of `BrainsuiteAppsComponent`, following the `organization.component.ts` `ChangeRoleDialogComponent` pattern:

```typescript
@Component({
  standalone: true,
  imports: [CommonModule, MatButtonModule, MatDialogModule],
  template: `
    <div class="rescore-dialog">
      <h3>Configuration changed</h3>
      <p>You've updated BrainSuite credentials or app names. Would you like to re-score all previously scored assets under the new configuration?</p>
      <p class="sub-note">Re-scoring queues all SCORED assets for the next 15-minute scoring cycle. This cannot be undone.</p>
      <div class="dialog-actions">
        <button mat-stroked-button (click)="dialogRef.close('keep')">Keep existing scores</button>
        <button mat-flat-button class="rescore-btn" (click)="dialogRef.close('rescore')">Re-score all assets</button>
      </div>
    </div>
  `,
  styles: [`
    .rescore-dialog { padding: 8px 4px; min-width: 260px; max-width: 440px; }
    .rescore-dialog h3 { font-size: 16px; font-weight: 600; margin: 0 0 12px; }
    .rescore-dialog p { font-size: 13px; color: var(--text-secondary); margin: 0 0 12px; }
    .rescore-dialog .sub-note { font-size: 13px; color: var(--text-secondary); }
    .dialog-actions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 20px; }
    .rescore-btn { background: var(--error) !important; color: white !important; }
  `],
})
export class RescoreDialogComponent {
  constructor(public dialogRef: MatDialogRef<RescoreDialogComponent>) {}
}
```

### STEP 3: Update BrainsuiteApp interface

Add `system_app_name` to the existing `BrainsuiteApp` interface:
```typescript
interface BrainsuiteApp {
  id: string;
  name: string;
  app_type: 'VIDEO' | 'IMAGE' | 'MIXED';
  is_default_for_video: boolean;
  is_default_for_image: boolean;
  description?: string;
  system_app_name?: string;  // NEW — Phase 12
}
```

### STEP 4: Add new state properties to BrainsuiteAppsComponent class

Add these after the existing state properties (after `appForm?: FormGroup;`, before constructor):

```typescript
// Credentials section state (Phase 12)
credentials: { client_id: string | null; has_secret: boolean; has_scored_assets: boolean } | null = null;
credentialsForm?: FormGroup;
credentialsCollapsed = false;
secretEditMode = false;
savingCredentials = false;
testingConnection = false;
testResult: { success: boolean; message: string } | null = null;

// Accordion state (Phase 12)
expandedAppId: string | null = null;
appNameForms: Record<string, FormGroup> = {};
savingAppName: Record<string, boolean> = {};
```

### STEP 5: Update constructor to inject MatDialog

```typescript
constructor(
  private api: ApiService,
  private fb: FormBuilder,
  private snackBar: MatSnackBar,
  private dialog: MatDialog,
) {}
```

### STEP 6: Update ngOnInit

Add credentials loading:
```typescript
ngOnInit(): void {
  this.loadApps();
  this.loadCredentials();
}
```

### STEP 7: Add new methods to component class

Add these methods:

```typescript
// --- Credentials section methods ---

loadCredentials(): void {
  this.api.get<{ client_id: string | null; has_secret: boolean; has_scored_assets: boolean }>('/brainsuite-config/credentials').subscribe({
    next: (data) => {
      this.credentials = data;
      const hasCredentials = !!data.client_id && data.has_secret;
      // Restore collapsed state from localStorage if credentials exist
      if (hasCredentials) {
        const saved = localStorage.getItem('bs_credentials_collapsed');
        if (saved === 'true') {
          this.credentialsCollapsed = true;
        }
      }
      this.initCredentialsForm();
    },
    error: () => {
      this.credentials = { client_id: null, has_secret: false, has_scored_assets: false };
      this.initCredentialsForm();
    },
  });
}

initCredentialsForm(): void {
  this.credentialsForm = this.fb.group({
    client_id: [this.credentials?.client_id || '', Validators.required],
    client_secret: [''],
  });
  this.secretEditMode = false;
}

get hasCredentials(): boolean {
  return !!this.credentials?.client_id && !!this.credentials?.has_secret;
}

expandCredentials(): void {
  this.credentialsCollapsed = false;
  localStorage.removeItem('bs_credentials_collapsed');
}

enableSecretEdit(): void {
  this.secretEditMode = true;
  this.credentialsForm?.get('client_secret')?.setValue('');
}

cancelSecretEdit(): void {
  this.secretEditMode = false;
  this.credentialsForm?.get('client_secret')?.setValue('');
}

saveCredentials(): void {
  if (this.credentialsForm?.invalid) return;
  this.savingCredentials = true;
  const payload = this.credentialsForm!.value;
  this.api.put<{ changed: boolean; has_scored_assets: boolean }>('/brainsuite-config/credentials', payload).subscribe({
    next: (resp) => {
      this.savingCredentials = false;
      this.snackBar.open('Credentials saved', '', { duration: 3000 });
      this.loadCredentials();
      if (resp.changed && resp.has_scored_assets) {
        this.openRescoreDialog();
      }
    },
    error: () => { this.savingCredentials = false; },
  });
}

// --- Test Connection methods ---

testConnection(): void {
  this.testingConnection = true;
  this.testResult = null;
  this.api.post<{ success: boolean; message: string }>('/brainsuite-config/test-connection', {}).subscribe({
    next: (result) => {
      this.testingConnection = false;
      this.testResult = result;
      if (result.success && this.hasCredentials) {
        this.credentialsCollapsed = true;
        localStorage.setItem('bs_credentials_collapsed', 'true');
      }
    },
    error: (err) => {
      this.testingConnection = false;
      this.testResult = { success: false, message: 'Could not reach BrainSuite — check your network connection' };
    },
  });
}

// --- Accordion methods ---

toggleAccordion(app: BrainsuiteApp): void {
  if (this.expandedAppId === app.id) {
    this.expandedAppId = null;
  } else {
    this.expandedAppId = app.id;
    if (!this.appNameForms[app.id]) {
      this.appNameForms[app.id] = this.fb.group({
        system_app_name: [app.system_app_name || ''],
      });
    }
  }
}

saveSystemAppName(app: BrainsuiteApp): void {
  const form = this.appNameForms[app.id];
  if (!form) return;
  this.savingAppName[app.id] = true;
  this.api.patch<{ changed: boolean; has_scored_assets: boolean }>(
    `/brainsuite-config/apps/${app.id}/system-app-name`,
    form.value,
  ).subscribe({
    next: (resp) => {
      this.savingAppName[app.id] = false;
      app.system_app_name = form.value.system_app_name;
      this.expandedAppId = null;
      this.snackBar.open('App name saved', '', { duration: 3000 });
      if (resp.changed && resp.has_scored_assets) {
        this.openRescoreDialog();
      }
    },
    error: () => { this.savingAppName[app.id] = false; },
  });
}

onClickAway(event: Event): void {
  if (this.expandedAppId) {
    const target = event.target as HTMLElement;
    if (!target.closest('.app-row') && !target.closest('.accordion-panel')) {
      this.expandedAppId = null;
    }
  }
}

// --- Re-score dialog ---

openRescoreDialog(): void {
  const ref = this.dialog.open(RescoreDialogComponent, {
    width: '480px',
    maxWidth: '480px',
  });
  ref.afterClosed().subscribe((action: string) => {
    if (action === 'rescore') {
      this.api.post('/brainsuite-config/rescore-all', {}).subscribe({
        next: () => {
          this.snackBar.open('Assets queued for re-scoring', '', { duration: 4000 });
        },
      });
    }
  });
}
```

### STEP 8: Update component template

Replace the component template (between backticks in `template: \`...\``) with the full template below. The template preserves ALL existing sections and adds new sections.

The complete template structure is:

```html
<div class="page-container" (click)="onClickAway($event)">

  <!-- A. BrainSuite Credentials Section (NEW - Phase 12, D-01) -->
  <section class="config-section" *ngIf="credentials">

    <!-- Collapsed state (D-02) -->
    <div *ngIf="credentialsCollapsed && hasCredentials" class="credentials-summary">
      <i class="bi bi-check-circle summary-icon"></i>
      <span>Client ID: {{ credentials!.client_id?.substring(0, 8) }}... — Connection verified</span>
      <button mat-stroked-button class="edit-credentials-btn" (click)="expandCredentials()">Edit credentials</button>
    </div>

    <!-- Expanded state -->
    <ng-container *ngIf="!credentialsCollapsed || !hasCredentials">
      <div class="section-header">
        <div>
          <h2>BrainSuite Credentials</h2>
          <p>Connect your BrainSuite account to enable creative scoring</p>
        </div>
      </div>
      <div class="section-body">
        <form [formGroup]="credentialsForm!" (ngSubmit)="saveCredentials()">

          <!-- Client ID field -->
          <div class="form-full">
            <mat-form-field appearance="outline" class="w-full">
              <mat-label>Client ID</mat-label>
              <input matInput formControlName="client_id" />
            </mat-form-field>
          </div>

          <!-- Client Secret field with Change/Discard pattern (D-06) -->
          <div class="form-full">
            <div class="secret-field-row">
              <mat-form-field appearance="outline" class="secret-input">
                <mat-label>Client Secret</mat-label>
                <input matInput
                  [type]="'password'"
                  formControlName="client_secret"
                  [readonly]="hasCredentials && !secretEditMode"
                  [placeholder]="hasCredentials && !secretEditMode ? '\u25CF\u25CF\u25CF\u25CF\u25CF\u25CF\u25CF\u25CF (saved)' : ''"
                />
              </mat-form-field>
              <button *ngIf="hasCredentials && !secretEditMode"
                mat-stroked-button type="button" class="change-secret-btn" (click)="enableSecretEdit()">
                Change secret
              </button>
              <button *ngIf="secretEditMode"
                mat-stroked-button type="button" class="change-secret-btn" (click)="cancelSecretEdit()">
                Discard changes
              </button>
            </div>
          </div>

          <!-- Save button -->
          <div class="form-actions">
            <button mat-flat-button type="submit" class="save-btn"
              [disabled]="credentialsForm!.get('client_id')!.invalid || savingCredentials">
              <mat-spinner *ngIf="savingCredentials" diameter="16"></mat-spinner>
              {{ savingCredentials ? 'Saving...' : 'Save Credentials' }}
            </button>
          </div>
        </form>

        <!-- Test Connection (D-08, D-09, D-10) -->
        <div class="test-connection-row">
          <button mat-stroked-button type="button"
            [disabled]="!hasCredentials || testingConnection"
            (click)="testConnection()">
            <mat-spinner *ngIf="testingConnection" diameter="16"></mat-spinner>
            <i *ngIf="!testingConnection" class="bi bi-plug"></i>
            {{ testingConnection ? 'Testing...' : 'Test Connection' }}
          </button>
          <div *ngIf="testResult" class="test-result"
            [class.test-success]="testResult.success"
            [class.test-failure]="!testResult.success">
            <i class="bi" [class.bi-check-circle]="testResult.success" [class.bi-x-circle]="!testResult.success"></i>
            <span>{{ testResult.message }}</span>
          </div>
        </div>
      </div>
    </ng-container>
  </section>

  <!-- B. Brainsuite Apps Section (EXISTING — preserved, with accordion additions) -->
  <section class="config-section">
    <div class="section-header">
      <div>
        <h2>Brainsuite Apps</h2>
        <p>Manage which Brainsuite apps are used to score your creative assets</p>
      </div>
      <button mat-flat-button class="add-btn" (click)="openAdd()">
        <i class="bi bi-plus-lg"></i> Add App
      </button>
    </div>

    <div *ngIf="!loading; else loadingTpl">
      <ng-container *ngFor="let app of apps">
        <div class="app-row">
          <div class="app-icon">
            <i class="bi bi-cpu"></i>
          </div>
          <div class="app-info">
            <span class="app-name">{{ app.name }}</span>
            <span class="app-type-badge" [class]="'type-' + app.app_type.toLowerCase()">{{ app.app_type }}</span>
            <span class="app-desc" *ngIf="app.description">{{ app.description }}</span>
          </div>
          <div class="app-defaults">
            <div class="default-flag" [class.active]="app.is_default_for_video">
              <i class="bi bi-camera-video"></i> Default for Video
            </div>
            <div class="default-flag" [class.active]="app.is_default_for_image">
              <i class="bi bi-image"></i> Default for Image
            </div>
          </div>
          <div class="app-actions">
            <button mat-icon-button (click)="editApp(app)"><i class="bi bi-pencil"></i></button>
            <button mat-icon-button (click)="deleteApp(app)"><i class="bi bi-trash"></i></button>
            <button mat-icon-button (click)="toggleAccordion(app)"
              [attr.aria-label]="expandedAppId === app.id ? 'Collapse app name settings' : 'Expand app name settings'">
              <i class="bi" [class.bi-chevron-down]="expandedAppId !== app.id"
                [class.bi-chevron-up]="expandedAppId === app.id"
                [class.chevron-expanded]="expandedAppId === app.id"></i>
            </button>
          </div>
        </div>

        <!-- Accordion panel (D-04) -->
        <div *ngIf="expandedAppId === app.id" class="accordion-panel">
          <mat-form-field appearance="outline" class="w-full">
            <mat-label>BrainSuite API App Name</mat-label>
            <input matInput [formControl]="appNameForms[app.id]?.get('system_app_name')!" />
          </mat-form-field>
          <p class="accordion-helper">e.g. ACE_VIDEO_SMV_API — the app name parameter used in BrainSuite scoring API calls</p>
          <div class="form-actions">
            <button mat-flat-button class="save-btn"
              [disabled]="savingAppName[app.id]"
              (click)="saveSystemAppName(app)">
              <mat-spinner *ngIf="savingAppName[app.id]" diameter="16"></mat-spinner>
              {{ savingAppName[app.id] ? 'Saving...' : 'Save App Name' }}
            </button>
          </div>
        </div>
      </ng-container>

      <div *ngIf="apps.length === 0" class="empty-apps">
        <i class="bi bi-cpu"></i>
        <span>No Brainsuite apps configured</span>
        <p>Add a Brainsuite app to enable creative scoring</p>
      </div>
    </div>

    <ng-template #loadingTpl>
      <div class="loading-row"><mat-spinner diameter="24"></mat-spinner></div>
    </ng-template>
  </section>

  <!-- C. Add/Edit Form (EXISTING — preserved verbatim) -->
  <section class="config-section" *ngIf="showForm">
    <div class="section-header">
      <div>
        <h2>{{ editingApp ? 'Edit App' : 'Add Brainsuite App' }}</h2>
      </div>
      <button mat-icon-button (click)="cancelForm()"><i class="bi bi-x-lg"></i></button>
    </div>
    <div class="section-body">
      <form [formGroup]="appForm!" (ngSubmit)="saveApp()">
        <div class="form-row">
          <mat-form-field appearance="outline">
            <mat-label>App Name</mat-label>
            <input matInput formControlName="name" />
          </mat-form-field>
          <mat-form-field appearance="outline">
            <mat-label>App Type</mat-label>
            <mat-select formControlName="app_type">
              <mat-option value="VIDEO">Video</mat-option>
              <mat-option value="IMAGE">Image / Static</mat-option>
              <mat-option value="MIXED">Mixed</mat-option>
            </mat-select>
          </mat-form-field>
        </div>
        <div class="form-full">
          <mat-form-field appearance="outline" class="w-full">
            <mat-label>Description (optional)</mat-label>
            <input matInput formControlName="description" />
          </mat-form-field>
        </div>
        <div class="form-checkboxes">
          <mat-checkbox formControlName="is_default_for_video">Set as default for VIDEO assets</mat-checkbox>
          <mat-checkbox formControlName="is_default_for_image">Set as default for IMAGE/CAROUSEL assets</mat-checkbox>
        </div>
        <div class="api-note">
          <i class="bi bi-info-circle"></i>
          <div>
            <p><strong>Note:</strong> Brainsuite app API credentials are configured server-side via environment variables.</p>
            <p>Contact your Brainsuite representative to obtain your App ID and API key.</p>
          </div>
        </div>
        <div class="form-actions">
          <button mat-stroked-button type="button" (click)="cancelForm()">Cancel</button>
          <button mat-flat-button type="submit" class="save-btn" [disabled]="appForm!.invalid || saving">
            <mat-spinner *ngIf="saving" diameter="16"></mat-spinner>
            {{ saving ? 'Saving...' : (editingApp ? 'Update' : 'Create App') }}
          </button>
        </div>
      </form>
    </div>
  </section>

  <!-- D. Info section (EXISTING — preserved verbatim) -->
  <section class="config-section info-section">
    <div class="section-body">
      <div class="info-header">
        <i class="bi bi-question-circle"></i>
        <h3>About Brainsuite Apps</h3>
      </div>
      <p>
        Brainsuite apps are creative intelligence tools that analyze your ad creative and provide
        effectiveness scores including attention, brand recall, emotion, and visual impact.
      </p>
      <ul>
        <li>Each app is optimized for a specific creative format (video or image)</li>
        <li>Default apps are automatically applied to new creative assets of the matching format</li>
        <li>You can override the app assignment at the platform connection level</li>
        <li>ACE Scores and Brainsuite KPIs shown in the dashboard are currently simulated data</li>
      </ul>
    </div>
  </section>
</div>
```

### STEP 9: Update component styles

Add these NEW styles to the existing styles block (keep all existing styles, append these):

```css
/* Phase 12: Credentials section */
.credentials-summary {
  display: flex; align-items: center; gap: 12px; padding: 12px 24px;
}
.credentials-summary .summary-icon { color: var(--success); font-size: 18px; }
.credentials-summary span { flex: 1; font-size: 13px; color: var(--text-secondary); }
.edit-credentials-btn { font-size: 13px; color: var(--accent) !important; }

.secret-field-row { display: flex; align-items: flex-start; gap: 12px; }
.secret-input { flex: 1; }
.change-secret-btn { font-size: 12px; margin-top: 8px; }

.test-connection-row { margin-top: 20px; display: flex; flex-direction: column; gap: 12px; }
.test-connection-row button { align-self: flex-start; display: flex; align-items: center; gap: 8px; }

.test-result {
  display: flex; align-items: center; gap: 8px; padding: 12px 16px;
  border-radius: 8px; font-size: 13px;
}
.test-success { background: rgba(46,204,113,0.15); color: var(--success); }
.test-failure { background: rgba(231,76,60,0.15); color: var(--error); }

/* Phase 12: Accordion */
.accordion-panel {
  padding: 16px 24px; background: var(--bg-secondary);
  border-bottom: 1px solid var(--border);
}
.accordion-helper { font-size: 12px; color: var(--text-muted); margin: -8px 0 16px; }
.chevron-expanded { color: var(--accent) !important; }
```

### IMPORTANT NOTES FOR EXECUTOR:

1. **Preserve ALL existing methods** (loadApps, openAdd, editApp, saveApp, cancelForm, deleteApp) exactly as they are.
2. **Do NOT remove or modify** the existing `interface BrainsuiteApp` fields — only add `system_app_name`.
3. **The (click)="onClickAway($event)"** is on the page-container div — this handles accordion close-on-click-away per D-04.
4. **The secret field placeholder** uses Unicode bullet characters `\u25CF` (8 of them) followed by ` (saved)` to render `●●●●●●●● (saved)`.
5. **The test result block** uses `.test-success` / `.test-failure` classes, NOT badge-success/badge-error (those are different patterns).
6. **The credentialsForm requires `client_id` only** — `client_secret` is not required because empty means "keep existing" (D-07). The save button is disabled by `credentialsForm!.get('client_id')!.invalid` not by `credentialsForm!.invalid` — this is intentional so saving with empty secret (keep existing) works.
7. **`api.put`** — verify `ApiService` has a `put` method. If it only has `patch`, use `this.api.patch` for the PUT credentials call, or add a `put` method. Check `api.service.ts`.
8. **`api.post`** — same: verify it exists on ApiService.
  </action>
  <verify>
    <automated>cd frontend && grep -c "BrainSuite Credentials" src/app/features/configuration/pages/brainsuite-apps.component.ts && grep -c "RescoreDialogComponent" src/app/features/configuration/pages/brainsuite-apps.component.ts && grep -c "Test Connection" src/app/features/configuration/pages/brainsuite-apps.component.ts && grep -c "accordion-panel" src/app/features/configuration/pages/brainsuite-apps.component.ts && grep -c "brainsuite-config" src/app/features/configuration/pages/brainsuite-apps.component.ts && echo "All content checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - brainsuite-apps.component.ts contains `RescoreDialogComponent` class definition
    - brainsuite-apps.component.ts contains `class BrainsuiteAppsComponent`
    - Template contains `<h2>BrainSuite Credentials</h2>` (credentials section heading)
    - Template contains `Test Connection` button text
    - Template contains `class="accordion-panel"` (accordion expand panel)
    - Template contains `class="credentials-summary"` (collapsed state row)
    - Template contains `bi-check-circle` and `bi-x-circle` (test result icons)
    - Template contains `bi-chevron-down` and `bi-chevron-up` (accordion icons)
    - Template contains `bi-plug` (test connection icon)
    - Component class has `loadCredentials()` method
    - Component class has `saveCredentials()` method
    - Component class has `testConnection()` method
    - Component class has `toggleAccordion()` method
    - Component class has `saveSystemAppName()` method
    - Component class has `openRescoreDialog()` method
    - Component class has `onClickAway()` method
    - API calls use `/brainsuite-config/credentials` (GET and PUT)
    - API calls use `/brainsuite-config/test-connection` (POST)
    - API calls use `/brainsuite-config/apps/${app.id}/system-app-name` (PATCH)
    - API calls use `/brainsuite-config/rescore-all` (POST)
    - Existing methods preserved: `loadApps`, `openAdd`, `editApp`, `saveApp`, `cancelForm`, `deleteApp`
    - Existing imports preserved: `MatButtonModule`, `MatFormFieldModule`, `MatInputModule`, `MatSelectModule`, `MatCheckboxModule`, `MatProgressSpinnerModule`, `MatSnackBarModule`
    - New imports present: `MatDialogModule`, `MatDialog`, `MatDialogRef`
    - `system_app_name` field added to BrainsuiteApp interface
    - `MatDialog` injected in constructor
    - Styles contain `.credentials-summary`, `.test-result`, `.test-success`, `.test-failure`, `.accordion-panel`, `.accordion-helper`, `.chevron-expanded`
  </acceptance_criteria>
  <done>Component fully modified with credentials section (expanded + collapsed states), secret masking (D-06), test connection (D-08/D-09/D-10), auto-collapse (D-02), accordion app name panels (D-04), re-score dialog (D-12/D-13), and all API wiring. All existing functionality preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of BrainSuite Configuration UI</name>
  <what-built>
BrainSuite Configuration UI on the Brainsuite Apps settings page:
- Credentials section card (Client ID + Secret with masking) above app list
- Test Connection button with colored inline status feedback
- Auto-collapse when credentials saved and test passed
- Accordion chevron on each app row expanding a BrainSuite API App Name input
- Re-score dialog when saving changed config and org has scored assets
  </what-built>
  <how-to-verify>
1. Open the app in browser, navigate to Settings > Brainsuite Apps page
2. **Credentials section visible:** Verify a "BrainSuite Credentials" card appears ABOVE the app list
3. **Enter credentials:** Type a Client ID and Client Secret, click "Save Credentials" — verify "Credentials saved" toast appears
4. **Secret masking:** After save, the secret field should show "●●●●●●●● (saved)" in read-only mode with a "Change secret" button
5. **Change/Discard:** Click "Change secret" — field becomes editable. Click "Discard changes" — returns to masked state without saving
6. **Test Connection:** Click "Test Connection" — verify spinner shows, then colored result block appears below (green if BrainSuite credentials are valid, red if not)
7. **Auto-collapse:** If test connection succeeds, the credentials section should collapse to a summary row showing "Client ID: {prefix}... — Connection verified" with "Edit credentials" button
8. **Edit credentials:** Click "Edit credentials" in collapsed state — section expands again
9. **App accordion:** Click the chevron icon on any app row — verify an inline panel expands below with "BrainSuite API App Name" input and "Save App Name" button
10. **Save app name:** Enter a value (e.g., "ACE_VIDEO_SMV_API"), click "Save App Name" — verify toast and panel closes
11. **Click-away:** Expand accordion again, click outside the panel — verify it closes
12. **Re-score dialog:** If the org has scored assets, saving changed credentials or app name should trigger a "Configuration changed" dialog with "Keep existing scores" and "Re-score all assets" buttons
13. **Existing functionality:** Verify Add App, Edit, Delete still work as before
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to phase verification, or describe any issues found</resume-signal>
  <action>Human visually verifies all UI elements work correctly in the browser</action>
  <verify>Human confirmation that all 13 verification steps pass</verify>
  <done>User types "approved" confirming all credentials, test connection, accordion, and dialog functionality works as expected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | All API calls go through authenticated ApiService with JWT bearer token |
| localStorage | Collapse state stored locally; not security-sensitive (no credentials stored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Information Disclosure | Client Secret in browser | mitigate | Secret is never stored in component state or localStorage. Password input type="password" prevents visual leakage. Read-only placeholder shows bullets, not the actual value. Secret only exists in the form field during active editing. |
| T-12-11 | Information Disclosure | localStorage collapse state | accept | Only stores boolean "true"/"false" under key `bs_credentials_collapsed`. No credential data. Low risk. |
| T-12-12 | Spoofing | API calls without auth | mitigate | All API calls go through ApiService which attaches JWT bearer token. Backend enforces admin role via `get_current_admin`. Non-admin users see 403. |
| T-12-13 | Tampering | Re-score dialog bypass | accept | If user bypasses dialog and calls rescore-all directly, backend still validates admin role and only resets COMPLETE assets. No additional harm possible. |
</threat_model>

<verification>
1. Visual inspection of credentials section in browser (human-verify checkpoint)
2. `grep -c "BrainSuite Credentials" frontend/src/app/features/configuration/pages/brainsuite-apps.component.ts` — returns >= 1
3. `grep -c "RescoreDialogComponent" frontend/src/app/features/configuration/pages/brainsuite-apps.component.ts` — returns >= 2 (class def + dialog.open)
4. `grep -c "brainsuite-config" frontend/src/app/features/configuration/pages/brainsuite-apps.component.ts` — returns >= 4 (one per API endpoint)
5. `grep -c "accordion-panel" frontend/src/app/features/configuration/pages/brainsuite-apps.component.ts` — returns >= 2 (template + styles)
</verification>

<success_criteria>
- Credentials section card visible above app list
- Client ID and Secret can be saved
- Secret field is masked when stored, with Change/Discard pattern
- Test Connection works with spinner and colored result
- Auto-collapse on successful test
- App row accordion with system_app_name input
- Re-score dialog triggers on changed config with scored assets
- All existing app management functionality preserved
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/12-credentials-app-name-settings-ui/12-03-SUMMARY.md`
</output>

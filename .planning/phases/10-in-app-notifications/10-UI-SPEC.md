---
phase: 10
slug: in-app-notifications
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-09
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Angular Material 17 + custom CSS tokens) |
| Preset | not applicable |
| Component library | Angular Material 17 (`@angular/material ^17.3.0`) |
| Icon library | Bootstrap Icons (`bootstrap-icons`) — `bi-*` class pattern |
| Font | Nunito Sans (Google Fonts), fallback: Avenir Next, sans-serif |

No shadcn. No third-party registries. All required components (`MatSnackBar`, `MatMenuTrigger`) are already installed.

Source: `frontend/src/styles.scss`, `frontend/package.json`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, unread badge padding, `notif-btn` badge offsets |
| sm | 8px | Compact button padding, notification item column gap (`gap: 2px` for text stack), `header-right` gap |
| md | 16px | Notification item horizontal padding, menu panel padding |
| lg | 24px | Empty state padding (vertical), header horizontal padding |
| xl | 32px | — |
| 2xl | 48px | — |
| 3xl | 64px | — |

Exceptions:
- Notification unread badge: 18px height, 10px border-radius (pill shape) — matches existing `.custom-badge` in `header.component.ts`
- Notification item min-height: 56px (multi-line content row) — matches existing `.notif-item`
- Notification item gap between icon and content: 10px — matches existing `.notif-item` gap
- Notification icon-wrap top margin: 2px (optical alignment with text) — matches existing `.notif-icon-wrap`
- Toast duration: 8000ms (per D-04, `CONTEXT.md`)

Source: `header.component.ts` existing styles + `CONTEXT.md` D-04

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label / notification title | 13px | 600 (semibold) | 1.4 |
| Caption / notification message | 12px | 400 (regular) | 1.4 |
| Timestamp | 11px | 400 (regular) | 1.0 |

Notes:
- Font: `'Nunito Sans', 'Avenir Next', sans-serif` — applied globally via `html, body` in `styles.scss`
- `.notif-title`: 13px / 600 — existing style in header component
- `.notif-msg`: 12px / 400, `var(--text-secondary)` — existing style
- `.notif-time`: 11px / 400, `var(--text-muted)` — existing style
- Notification menu panel heading "Notifications": 14px / 600 — existing `.notif-header span` style
- "Mark all read" action button: 12px / accent color — existing `.mark-all-btn` style
- Toast message: inherits MatSnackBar defaults (14px / 400); no override needed

Source: `header.component.ts` styles, `styles.scss`

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg-primary)` — dark: `#1B1B1B`, light: `#F5F6FA` | Page background, header background via `var(--bg-secondary)` |
| Secondary (30%) | `var(--bg-card)` — dark: `#2C2C2C`, light: `#FFFFFF` | MatMenu panel background, notification menu dropdown |
| Accent (10%) | `#FF7700` (`var(--accent)`) | Unread badge background, unread dot, "Mark all read" button, `icon-join` icon color, active selected options |
| Destructive | `#E74C3C` (`var(--error)`) | `icon-rejected` icon color — token expired and sync failed icon tint |

Accent reserved for:
- Unread count badge on bell icon (`custom-badge` background)
- Unread indicator dot on notification item (`notif-unread-dot` background)
- "Mark all read" text button label
- `SYNC_COMPLETE` / `SCORING_BATCH_COMPLETE` notification icon tint (reuses `icon-join` — accent color)
- `icon-approved` (`#34A853` green, NOT accent) — reserved for `SYNC_COMPLETE` if a distinct success green is preferred; default: reuse `icon-approved` green for `SYNC_COMPLETE`

Semantic color map for new notification type icons:

| Type | Icon Class | Color |
|------|------------|-------|
| `SYNC_COMPLETE` | `icon-approved` | `#34A853` (green) |
| `SYNC_FAILED` | `icon-rejected` | `var(--error)` = `#E74C3C` |
| `TOKEN_EXPIRED` | `icon-rejected` | `var(--error)` = `#E74C3C` |
| `SCORING_BATCH_COMPLETE` | `icon-join` | `var(--accent)` = `#FF7700` |

Unread notification item row background: `rgba(255, 119, 0, 0.04)` — existing `.notif-item.unread` style.
Toast background: Angular Material default dark (`MatSnackBar` inherits theme — no override).

Source: `styles.scss` CSS variables, `header.component.ts` styles

---

## Component Inventory

All components are already present in the project. No new installs required.

| Component | Module | Phase 10 Usage |
|-----------|--------|---------------|
| `MatSnackBar` | `MatSnackBarModule` | Toast for `SYNC_FAILED` / `TOKEN_EXPIRED` high-priority events (D-03, D-04) |
| `MatMenuTrigger` | `MatMenuModule` (already imported) | `@ViewChild(MatMenuTrigger)` reference to programmatically call `openMenu()` on 'View' toast action |
| Bell `<button mat-icon-button>` | `MatButtonModule` (already imported) | Existing — no change to DOM structure |
| `.custom-badge` | custom CSS (existing) | Existing — no change needed |
| `.notif-unread-dot` | custom CSS (existing) | Existing — no change needed |

New icons to add to `getNotifIcon()` switch:

| `n.type` value | Bootstrap Icon class | Visual meaning |
|----------------|----------------------|----------------|
| `SYNC_COMPLETE` | `bi-check2-circle` | Success / completion (distinct from `bi-check-circle-fill` used by JOIN_APPROVED) |
| `SYNC_FAILED` | `bi-exclamation-triangle-fill` | Warning / error |
| `TOKEN_EXPIRED` | `bi-shield-exclamation` | Auth / security issue |
| `SCORING_BATCH_COMPLETE` | `bi-bar-chart-fill` | Data / analytics event |

Source: `RESEARCH.md` Pattern 6, `CONTEXT.md` Claude's Discretion (icons)

---

## Interaction Contract

### Bell icon state machine

| State | Visual |
|-------|--------|
| No unread | `bi-bell` icon, no badge |
| 1–9 unread | `bi-bell` icon + `.custom-badge` showing numeric count |
| 10+ unread | `bi-bell` icon + `.custom-badge` showing `9+` |

Badge position: `top: 4px; right: 4px` (absolute, inside `.notif-btn`).

### Notification menu (MatMenu `#notifMenu`)

- Opens on bell button click (`[matMenuTriggerFor]="notifMenu"`)
- Also opens programmatically via `MatMenuTrigger.openMenu()` when user clicks 'View' on a toast
- `xPosition="before"` — panel opens leftward of bell
- Max visible items: no hard limit; scrollable via MatMenu default overflow
- Empty state: `bi-bell` icon at 24px + opacity 0.4, text "No notifications"

### Toast (MatSnackBar)

- Triggered by: `SYNC_FAILED` or `TOKEN_EXPIRED` notification with `created_at > lastToastCheckAt`
- One toast per matched notification (no batching)
- Duration: 8000ms
- Action label: "View"
- Action effect: calls `notifMenuTrigger?.openMenu()` then `loadNotifications()`
- `lastToastCheckAt` updated on every poll tick regardless of whether a toast fired

### Polling

- `setInterval(() => this.loadUnreadCount(), 30000)` — existing pattern, extended only
- No SSE. No WebSockets.

### Mark-as-read interactions

- Click any notification item → `markRead(n)` → `POST /users/notifications/{id}/read`
- Click "Mark all read" → `markAllRead()` → `POST /users/notifications/read-all`
- Both exist and are implemented. No new interaction needed.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Menu panel heading | Notifications |
| Empty state heading | No notifications |
| Empty state body | (none — icon only with heading, matches existing pattern) |
| Mark all read CTA | Mark all read |
| Toast action label | View |
| `SYNC_COMPLETE` title | {PLATFORM} Sync Complete |
| `SYNC_COMPLETE` message | Initial sync complete. Your {platform} creatives are now available. |
| `SYNC_FAILED` title | {PLATFORM} Sync Failed |
| `SYNC_FAILED` message | Sync failed for your {platform} account. Check your connection settings. |
| `TOKEN_EXPIRED` title | {PLATFORM} Token Expired |
| `TOKEN_EXPIRED` message | Your {platform} access token has expired. Reconnect to resume syncing. |
| `SCORING_BATCH_COMPLETE` title | Scoring Complete |
| `SCORING_BATCH_COMPLETE` message | {N} creative{s} scored in this batch. |

Platform name formatting: `connection.platform.title()` — e.g. "Meta", "Tiktok", "Google Ads", "Dv360". Planner may wish to normalise to "TikTok" / "DV360" using a display-name lookup. The copy contract uses `{PLATFORM}` as a placeholder for the platform display name.

Destructive actions: none in this phase. No confirmation dialogs required.

Source: `RESEARCH.md` Notification Message Copy table, `CONTEXT.md` D-04

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Angular Material official | `MatSnackBar`, `MatMenuTrigger` | not required — first-party library, already installed |
| shadcn official | none | not applicable — no shadcn in this project |
| third-party | none | not applicable |

No third-party registries. Registry vetting gate: not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---
phase: "19.3"
plan: "02"
type: execute
wave: 2
depends_on:
  - "19.3-01"
files_modified:
  - backend/app/services/sync/scheduler.py
autonomous: true
requirements:
  - TKTOK-01
  - TKTOK-02

must_haves:
  truths:
    - "When scoring_enabled=False, _run_google_ads_asset_downloads returns before create_background_job is called"
    - "When scoring_enabled=False, _run_dv360_asset_downloads returns before create_background_job is called"
    - "When scoring_enabled=False, _run_tiktok_creatives_deferred returns before create_background_job is called"
    - "When scoring_enabled=False, _run_meta_creatives_deferred returns before create_background_job is called"
    - "When scoring_enabled=True, all 4 functions proceed normally (no regression)"
    - "The gate pattern exactly mirrors scoring_job.py:65-70"
  artifacts:
    - path: "backend/app/services/sync/scheduler.py"
      provides: "scoring_enabled gate at entry of all 4 download functions"
      contains: "scoring_enabled"
  key_links:
    - from: "backend/app/services/sync/scheduler.py (_run_*_asset_downloads)"
      to: "app.models.system_config.SystemConfig"
      via: "select(SystemConfig).limit(1)"
      pattern: "scoring_enabled"
---

<objective>
Add SystemConfig.scoring_enabled guard at the entry of all 4 platform asset download functions in scheduler.py, turning the failing Wave 1 test GREEN.

Purpose: Aligns asset download lifecycle with the SuperAdmin auto-scoring toggle. When scoring is disabled, no download jobs are created for any platform (Meta, TikTok, Google Ads, DV360), consistent with the gate already present in scoring_job.py.
Output: scheduler.py with 4 functions each containing an early-return guard; all 4 Wave 1 tests passing.
</objective>

<execution_context>
@/Users/sebastian.dettweiler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastian.dettweiler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sebastian.dettweiler/Claude Code/platform-connector/brainsuite-platform-connector/.planning/ROADMAP.md
@/Users/sebastian.dettweiler/Claude Code/platform-connector/brainsuite-platform-connector/.planning/phases/19.3-close-gap-phase-15-asset-url-upsert-exclusion-scoring-enabled/19.3-RESEARCH.md

<interfaces>
<!-- Gate pattern to replicate — from scoring_job.py:60–70 -->
```python
async with get_session_factory()() as db:
    cfg_result = await db.execute(select(SystemConfig).limit(1))
    system_cfg = cfg_result.scalar_one_or_none()
    if system_cfg is not None and not system_cfg.scoring_enabled:
        logger.info("Scoring batch: auto-scoring disabled by superadmin (scoring_enabled=False), exiting")
        return
```

<!-- _run_google_ads_asset_downloads — scheduler.py:461 -->
<!-- Currently starts with: connection = None; bg_job_id = None; try: async with get_session_factory()() ... -->
<!-- Gate must be inserted BEFORE the try: block that creates bg_job_id -->

<!-- _run_dv360_asset_downloads — scheduler.py:719 -->
<!-- Same structure as google_ads; gate goes before the try: block -->

<!-- _run_tiktok_creatives_deferred — scheduler.py:655 -->
<!-- Gate goes at function start, before any DB work -->

<!-- _run_meta_creatives_deferred — scheduler.py:591 -->
<!-- Gate goes at function start, before any DB work -->

<!-- SystemConfig is already imported inside _run_google_ads_asset_downloads and _run_dv360_asset_downloads -->
<!-- as local imports (from app.models.system_config import SystemConfig) -->
<!-- For _run_tiktok_creatives_deferred and _run_meta_creatives_deferred, check if SystemConfig is imported -->
<!-- and add local import if missing -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scoring_enabled gate to all 4 download functions in scheduler.py</name>
  <files>backend/app/services/sync/scheduler.py</files>

  <read_first>
    - backend/app/services/sync/scheduler.py (read lines 580–720 to see _run_meta_creatives_deferred and _run_tiktok_creatives_deferred entry points; read lines 461–500 and 719–760 to confirm google_ads and dv360 entry points)
    - backend/app/services/sync/scoring_job.py (lines 60–75 — gate pattern to mirror exactly)
  </read_first>

  <action>
For each of the 4 download functions, insert the scoring_enabled gate block as the FIRST executable statement in the function body (before any connection lookup, before any try: block, before any BackgroundJob creation).

Gate block to insert (adapt logger.info message per platform):

```python
# Auto-download gate — mirrors scoring gate in scoring_job.py:68
# download gate mirrors scoring gate: if scoring is disabled, skip asset downloads too
from app.models.system_config import SystemConfig
from sqlalchemy import select
async with get_session_factory()() as _gate_db:
    _system_cfg = (await _gate_db.execute(select(SystemConfig).limit(1))).scalar_one_or_none()
    if _system_cfg is not None and not _system_cfg.scoring_enabled:
        logger.info("Auto-download disabled (scoring_enabled=False); skipping {PLATFORM} asset downloads")
        return
```

Apply to these 4 functions with the platform name substituted in the log message:

1. `_run_google_ads_asset_downloads` (line ~461): "skipping Google Ads asset downloads"
2. `_run_dv360_asset_downloads` (line ~719): "skipping DV360 asset downloads"
3. `_run_tiktok_creatives_deferred` (line ~655): "skipping TikTok asset downloads"
4. `_run_meta_creatives_deferred` (line ~591): "skipping Meta asset downloads"

IMPORTANT NOTES:
- Use local imports inside the function (`from app.models.system_config import SystemConfig`) to avoid circular import risk — match the style already used in _run_google_ads_asset_downloads (line 466) and _run_dv360_asset_downloads (line 724) where SystemConfig is already imported locally.
- Use `_gate_db` and `_system_cfg` as variable names to avoid shadowing any existing `db` or `system_cfg` variables in the function scope below.
- The `select` import: if `select` is already imported at module-level in scheduler.py, do not import it again locally. Check the top of scheduler.py for `from sqlalchemy import select`.
- Do NOT add the gate inside the existing `try:` block — it must be BEFORE the try: so that early return does not skip exception handlers.
- Verify the exact line numbers by reading the file before editing.
  </action>

  <verify>
    <automated>grep -c "scoring_enabled" /Users/sebastian.dettweiler/Claude\ Code/platform-connector/brainsuite-platform-connector/backend/app/services/sync/scheduler.py</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "scoring_enabled" backend/app/services/sync/scheduler.py returns 4 or more (one per function minimum)
    - grep -n "scoring_enabled" backend/app/services/sync/scheduler.py shows hits inside _run_google_ads_asset_downloads, _run_dv360_asset_downloads, _run_tiktok_creatives_deferred, _run_meta_creatives_deferred — confirmed by line numbers matching those functions
    - The gate block appears BEFORE the first `bg_job_id` assignment in each function (grep -n "bg_job_id\|scoring_enabled" scheduler.py and verify order)
    - docker-compose -f docker-compose.dev.yml exec -T backend python -c "import app.services.sync.scheduler" exits 0 (no import error)
  </acceptance_criteria>

  <done>All 4 download functions have scoring_enabled gate as first executable block; scheduler.py imports successfully</done>
</task>

<task type="auto">
  <name>Task 2: Run all 4 phase tests to confirm GREEN</name>
  <files></files>

  <read_first>
    - backend/tests/services/test_scheduler.py (verify the 2 tests from Wave 1 are present)
    - backend/tests/test_tiktok_sync.py (verify the 2 tests from Wave 1 are present)
  </read_first>

  <action>
Run the full set of 4 phase tests to confirm:
- test_upsert_preserves_asset_url: PASSES (exclusion already in place, invariant holds)
- test_upsert_preserves_video_source_url: PASSES (exclusion already in place, invariant holds)
- test_downloads_skipped_when_scoring_disabled: PASSES (gate now present in all 4 functions)
- test_downloads_proceed_when_scoring_enabled: PASSES (gate does not block scoring_enabled=True)

Command:
`docker-compose -f docker-compose.dev.yml exec -T backend pytest backend/tests/test_tiktok_sync.py::test_upsert_preserves_asset_url backend/tests/test_tiktok_sync.py::test_upsert_preserves_video_source_url backend/tests/services/test_scheduler.py::test_downloads_skipped_when_scoring_disabled backend/tests/services/test_scheduler.py::test_downloads_proceed_when_scoring_enabled -v 2>&1 | tail -20`

If test_downloads_skipped_when_scoring_disabled still fails after adding the gate:
1. Check that the gate is truly BEFORE the try: block (not inside it)
2. Check that the mock patch path matches the actual import path in scheduler.py (e.g., `app.services.sync.scheduler.get_session_factory`)
3. Read the test failure message — it will tell you which assertion failed and why

Do NOT skip failures. Fix the gate or the test mock until all 4 tests are GREEN.
  </action>

  <verify>
    <automated>cd /Users/sebastian.dettweiler/Claude\ Code/platform-connector/brainsuite-platform-connector && docker-compose -f docker-compose.dev.yml exec -T backend pytest backend/tests/test_tiktok_sync.py::test_upsert_preserves_asset_url backend/tests/test_tiktok_sync.py::test_upsert_preserves_video_source_url backend/tests/services/test_scheduler.py::test_downloads_skipped_when_scoring_disabled backend/tests/services/test_scheduler.py::test_downloads_proceed_when_scoring_enabled -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - pytest output contains "4 passed" with no failures
    - No test shows "ERROR" status (only PASSED or FAILED are acceptable outcomes; ERROR means import problem)
    - test_downloads_skipped_when_scoring_disabled shows PASSED (this is the key regression test for the gate)
  </acceptance_criteria>

  <done>All 4 tests GREEN; TKTOK-01 and TKTOK-02 download gate coverage confirmed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler.py → SystemConfig | Internal DB read; no external input crosses this boundary |

## STRIDE Threat Register

No threats apply. This plan adds a DB read gate at the entry of 4 background functions. No new network surface, no new auth paths, no new external inputs, no new data exposed to callers. SystemConfig.scoring_enabled mutation is already gated to superadmin-only in the existing API layer (unchanged by this plan).

Disposition: **accept** — no ASVS L1 controls required for an internal feature flag read.
</threat_model>

<verification>
Phase-level verification after both plans complete:

1. Upsert exclusion confirmed: `grep -n "asset_url.*video_source_url\|video_source_url.*asset_url" backend/app/services/sync/tiktok_sync.py` — returns the exclusion comment at lines ~378–379
2. Gate present in all 4 functions: `grep -c "scoring_enabled" backend/app/services/sync/scheduler.py` returns >= 4
3. All 4 tests GREEN: `docker-compose -f docker-compose.dev.yml exec -T backend pytest backend/tests/test_tiktok_sync.py::test_upsert_preserves_asset_url backend/tests/test_tiktok_sync.py::test_upsert_preserves_video_source_url backend/tests/services/test_scheduler.py::test_downloads_skipped_when_scoring_disabled backend/tests/services/test_scheduler.py::test_downloads_proceed_when_scoring_enabled -v 2>&1 | tail -5`
4. No regression in existing scheduler tests: `docker-compose -f docker-compose.dev.yml exec -T backend pytest backend/tests/services/test_scheduler.py -v 2>&1 | tail -10`
</verification>

<success_criteria>
- SystemConfig.scoring_enabled gate present in all 4 `_run_*` download functions in scheduler.py
- Gate position is BEFORE any BackgroundJob creation (early return produces no DB side effects)
- All 4 phase tests pass: 2 upsert invariant tests + 2 scheduler gate tests
- tiktok_sync.py upsert exclusion set already confirmed correct (no code change needed there)
- TKTOK-01 and TKTOK-02 requirements satisfied: TikTok creatives survive re-sync; download respects scoring toggle for all platforms
</success_criteria>

<output>
After completion, create `.planning/phases/19.3-close-gap-phase-15-asset-url-upsert-exclusion-scoring-enabled/19.3-02-SUMMARY.md`
</output>

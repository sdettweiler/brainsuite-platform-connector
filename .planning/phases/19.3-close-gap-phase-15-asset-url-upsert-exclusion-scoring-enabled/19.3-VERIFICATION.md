---
phase: 19.3-close-gap-phase-15-asset-url-upsert-exclusion-scoring-enabled
verified: 2026-05-13T16:30:00Z
status: passed
score: 8/8
overrides_applied: 0
---

# Phase 19.3: Close Gap — Phase 15 (asset_url upsert exclusion + scoring_enabled gate) Verification Report

**Phase Goal:** TikTok asset_url and video_source_url are protected from null during re-sync; asset download respects SystemConfig.scoring_enabled across all platforms
**Verified:** 2026-05-13T16:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

All 3 ROADMAP success criteria plus 5 must-have truths from plan frontmatter verified.

| #  | Truth                                                                                                                | Status     | Evidence                                                                                                       |
|----|----------------------------------------------------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------------------|
| 1  | A re-sync of an already-downloaded TikTok creative does not temporarily null asset_url or video_source_url (SC-1)    | VERIFIED   | tiktok_sync.py line 379: `"asset_url", "video_source_url"` in ON CONFLICT exclusion set — both excluded from `update_cols` dict passed to `set_=` |
| 2  | When SystemConfig.scoring_enabled is false, no asset downloads are initiated for any platform (SC-2)                 | VERIFIED   | scheduler.py: gate at lines 472-476 (Google Ads), 608-612 (Meta), 682-686 (TikTok), 758-762 (DV360) — all 4 return before `bg_job_id` or `create_background_job` |
| 3  | The ON CONFLICT exclusion list in _upsert_records includes asset_url and video_source_url (SC-3)                     | VERIFIED   | tiktok_sync.py lines 365-384: `if c.name not in (... "asset_url", "video_source_url")` — confirmed in production upsert comprehension |
| 4  | test_upsert_preserves_asset_url verifies asset_url survives ON CONFLICT re-sync (plan 01)                            | VERIFIED   | test_tiktok_sync.py line 316: function present with `@pytest.mark.asyncio`; asserts `"asset_url" not in update_cols_keys` |
| 5  | test_upsert_preserves_video_source_url verifies video_source_url survives ON CONFLICT re-sync (plan 01)              | VERIFIED   | test_tiktok_sync.py line 351: function present with `@pytest.mark.asyncio`; asserts `"video_source_url" not in update_cols_keys` |
| 6  | test_downloads_skipped_when_scoring_disabled verifies all 4 platform download functions exit early (plan 01+02)      | VERIFIED   | test_scheduler.py line 70: function present; mock patches `get_session_factory` + `create_background_job`; asserts `call_count == 0`; Wave 2 gate makes this GREEN |
| 7  | test_downloads_proceed_when_scoring_enabled verifies download functions call through when scoring_enabled=True (plan 01+02) | VERIFIED | test_scheduler.py line 115: function present; asserts `mock_create_job.call_count >= 1` |
| 8  | Gate pattern exactly mirrors scoring_job.py:65-70 (plan 02)                                                         | VERIFIED   | scoring_job.py lines 65-70 pattern: `async with get_session_factory()() as db: ... scalar_one_or_none() ... if not scoring_enabled: return`. scheduler.py gates use identical structure with `_gate_db`/`_system_cfg` variable names; all 4 functions have `# Auto-download gate — mirrors scoring gate in scoring_job.py:68` comment |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact                                                    | Expected                                  | Status   | Details                                                                                         |
|-------------------------------------------------------------|-------------------------------------------|----------|-------------------------------------------------------------------------------------------------|
| `backend/app/services/sync/tiktok_sync.py`                  | upsert exclusion for asset_url / video_source_url | VERIFIED | Lines 369-379: both fields in `not in (...)` exclusion tuple of `update_cols` comprehension |
| `backend/app/services/sync/scheduler.py`                    | scoring_enabled gate in all 4 functions   | VERIFIED | 8 occurrences of `scoring_enabled` confirmed via grep; gate BEFORE `bg_job_id` in all 4 functions |
| `backend/tests/test_tiktok_sync.py`                         | upsert exclusion unit tests               | VERIFIED | Lines 316-385: 2 test functions with `@pytest.mark.asyncio`, substantive assertions, symbolically inspect `TikTokRawPerformance.__table__.columns` |
| `backend/tests/services/test_scheduler.py`                  | scoring_enabled gate integration tests    | VERIFIED | Lines 70-155: 2 test functions present, full mock setup using `MagicMock`/`AsyncMock`, correct `scalar_one_or_none` as `MagicMock` (synchronous) |

### Key Link Verification

| From                                                   | To                                          | Via                              | Status   | Details                                                                                           |
|--------------------------------------------------------|---------------------------------------------|----------------------------------|----------|---------------------------------------------------------------------------------------------------|
| `tiktok_sync.py` `_upsert_records`                     | `TikTokRawPerformance` ON CONFLICT exclusion | column name tuple in `not in ()` | VERIFIED | `"asset_url", "video_source_url"` appear at line 379 inside the comprehension filter              |
| `scheduler.py` `_run_google_ads_asset_downloads`       | `SystemConfig.scoring_enabled`              | `select(SystemConfig).limit(1)` + `_gate_db` | VERIFIED | Lines 472-476; gate returns before line 479 `bg_job_id = None` and line 493 `create_background_job` |
| `scheduler.py` `_run_meta_creatives_deferred`          | `SystemConfig.scoring_enabled`              | `select(SystemConfig).limit(1)` + `_gate_db` | VERIFIED | Lines 608-612; gate returns before line 614 `bg_job_id = None` and line 621 `create_background_job` |
| `scheduler.py` `_run_tiktok_creatives_deferred`        | `SystemConfig.scoring_enabled`              | `select(SystemConfig).limit(1)` + `_gate_db` | VERIFIED | Lines 682-686; gate returns before line 688 `bg_job_id = None` and line 695 `create_background_job` |
| `scheduler.py` `_run_dv360_asset_downloads`            | `SystemConfig.scoring_enabled`              | `select(SystemConfig).limit(1)` + `_gate_db` | VERIFIED | Lines 758-762; gate returns before line 765 `bg_job_id = None` and line 782 `create_background_job` |
| `test_scheduler.py` `test_downloads_skipped_when_scoring_disabled` | `scheduler._run_*` (all 4)    | `patch("app.services.sync.scheduler.get_session_factory", ...)` | VERIFIED | All 4 functions imported and called; `create_background_job` patched at correct module path |
| `test_tiktok_sync.py` upsert tests                     | `app.models.performance.TikTokRawPerformance` | `from app.models.performance import TikTokRawPerformance` | VERIFIED | Import path confirmed correct in both test functions (note: not `app.models.tiktok` — deviation from original plan template, corrected during execution) |

### Data-Flow Trace (Level 4)

Not applicable. Phase 19.3 adds early-return guards and test invariants — no new data rendering paths introduced.

### Behavioral Spot-Checks

| Behavior                                          | Command                                                                         | Result                      | Status  |
|---------------------------------------------------|---------------------------------------------------------------------------------|-----------------------------|---------|
| scoring_enabled gate count in scheduler.py        | `grep -c "scoring_enabled" scheduler.py`                                        | 8 (2 per function)          | PASS    |
| Gate precedes bg_job_id in Google Ads function    | lines 474-479 ordering confirmed in code read                                   | gate at 474, bg_job_id=None at 479 | PASS |
| Gate precedes bg_job_id in Meta function          | lines 610-614 ordering confirmed in code read                                   | gate at 610, bg_job_id=None at 614 | PASS |
| Gate precedes bg_job_id in TikTok function        | lines 684-688 ordering confirmed in code read                                   | gate at 684, bg_job_id=None at 688 | PASS |
| Gate precedes bg_job_id in DV360 function         | lines 760-765 ordering confirmed in code read                                   | gate at 760, bg_job_id=None at 765 | PASS |
| All 4 phase commit hashes exist in git log        | `git log --oneline \| grep d060a2d\|e25577f\|7278d2a\|97d7671`                 | All 4 found                 | PASS    |
| tiktok_sync.py upsert exclusion at line 379       | `grep -n "asset_url.*video_source_url" tiktok_sync.py`                          | Line 379 confirmed          | PASS    |

### Requirements Coverage

| Requirement | Source Plan  | Description                                                                    | Status    | Evidence                                                                                   |
|-------------|-------------|--------------------------------------------------------------------------------|-----------|--------------------------------------------------------------------------------------------|
| TKTOK-01    | 19.3-01, 19.3-02 | User sees TikTok video creatives in dashboard after sync (video files downloaded to MinIO/S3, stored as video_url on CreativeAsset) | VERIFIED | asset_url excluded from upsert ON CONFLICT (preserved through re-sync); scoring_enabled gate prevents spurious download suppression for videos |
| TKTOK-02    | 19.3-01, 19.3-02 | User sees TikTok image creatives in dashboard after sync                       | VERIFIED  | Same upsert exclusion protects image asset_url; gate applies equally to all asset types     |

Note: TKTOK-01 and TKTOK-02 were originally marked Complete from Phase 15. Phase 19.3 closes two gap items identified during Phase 15 gap review (upsert null window + missing scoring_enabled gate). Requirements remain satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | No TODO/FIXME/placeholder patterns found in gate blocks or test functions |

The mock choice of `MagicMock` (not `AsyncMock`) for `scalar_one_or_none` is correct — SQLAlchemy's `scalar_one_or_none()` is a synchronous method on the result object. This was a bug in the Wave 1 stub that Wave 2 corrected (`97d7671`).

### Human Verification Required

None. All phase deliverables are verifiable programmatically via code inspection.

The only item that would benefit from a live environment test is the end-to-end behavior of the gate (i.e., a superadmin disables scoring and triggers a sync, confirming no download jobs appear in the BackgroundJob table). However, the unit tests in `test_scheduler.py` cover this behavior via mocks, and the ROADMAP notes the full live test as a validation concern for UAT, not for this gap-closure phase.

### Gaps Summary

No gaps. All phase deliverables are present, substantive, and correctly wired.

- **Upsert exclusion (SC-3):** `asset_url` and `video_source_url` are excluded from `update_cols` in `_upsert_records` at `tiktok_sync.py:369-379`. The ON CONFLICT DO UPDATE set will never overwrite these fields during a re-sync.
- **Scoring gate (SC-2):** All 4 platform download functions (`_run_google_ads_asset_downloads`, `_run_meta_creatives_deferred`, `_run_tiktok_creatives_deferred`, `_run_dv360_asset_downloads`) have the gate as their first executable block, before any `bg_job_id` assignment or `create_background_job` call.
- **Gate pattern parity:** The gate structure is identical to `scoring_job.py:65-70`, using `select(SystemConfig).limit(1)`, `scalar_one_or_none()`, and an early `return` if `not scoring_enabled`.
- **Test coverage:** 4 test functions across 2 test files confirm the invariants programmatically. Commits `d060a2d`, `e25577f`, `7278d2a`, `97d7671` all verified in git log.

---

_Verified: 2026-05-13T16:30:00Z_
_Verifier: Claude (gsd-verifier)_

---
context: default
phase: none
task: debugging
status: blocked
last_updated: 2026-04-28T17:13:47.628Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: upload_bytes refactor may have broken manual video download endpoint — internal error observed but root cause not yet identified. Do NOT assume code is correct; read the backend error logs first.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Never suggest `docker volume destruction` without warning | `down -v` or `--volumes` wipes the dev DB | blocking | Always warn + confirm backup before any volume-destructive command |
| Never reset PROCESSING assets | Assets in PROCESSING status have live BrainSuite job IDs | blocking | Only reset stuck PENDING, never PROCESSING |

<current_state>
Just completed two commits:
1. `5ce7221` — fix(autofill): skip assets with no asset_url in backfill query
2. `8c8cfc4` — refactor(sync): upload assets in-memory, remove local filesystem writes

After restarting dev environment (Docker/Colima disk was full, resolved by pruning), user reports:
- Video downloads during sync still not working
- Autofill not triggering after sync
- Manually triggering video download via UI shows "internal error" (HTTP 500)

Backend error logs were not captured before context ran out — that's the first thing to do.
</current_state>

<completed_work>

- Autofill backfill fix: `asset_url != ""` filter added to both FAILED/PENDING and COMPLETE-but-no-values queries in `ai_autofill.py`
- In-memory upload refactor for Google Ads sync:
  - `_download_thumbnail`: removed `org_dir` param, now uses `upload_bytes` directly
  - `_download_video`: removed `org_dir` param, uses `tempfile.NamedTemporaryFile` + `finally` cleanup
  - `_upsert_records`: removed `org_dir` param
  - `sync_date_range`: removed `_CREATIVES_DIR`/`org_dir` creation
- In-memory upload refactor for DV360 sync:
  - Removed `_CREATIVES_DIR` constant
  - `_download_image_asset`: removed `org_dir`, uses `upload_bytes`
  - `_download_youtube_thumbnail`: removed `org_dir`, uses `upload_bytes`
  - `_download_video_asset`: removed `org_dir`, uses tempfile, now returns `(duration, served_url)` instead of `(local_path, served_url)`
  - `_upsert_records`: removed `org_dir` creation, updated return value
  - `download_assets_post_commit`: removed `org_dir` from asset_info, updated all method calls
- Added `frontend/.dockerignore` with `.angular`, `node_modules`, `dist` to prevent 2.58 GB Angular cache from bloating Docker images
</completed_work>

<remaining_work>

- **Investigate and fix the "internal error" on manual video download trigger** — need to read backend logs for the actual stack trace
- **Investigate why autofill is not triggering after sync** — may be related to the asset_url fix or a separate issue
- **Verify video downloads work end-to-end** after the in-memory upload refactor
- Root cause hypothesis: the manual video download endpoint may reference `org_dir` or `_CREATIVES_DIR` somewhere outside the sync files (e.g. in an API route or task handler), or `upload_bytes` may be missing from the object storage implementation
</remaining_work>

<decisions_made>

- Used `upload_bytes` for thumbnails/images (no temp file needed — data already in memory)
- Used `tempfile.NamedTemporaryFile` + `finally` for videos (yt-dlp requires a file path)
- DV360 `_download_video_asset` now returns `(Optional[float], Optional[str])` — duration extracted before temp file cleanup
</decisions_made>

<blockers>

- **Internal error on manual video download** — unknown root cause, backend logs needed
- **Autofill not triggering** — unknown if related to the asset_url backfill fix or something else
</blockers>

## Required Reading (in order)
1. `backend/app/services/sync/google_ads_sync.py` — refactored this session, understand the new upload_bytes/tempfile pattern
2. `backend/app/services/sync/dv360_sync.py` — same refactor
3. `backend/app/services/ai_autofill.py` — backfill fix applied this session
4. Backend logs at startup — `docker-compose logs --tail=100 backend` filtered for ERROR/Traceback

## Infrastructure State
- Dev environment: Colima + docker-compose, running via `make dev`
- Colima `/var/lib/containerd` partition: 98 GB, ~31 GB free after prune (was 100% full)
- `frontend/.dockerignore` added this session — Angular cache no longer included in image builds
- All code changes committed, no uncommitted files

<context>
The in-memory upload refactor was intended to remove dependency on a local `static/creatives/` directory (which doesn't exist in containerized environments). The refactor completed successfully and both commits landed. However, on testing, the manual video download endpoint throws a 500. The autofill issue may be pre-existing or related to the backfill fix. The backend logs were not captured before context exhausted — that's the critical first step when resuming.
</context>

<next_action>
Start with: `docker-compose logs --tail=150 backend 2>/dev/null | grep -E "ERROR|Traceback|500|video|download|upload_bytes" -i | tail -50` to find the actual error causing the manual video download 500. Then trace it to the source.
</next_action>

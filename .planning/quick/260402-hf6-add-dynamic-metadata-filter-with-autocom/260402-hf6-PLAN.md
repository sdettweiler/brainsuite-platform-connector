---
phase: quick
plan: 260402-hf6
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/endpoints/dashboard.py
  - backend/app/api/v1/endpoints/assets.py
  - backend/app/schemas/creative.py
  - frontend/src/app/features/dashboard/dashboard.component.ts
autonomous: false
requirements: [QT-260402-hf6]

must_haves:
  truths:
    - "User can click 'Add Filter' in toolbar and pick a metadata field from a popover"
    - "User can type to search values with autocomplete (combined predefined + actual DB values)"
    - "User can select multiple values per field (checkboxes, OR logic within field)"
    - "Active filters appear as dismissible chips between toolbar and stats"
    - "Filters are applied to the asset list (AND across fields, OR within field)"
    - "Clear all link removes all metadata filters at once"
    - "New metadata fields added in Settings automatically appear in the filter picker"
  artifacts:
    - path: "backend/app/api/v1/endpoints/assets.py"
      provides: "GET /assets/metadata-filter-values?field_id=<uuid> endpoint"
      contains: "metadata-filter-values"
    - path: "backend/app/api/v1/endpoints/dashboard.py"
      provides: "meta_filters query param on GET /dashboard/assets"
      contains: "meta_filters"
    - path: "backend/app/schemas/creative.py"
      provides: "Updated DashboardFilterParams with List[str] metadata_filters"
      contains: "metadata_filters"
    - path: "frontend/src/app/features/dashboard/dashboard.component.ts"
      provides: "Add Filter button, popover, chips row, filter integration"
      contains: "metadataFilters"
  key_links:
    - from: "frontend/dashboard.component.ts"
      to: "/api/v1/assets/metadata-filter-values"
      via: "API call when user selects a field in popover step 2"
      pattern: "metadata-filter-values"
    - from: "frontend/dashboard.component.ts"
      to: "/api/v1/dashboard/assets"
      via: "meta_filters query param in loadData()"
      pattern: "meta_filters"
    - from: "backend/dashboard.py meta_filters"
      to: "AssetMetadataValue JOIN"
      via: "SQL JOIN + WHERE value IN for each filter"
      pattern: "AssetMetadataValue.*field_id"
---

<objective>
Add a dynamic metadata filter with autocomplete to the dashboard, allowing users to filter assets by any active metadata field (brand, language, stage, etc.) with multi-select OR logic within a field and AND logic across fields. New fields added in Settings automatically appear as filterable options.

Purpose: Enable granular asset filtering by custom metadata dimensions, a core workflow need for agencies managing creatives across brands/languages/stages.
Output: Working "Add Filter" button in toolbar, 2-step popover (pick field, then search/select values), dismissible chips row, backend filtering via meta_filters query param, new metadata-filter-values endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260402-hf6-add-dynamic-metadata-filter-with-autocom/260402-hf6-CONTEXT.md

@backend/app/api/v1/endpoints/dashboard.py
@backend/app/api/v1/endpoints/assets.py
@backend/app/schemas/creative.py
@backend/app/models/metadata.py
@backend/app/models/creative.py
@frontend/src/app/features/dashboard/dashboard.component.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/models/creative.py:
```python
class AssetMetadataValue(Base):
    __tablename__ = "asset_metadata_values"
    id: Mapped[uuid.UUID]
    asset_id: Mapped[uuid.UUID]  # FK creative_assets.id
    field_id: Mapped[uuid.UUID]  # FK metadata_fields.id
    value: Mapped[str]           # String(500)
    # UniqueConstraint("asset_id", "field_id", name="uq_asset_metadata_field")
```

From backend/app/models/metadata.py:
```python
class MetadataField(Base):
    __tablename__ = "metadata_fields"
    id: Mapped[uuid.UUID]
    organization_id: Mapped[uuid.UUID]
    name: Mapped[str]
    label: Mapped[str]
    field_type: Mapped[str]  # SELECT, TEXT, NUMBER
    is_active: Mapped[bool]
    sort_order: Mapped[int]
    allowed_values: Mapped[list["MetadataFieldValue"]]  # relationship

class MetadataFieldValue(Base):
    __tablename__ = "metadata_field_values"
    id: Mapped[uuid.UUID]
    field_id: Mapped[uuid.UUID]
    value: Mapped[str]
    label: Mapped[str]
    sort_order: Mapped[int]
```

From backend/app/schemas/creative.py (line 109):
```python
class DashboardFilterParams(BaseModel):
    metadata_filters: Optional[Dict[str, str]] = None  # CHANGE to Optional[Dict[str, List[str]]]
```

From backend/app/api/v1/endpoints/dashboard.py (line 183):
```python
@router.get("/assets", response_model=dict)
async def get_dashboard_assets(
    # ... existing params ...
    score_min: Optional[float] = Query(default=None),
    score_max: Optional[float] = Query(default=None),
    # ADD: meta_filters: Optional[List[str]] = Query(default=None)
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
```

Frontend dashboard.component.ts key points:
- Line 183: `<div class="toolbar-spacer"></div>` -- "Add Filter" button goes BEFORE this spacer, AFTER the score slider
- Line 193: `<div class="agg-stats">` -- chips row goes BETWEEN toolbar (line 191 `</div>`) and agg-stats
- Line 1294: `loadData()` method builds params and calls `/dashboard/assets`
- Line 1334: `onFilterChange()` resets page and calls loadData()
- Existing filter pattern: params built as `any` object, conditionally adding keys
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — new metadata-filter-values endpoint + meta_filters on dashboard/assets</name>
  <files>
    backend/app/api/v1/endpoints/assets.py
    backend/app/api/v1/endpoints/dashboard.py
    backend/app/schemas/creative.py
  </files>
  <action>
    1. **New endpoint in assets.py** — Add `GET /assets/metadata-filter-values` endpoint:
       - Query param: `field_id: uuid.UUID = Query(...)` (required)
       - Auth: `get_current_user` dependency (org-scoped security)
       - Logic: Query `MetadataFieldValue` for predefined values (label+value), then query `SELECT DISTINCT value FROM asset_metadata_values WHERE field_id = :field_id AND asset_id IN (SELECT id FROM creative_assets WHERE organization_id = :org_id)` for actual used values. Combine both lists, de-duplicate by value (predefined take precedence). Return `List[dict]` with `{value: str, label: str, source: "predefined"|"actual"}`.
       - Import `MetadataFieldValue` from `app.models.metadata` and `AssetMetadataValue` from `app.models.creative`.

    2. **Update DashboardFilterParams in schemas/creative.py** — Change line 116:
       `metadata_filters: Optional[Dict[str, str]] = None` -> `metadata_filters: Optional[Dict[str, List[str]]] = None`
       (This schema is used for reference/export; the dashboard endpoint uses individual Query params.)

    3. **Add meta_filters to dashboard.py `get_dashboard_assets`** — Add parameter after `score_max`:
       `meta_filters: Optional[List[str]] = Query(default=None)`
       Format: each string is `"field_id:value1,value2"`.
       Parse each entry: split on first `:` to get field_id and comma-separated values.
       For each parsed filter, add a JOIN + WHERE:
       ```python
       for i, mf in enumerate(parsed_meta_filters):
           alias = aliased(AssetMetadataValue)
           query = query.join(alias, and_(
               alias.asset_id == CreativeAsset.id,
               alias.field_id == mf['field_id'],
               alias.value.in_(mf['values']),
           ))
       ```
       Import `aliased` from `sqlalchemy.orm` (add to existing import line).
       Place the meta_filters filtering block after the existing `score_max` filter block (around line 300) and before the "Only assets with performance" comment (line 302).

    4. **Also apply meta_filters to `get_dashboard_stats`** — Add the same `meta_filters: Optional[List[str]] = Query(default=None)` param. Parse identically. Apply the same JOIN pattern to the stats base query so stats reflect the filtered view. The stats endpoint starts around line 51.
  </action>
  <verify>
    <automated>cd /Users/sebastian.dettweiler/Claude\ Code/platform-connector/brainsuite-platform-connector && python -c "
from backend.app.api.v1.endpoints.assets import router as assets_router
from backend.app.api.v1.endpoints.dashboard import router as dashboard_router
routes = [r.path for r in assets_router.routes]
assert '/metadata-filter-values' in routes or 'metadata-filter-values' in str(routes), f'Missing endpoint: {routes}'
print('Backend endpoints OK')
" 2>/dev/null || cd backend && python -c "
import ast, sys
with open('app/api/v1/endpoints/assets.py') as f: src = f.read()
assert 'metadata-filter-values' in src, 'Missing metadata-filter-values endpoint'
with open('app/api/v1/endpoints/dashboard.py') as f: src = f.read()
assert 'meta_filters' in src, 'Missing meta_filters param in dashboard'
with open('app/schemas/creative.py') as f: src = f.read()
assert 'List[str]' in src or 'list[str]' in src, 'metadata_filters not updated to List'
print('All backend checks passed')
"</automated>
  </verify>
  <done>
    - GET /assets/metadata-filter-values?field_id=UUID returns combined predefined + actual values, org-scoped
    - GET /dashboard/assets accepts meta_filters query param (repeating), applies JOIN+WHERE for each
    - GET /dashboard/stats accepts meta_filters and applies same filtering so stats match filtered view
    - DashboardFilterParams.metadata_filters updated to Dict[str, List[str]]
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Add Filter button, 2-step popover, chips row, and API integration</name>
  <files>
    frontend/src/app/features/dashboard/dashboard.component.ts
  </files>
  <action>
    **All changes in the single-file dashboard.component.ts (inline template + styles).**

    1. **Add Angular Material imports** at the top:
       - `MatAutocompleteModule` from `@angular/material/autocomplete`
       - `MatChipsModule` from `@angular/material/chips`
       - `MatIconModule` from `@angular/material/icon` (if not already imported)
       - `CdkConnectedOverlay, CdkOverlayOrigin` from `@angular/cdk/overlay`
       - Add all to the `imports` array in the `@Component` decorator.

    2. **Add component state properties** (after the existing filter properties like `scoreMin`, around line 982):
       ```typescript
       // Metadata filter state
       metadataFields: any[] = [];  // loaded from /assets/metadata-fields
       activeMetadataFilters: Map<string, { fieldLabel: string, values: string[] }> = new Map();
       // Popover state
       filterPopoverOpen = false;
       filterPopoverStep: 'field' | 'value' = 'field';
       selectedFilterField: any = null;  // the field being configured
       filterValues: { value: string, label: string, source: string }[] = [];
       filteredFilterValues: { value: string, label: string, source: string }[] = [];
       filterValueSearch = '';
       pendingFilterSelections: Set<string> = new Set();
       ```

    3. **Load metadata fields on init** — In `ngOnInit()` (around line 1050), add:
       ```typescript
       this.api.get<any[]>('/assets/metadata-fields').subscribe({
         next: (fields) => this.metadataFields = fields,
       });
       ```

    4. **Add popover methods:**
       - `openFilterPopover()`: sets `filterPopoverOpen = true`, `filterPopoverStep = 'field'`, resets search/selections.
       - `selectFilterField(field)`: sets `selectedFilterField = field`, `filterPopoverStep = 'value'`, calls API `GET /assets/metadata-filter-values?field_id={field.id}` to load values. Pre-check any values already in `activeMetadataFilters` for this field. Set `filteredFilterValues` = full list initially.
       - `onFilterValueSearch(term: string)`: filters `filterValues` by term (case-insensitive substring on label and value), updates `filteredFilterValues`.
       - `toggleFilterValue(value: string)`: add/remove from `pendingFilterSelections` Set.
       - `confirmFilterSelection()`: if `pendingFilterSelections.size > 0`, set `activeMetadataFilters.set(selectedFilterField.id, { fieldLabel: selectedFilterField.label, values: [...pendingFilterSelections] })`. Close popover. Call `onFilterChange()`.
       - `removeMetadataFilter(fieldId: string)`: delete from `activeMetadataFilters`, call `onFilterChange()`.
       - `clearAllMetadataFilters()`: clear the Map, call `onFilterChange()`.

    5. **Update `loadData()` method** (around line 1294) — After existing params construction, add:
       ```typescript
       if (this.activeMetadataFilters.size > 0) {
         params['meta_filters'] = [];
         this.activeMetadataFilters.forEach((filter, fieldId) => {
           params['meta_filters'].push(`${fieldId}:${filter.values.join(',')}`);
         });
       }
       ```
       Note: Angular HttpClient with `HttpParams` handles array params by repeating the key. But since the existing code uses `this.api.get()` with a plain object, verify how `ApiService` serializes array params. If it uses `HttpParams.append`, arrays work. If not, join with a separator or pass multiple keys. Check `ApiService.get()` implementation — likely need to convert array to repeated params. Simplest: if `api.get` just passes params as `{ params }` to HttpClient, Angular handles arrays by repeating the key automatically.

    6. **Update `loadStats()` method** — Apply same `meta_filters` param construction so stats reflect filtered view.

    7. **Template — Add Filter button** in the toolbar, AFTER the score-slider div (line 182) and BEFORE `<div class="toolbar-spacer">` (line 184):
       ```html
       <!-- Metadata filter -->
       <div class="metadata-filter-trigger" cdkOverlayOrigin #filterOrigin="cdkOverlayOrigin">
         <button mat-stroked-button class="add-filter-btn" (click)="openFilterPopover()">
           <i class="bi bi-funnel"></i> Add Filter
           <span *ngIf="activeMetadataFilters.size > 0" class="filter-badge">{{ activeMetadataFilters.size }}</span>
         </button>
       </div>

       <!-- Filter popover (connected overlay) -->
       <ng-template cdkConnectedOverlay
         [cdkConnectedOverlayOrigin]="filterOrigin"
         [cdkConnectedOverlayOpen]="filterPopoverOpen"
         (overlayOutsideClick)="filterPopoverOpen = false"
         [cdkConnectedOverlayHasBackdrop]="true"
         cdkConnectedOverlayBackdropClass="cdk-overlay-transparent-backdrop">
         <div class="filter-popover">
           <!-- Step 1: Pick field -->
           <div *ngIf="filterPopoverStep === 'field'" class="filter-step">
             <div class="filter-step-header">Select metadata field</div>
             <button *ngFor="let field of metadataFields"
               class="filter-field-option"
               (click)="selectFilterField(field)">
               {{ field.label }}
               <span *ngIf="activeMetadataFilters.has(field.id)" class="filter-active-dot"></span>
             </button>
             <div *ngIf="metadataFields.length === 0" class="filter-empty">No metadata fields configured</div>
           </div>

           <!-- Step 2: Search/select values -->
           <div *ngIf="filterPopoverStep === 'value'" class="filter-step">
             <div class="filter-step-header">
               <button mat-icon-button (click)="filterPopoverStep = 'field'" class="filter-back-btn">
                 <i class="bi bi-arrow-left"></i>
               </button>
               {{ selectedFilterField?.label }}
             </div>
             <input class="filter-search-input"
               placeholder="Search values..."
               [(ngModel)]="filterValueSearch"
               (ngModelChange)="onFilterValueSearch($event)" />
             <div class="filter-values-list">
               <label *ngFor="let v of filteredFilterValues" class="filter-value-option">
                 <mat-checkbox
                   [checked]="pendingFilterSelections.has(v.value)"
                   (change)="toggleFilterValue(v.value)">
                   {{ v.label || v.value }}
                 </mat-checkbox>
               </label>
               <div *ngIf="filteredFilterValues.length === 0" class="filter-empty">No matching values</div>
             </div>
             <button mat-flat-button color="primary" class="filter-apply-btn"
               [disabled]="pendingFilterSelections.size === 0"
               (click)="confirmFilterSelection()">
               Apply ({{ pendingFilterSelections.size }})
             </button>
           </div>
         </div>
       </ng-template>
       ```

    8. **Template — Chips row** between toolbar closing `</div>` (line 191) and `<div class="agg-stats">` (line 194):
       ```html
       <!-- Active metadata filter chips -->
       <div class="metadata-filter-chips" *ngIf="activeMetadataFilters.size > 0">
         <span *ngFor="let entry of activeMetadataFilters | keyvalue" class="metadata-filter-chip">
           {{ entry.value.fieldLabel }}: {{ entry.value.values.join(', ') }}
           <button class="chip-remove" (click)="removeMetadataFilter(entry.key)">
             <i class="bi bi-x"></i>
           </button>
         </span>
         <a class="clear-all-link" (click)="clearAllMetadataFilters()">Clear all</a>
       </div>
       ```

    9. **Styles** — Add within the component's `styles` block:
       ```css
       .add-filter-btn {
         display: flex; align-items: center; gap: 4px;
         font-size: 13px; height: 36px;
         border-color: var(--border); color: var(--text-secondary);
       }
       .add-filter-btn .filter-badge {
         background: var(--accent); color: #fff; border-radius: 50%;
         width: 18px; height: 18px; font-size: 11px;
         display: inline-flex; align-items: center; justify-content: center;
       }
       .filter-popover {
         background: var(--bg-card); border: 1px solid var(--border);
         border-radius: 8px; box-shadow: 0 8px 24px rgba(0,0,0,.12);
         width: 280px; max-height: 400px; overflow: hidden;
       }
       .filter-step { display: flex; flex-direction: column; }
       .filter-step-header {
         padding: 12px 16px; font-weight: 600; font-size: 13px;
         border-bottom: 1px solid var(--border);
         display: flex; align-items: center; gap: 8px;
       }
       .filter-back-btn { width: 28px; height: 28px; }
       .filter-field-option {
         padding: 10px 16px; border: none; background: none;
         text-align: left; cursor: pointer; font-size: 13px;
         color: var(--text-primary); display: flex; align-items: center; justify-content: space-between;
       }
       .filter-field-option:hover { background: var(--bg-hover); }
       .filter-active-dot {
         width: 6px; height: 6px; border-radius: 50%; background: var(--accent);
       }
       .filter-search-input {
         margin: 8px 12px; padding: 8px 12px;
         border: 1px solid var(--border); border-radius: 6px;
         font-size: 13px; background: var(--bg-input, var(--bg-card));
         color: var(--text-primary); outline: none;
       }
       .filter-search-input:focus { border-color: var(--accent); }
       .filter-values-list {
         max-height: 240px; overflow-y: auto; padding: 4px 0;
       }
       .filter-value-option {
         display: block; padding: 4px 16px; font-size: 13px; cursor: pointer;
       }
       .filter-value-option:hover { background: var(--bg-hover); }
       .filter-apply-btn { margin: 8px 12px 12px; }
       .filter-empty { padding: 16px; text-align: center; color: var(--text-muted); font-size: 13px; }

       .metadata-filter-chips {
         display: flex; flex-wrap: wrap; gap: 8px; padding: 8px 24px 0;
         align-items: center;
       }
       .metadata-filter-chip {
         display: inline-flex; align-items: center; gap: 4px;
         background: var(--bg-hover, rgba(255,255,255,.06));
         border: 1px solid var(--border); border-radius: 16px;
         padding: 4px 8px 4px 12px; font-size: 12px; color: var(--text-secondary);
       }
       .chip-remove {
         border: none; background: none; cursor: pointer;
         color: var(--text-muted); padding: 0; line-height: 1;
         display: inline-flex; align-items: center;
       }
       .chip-remove:hover { color: var(--text-primary); }
       .clear-all-link {
         font-size: 12px; color: var(--accent); cursor: pointer;
         text-decoration: none;
       }
       .clear-all-link:hover { text-decoration: underline; }
       ```

    10. **Import CDK overlay module** — Add `OverlayModule` from `@angular/cdk/overlay` to the component imports array. This provides the `cdkConnectedOverlay` and `cdkOverlayOrigin` directives.
  </action>
  <verify>
    <automated>cd /Users/sebastian.dettweiler/Claude\ Code/platform-connector/brainsuite-platform-connector/frontend && npx ng build --configuration=production 2>&1 | tail -5</automated>
  </verify>
  <done>
    - "Add Filter" button visible in toolbar after score slider, before spacer
    - Clicking opens a popover with Step 1 (field list from /assets/metadata-fields)
    - Selecting a field transitions to Step 2 (value search + checkboxes, data from /assets/metadata-filter-values)
    - Confirming adds filter chip(s) between toolbar and stats row
    - Chips show "FieldLabel: val1, val2 x" with dismiss button
    - "Clear all" link removes all metadata filters
    - loadData() sends meta_filters params, backend filters assets accordingly
    - loadStats() also sends meta_filters so stats reflect filtered view
    - Chips row hidden (zero height) when no active filters
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dynamic metadata filter with autocomplete on the dashboard: "Add Filter" button, 2-step popover (pick field then search/select values), dismissible chips row, backend filtering via meta_filters.</what-built>
  <how-to-verify>
    1. Navigate to the dashboard (http://localhost:4200/dashboard)
    2. Verify "Add Filter" button appears in the toolbar after the score slider
    3. Click "Add Filter" — a popover should show all active metadata fields (e.g., Brand, Language, Stage)
    4. Select a field — popover transitions to value search with checkboxes
    5. Type in the search box — values filter in real-time
    6. Check 2+ values, click "Apply" — popover closes
    7. Verify a chip appears below the toolbar: "Brand: Nike, Adidas x"
    8. Verify the asset list is filtered (only assets matching the selected metadata values)
    9. Verify stats section reflects the filtered subset
    10. Click the "x" on a chip — filter removed, list updates
    11. Add a second filter (different field) — verify AND logic (both filters applied)
    12. Click "Clear all" — all filters removed, full list restored
    13. Verify chip row is completely hidden when no filters are active (no empty space)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Backend: New endpoint returns combined predefined + actual metadata values, org-scoped
- Backend: Dashboard assets and stats endpoints accept and apply meta_filters
- Frontend: Add Filter popover works with 2-step flow (field selection -> value search/select)
- Frontend: Multi-select with checkboxes within a field (OR logic)
- Frontend: AND logic across different field filters
- Frontend: Chips row appears/hides dynamically, dismiss individual or clear all
- Frontend: Production build succeeds without errors
</verification>

<success_criteria>
- User can filter dashboard assets by any active metadata field using the Add Filter popover
- Multiple values per field are supported (OR logic) with checkboxes
- Multiple fields are supported (AND logic across fields)
- Active filters shown as dismissible chips with "Clear all" option
- Stats reflect filtered view
- Newly added metadata fields automatically appear in the filter picker
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260402-hf6-add-dynamic-metadata-filter-with-autocom/260402-hf6-SUMMARY.md`
</output>

# Repository Cleanup Plan

Generated: 2026-03-31

## Summary

Audit of the entire repository tree (excluding `.git/` and `.claude/worktrees/`) found **37 file/directory candidates** across 7 categories. The repository migrated from Replit and carries significant Replit-specific scaffolding. The largest single wins by disk size are `frontend/node_modules/` (529 MB) and `frontend/.angular/` (1.5 GB) — both already in `.gitignore` and safe to delete from the working tree at any time.

**Categories:**

| # | Category | Candidates |
|---|----------|-----------|
| 1 | Replit Leftovers | 7 |
| 2 | Temp / Editor Artifacts | 3 |
| 3 | Dead CI / Deploy Configs | 0 |
| 4 | Build / Cache Dirs (should be gitignored) | 5 |
| 5 | Stale / Unused Root Scripts & Metadata | 3 |
| 6 | Attached Assets (Replit scratch uploads) | 2 dirs + individual files |
| 7 | Other Legacy / Obsolete | 5 |

Docker, Makefile, and all `docs/`, `designs/`, `brainsuite_api/`, `scripts/`, and `PLATFORM_SETUP.md` are **confirmed safe** — see final section.

---

## Category 1: Replit Leftovers

These files were created by or for the Replit IDE/platform. The project now runs locally via Docker Compose and deploys to cloud infra — none of these are needed.

| File/Dir | Reasoning |
|----------|-----------|
| `.replit` | Replit IDE configuration: module list, nix channel, port mappings, workflow definitions, deployment target (`autoscale`). Completely Replit-specific. Also contains plaintext secrets in `[userenv.shared]` (SECRET_KEY, TOKEN_ENCRYPTION_KEY, YOUTUBE_COOKIES) — **this is a security risk if the repo is ever public.** |
| `.replit_integration_files/` | Auto-generated by Replit's "JavaScript Object Storage" integration. Contains a TypeScript React `ObjectUploader.tsx` component and server-side `objectStorage.ts` SDK wrappers. None of these are imported by the Angular frontend or FastAPI backend. Safe to delete entirely. |
| `replit_start.sh` | Replit startup script: configures Replit-specific env vars (`REPLIT_DEV_DOMAIN`), installs deps inline, manages a health-check placeholder server. The Docker-based workflow uses `Makefile` + `docker-compose`. This script is not referenced in any current workflow outside `.replit`. |
| `replit.md` | Project README written for Replit: mentions "Replit-managed PostgreSQL", Replit secrets panel, and the `replit_start.sh` entrypoint. Contains outdated tech-stack summary (references features not yet built). |
| `REPLIT_SETUP.md` | Step-by-step Replit deployment guide (Import from GitHub → Replit, add Replit DB, set Replit Secrets). Entirely Replit-platform-specific. Current deployment is Docker. |
| `main.py` | Root-level file containing only `def main(): print("Hello from repl-nix-workspace!")`. This is the Replit default entry point stub — completely unused by the FastAPI backend or any other part of the codebase. |
| `pyproject.toml` | Root-level pyproject.toml with `name = "repl-nix-workspace"` and empty `dependencies = []`. The only useful content is `[tool.pytest.ini_options]` pointing tests at `backend/tests/` — but pytest already discovers this if run from the repo root, and the backend has its own configuration. This is a Replit nix workspace artifact. **Closer inspection recommended** before deleting: verify `pytest` still works from repo root without it (it will, because `testpaths` defaults gracefully). |

---

## Category 2: Temp / Editor Artifacts

| File/Dir | Reasoning |
|----------|-----------|
| `.DS_Store` | macOS Finder metadata file in the repo root. Already in `.gitignore` but present on disk. No functional value. |
| `designs/.DS_Store` | Same as above, inside `designs/`. |
| `frontend/.angular-cli-analytics.json` | Contains `{}` — an empty Angular CLI analytics opt-out/opt-in stub. Generated automatically when `ng` runs for the first time. Has no project configuration value. Already covered by `.gitignore`'s `.angular/` rule. |

---

## Category 3: Dead CI / Deploy Configs

No external CI configs (`.circleci/`, `.travis.yml`, `Procfile`, `render.yaml`, `railway.json`, `fly.toml`, `vercel.json`, `netlify.toml`) were found at the repo root level. (`.travis.yml` files found only inside `frontend/node_modules/` — not project files.)

**No action required in this category.**

---

## Category 4: Build / Cache Dirs (should be gitignored / can be deleted from working tree)

All of these are already covered by `.gitignore` entries. They can be safely deleted from the working tree at any time; they will be regenerated on next build/install/test run.

| File/Dir | Size | Reasoning |
|----------|------|-----------|
| `frontend/.angular/` | ~1.5 GB | Angular incremental build cache. Regenerated by `ng build` or `ng serve`. `.gitignore` covers it via `.angular/`. |
| `frontend/node_modules/` | ~529 MB | NPM dependency tree. Regenerated by `npm install`. Covered by `.gitignore`. |
| `frontend/dist/` | ~5 MB | Angular production build output. Regenerated by `ng build --configuration production`. Covered by `.gitignore`. |
| `backend/__pycache__/` (x14 dirs) | small | Python bytecode cache dirs scattered throughout `backend/app/`, `backend/tests/`, `backend/alembic/`. Covered by `.gitignore` via `__pycache__/`. |
| `.pytest_cache/` (root + `backend/`) | small | Pytest run cache. Both root and `backend/` copies present. Covered by `.gitignore` via `.pytest_cache/`. |

---

## Category 5: Stale / Unused Root Scripts and Metadata

| File/Dir | Reasoning |
|----------|-----------|
| `build.sh` | Shell script that runs `npm install + ng build` then `pip install + touch .deps_installed`. Was referenced by `.replit` as the Replit deployment build step. With Docker, the `Dockerfile.frontend` and `Dockerfile.backend` own the build process. However, this script could serve as a convenience local build shortcut — **needs closer inspection**. If `Makefile` `dev` and `up` targets are the canonical local workflow, `build.sh` is redundant. |
| `package.json` (root) | Root `package.json` named `"workspace"` with dependencies like `@uppy/core`, `@google-cloud/storage`, `@uppy/react`, `echarts`, `ngx-echarts`. These are **not used by the Angular frontend** (which has its own `frontend/package.json`) and not by the Python backend. This appears to be a Replit workspace-level package.json created when Replit's "JavaScript Object Storage" integration was added. The accompanying `package-lock.json` is also vestigial. |
| `package-lock.json` (root) | Lock file for the root `package.json`. Vestigial — same reasoning as above. Committing this adds 100+ KB of noise with no functional value in the Docker/Angular workflow. |

---

## Category 6: Attached Assets (Replit Scratch Uploads)

Replit auto-saves all files uploaded via its chat/agent into `attached_assets/`. These are not referenced by any source code.

| File/Dir | Reasoning |
|----------|-----------|
| `attached_assets/` (entire directory) | Contains 47 files (2.8 MB total): platform logos (Google Ads, DV360, Meta, Brainsuite), Replit-timestamped UI screenshots (`image_177*.png`, `targeted_element_177*.png`), two Excel field-reference spreadsheets (`field_reference_v3`, `field_reference_v4`), two Pasted text files (DV360 API docs, Meta creative instructions), and **7 YouTube cookie files** (`www.youtube.com_cookies_*.txt`). The cookie files are a security/privacy concern if this repo is ever shared. Logos that are genuinely used in the frontend should be copied to `frontend/src/assets/` and the rest discarded. **Needs closer inspection** per file before bulk deletion. |
| `AIMPOWER-BRAINSUITE-CASEFILM-22-08-23-CUTDOWN.mp4 [2ufRFktMijc].webm` | 5.3 MB video file in the repo root. YouTube download artifact (note the YouTube video ID in the filename). Not referenced anywhere in source code. Likely used as a test creative during Replit development. |

---

## Category 7: Other Legacy / Obsolete

| File/Dir | Reasoning |
|----------|-----------|
| `backend/.deps_installed` | A marker file created by `replit_start.sh` and `build.sh` to cache pip install. Docker image builds handle dependency installation; this marker has no meaning in a container workflow and will accumulate stale state on developer machines. |
| `backend/static/creatives/` (test video) | Contains a single 3.9 MB MP4 (`vid_yt_798231948655.mp4` under a UUID directory). This appears to be a test creative uploaded during development. The production workflow uses object storage (MinIO/GCS) — this file is a leftover dev artifact. The `backend/static/` directory itself is needed for FastAPI's static file mount; only the contents are stale. |
| `scripts/spike_static_api.py` | A one-time discovery/validation script for the BrainSuite Static API. Per `docs/PRODUCTION_CHECKLIST.md` it should be run once before production go-live. After that run, it has no ongoing value. **Closer inspection required**: the PRODUCTION_CHECKLIST.md explicitly references running this script, so it should be kept until PROD-01 is marked complete. |
| `brainsuite_api/` (PDF/TXT copies) | Contains `API Docs General.pdf`, `API Docs General.txt`, `SMV API Docs_compressed.pdf`, `SMV API Docs_compressed.txt`. The `.txt` conversions of PDFs and the PDFs themselves are upstream vendor docs that were useful during Phase 3 discovery but are now superseded by `docs/BRAINSUITE_API.md` (the authoritative living reference). The `SMS_api_docs.md` and `kpi_structure.md` may still be useful for Phase 4 (video scoring visualization). **Needs closer inspection**: keep `SMS_api_docs.md` and `kpi_structure.md`, evaluate the PDF/TXT bulk dump. |
| `frontend/.angular-cli-analytics.json` | Already listed in Category 2. Mentioned here as it lives at `frontend/` level rather than inside `.angular/` cache dir. |

---

## Recommendation

**Priority 1 — High confidence, delete immediately:**

1. `.replit` — contains hardcoded secrets in `[userenv.shared]`; security risk
2. `.replit_integration_files/` — dead Replit integration code, nothing imports it
3. `replit_start.sh` — Replit-only startup script
4. `replit.md` — outdated Replit README
5. `REPLIT_SETUP.md` — Replit deployment guide
6. `main.py` — Replit default stub
7. `.DS_Store` (root and `designs/`)
8. `frontend/.angular-cli-analytics.json`
9. `backend/.deps_installed` — stale marker file
10. `AIMPOWER-BRAINSUITE-CASEFILM-22-08-23-CUTDOWN.mp4 [2ufRFktMijc].webm` — 5.3 MB YouTube download artifact
11. Root `package.json` and `package-lock.json` — Replit workspace artifacts

**Priority 2 — Delete from working tree (gitignored, regenerated on demand):**

12. `frontend/.angular/` (1.5 GB)
13. `frontend/node_modules/` (529 MB)
14. `frontend/dist/` (5 MB)
15. All `backend/**/__pycache__/` dirs
16. `.pytest_cache/` (root) and `backend/.pytest_cache/`

**Priority 3 — Needs closer inspection before deleting:**

17. `attached_assets/` — bulk delete but first: copy any logo/asset files that are actually referenced in `frontend/src/assets/`; verify YouTube cookies are not referenced anywhere
18. `brainsuite_api/` PDF and TXT files — keep `SMS_api_docs.md` and `kpi_structure.md`; delete the bulk PDF/TXT vendor doc dumps
19. `backend/static/creatives/` contents — verify this UUID is not a real user record in the DB; if dev artifact, delete the MP4 and folder
20. `pyproject.toml` (root) — keep only if removing it breaks `pytest` discovery from repo root; otherwise delete (backend tests can be run via `cd backend && pytest`)
21. `build.sh` — keep only if used in a CI pipeline or local workflow outside Docker; otherwise superseded by `Makefile`
22. `scripts/spike_static_api.py` — keep until PROD-01 checklist item is completed; delete after

---

## Files Confirmed Safe (NOT for deletion)

| File/Dir | Why it looks suspicious but is needed |
|----------|---------------------------------------|
| `docker-compose.yml` and `docker-compose.dev.yml` | Active deployment and dev workflow. Referenced by `Makefile`. |
| `docker/` (all Dockerfiles + `nginx.conf`) | Build definitions for production containers. |
| `Makefile` | Developer-facing convenience wrapper around docker-compose commands. |
| `PLATFORM_SETUP.md` | OAuth setup guide for Meta, Google, TikTok developer portals. Actively needed during dev and onboarding. |
| `scripts/setup.py` | Interactive `.env` generator for new developers. Used by `Makefile setup` target. |
| `.env.example` (root) | Docker Compose environment template. Needed. |
| `backend/.env.example` | Backend-specific env template for local non-Docker dev. Needed. |
| `brainsuite_api/SMS_api_docs.md` | Claude Code reference for the BrainSuite Static API — used during Phase 3 and needed for Phase 4. |
| `brainsuite_api/kpi_structure.md` | KPI/pillar structure reference for ACE Video scoring — needed for Phase 4 visualization work. |
| `docs/BRAINSUITE_API.md` | Living API reference, actively updated through Phase 3. |
| `docs/PRODUCTION_CHECKLIST.md` | Active checklist for go-live. Not done yet. |
| `designs/` | Design mockups and HTML prototype for the creative effectiveness dashboard. Needed for Phase 4 UI implementation. |
| `backend/static/` directory | FastAPI static file mount point. The directory must exist. Only the contents (test video) are stale. |
| `frontend/proxy.conf.json` | Angular dev server proxy — routes `/api`, `/static`, `/objects` to the backend during `ng serve`. |

---
phase: quick
plan: 260331-l16
subsystem: repository-hygiene
tags: [cleanup, replit, audit]
key-files:
  created:
    - .planning/quick/260331-l16-analyze-the-entire-folder-structure-for-/CLEANUP-PLAN.md
  modified: []
decisions:
  - ".replit contains hardcoded plaintext secrets in [userenv.shared] — flagged as security risk in cleanup plan"
  - "Root package.json and pyproject.toml are Replit workspace artifacts, not project files"
  - "brainsuite_api/ PDF/TXT dumps are superseded by docs/BRAINSUITE_API.md; SMS_api_docs.md and kpi_structure.md should be retained for Phase 4"
metrics:
  duration: 15min
  completed: "2026-03-31"
---

# Quick Task 260331-l16: Repository Cleanup Plan Summary

**One-liner:** Full repo audit identifying 37 Replit/legacy artifact candidates across 7 categories, prioritized by deletion confidence, with security flag on `.replit` plaintext secrets.

## What Was Done

Performed a comprehensive audit of the entire repository tree (excluding `.git/` and `.claude/worktrees/`). Used targeted `find` commands to enumerate all files, then cross-referenced with the active tech stack (Angular frontend in `frontend/`, Python/FastAPI backend in `backend/`, Docker Compose workflow) to classify each file as active, legacy, or stale.

## Output

`CLEANUP-PLAN.md` at `.planning/quick/260331-l16-analyze-the-entire-folder-structure-for-/CLEANUP-PLAN.md`

**158 lines, 7 categories, 37 candidates identified.**

## Key Findings

**Highest priority (security):**
- `.replit` contains hardcoded `SECRET_KEY`, `TOKEN_ENCRYPTION_KEY`, and a full YouTube Netscape cookie dump in `[userenv.shared]`. This is a risk if the repo is ever made public.

**Highest priority by disk size:**
- `frontend/.angular/` — 1.5 GB (gitignored build cache)
- `frontend/node_modules/` — 529 MB (gitignored)
- `frontend/dist/` — 5 MB (gitignored build output)

**Replit scaffolding to remove:**
- `.replit`, `.replit_integration_files/`, `replit_start.sh`, `replit.md`, `REPLIT_SETUP.md`, `main.py` — all Replit-only, not used in Docker/cloud workflow
- Root `package.json` / `package-lock.json` — created by Replit's JS Object Storage integration, not used by Angular frontend or Python backend

**Needs closer inspection before deleting:**
- `attached_assets/` — contains YouTube cookie files and possibly logos still in use
- `backend/static/creatives/` — contains one test MP4; verify UUID is not a live DB record
- `scripts/spike_static_api.py` — keep until PROD-01 checklist item is completed
- Root `pyproject.toml` — only value is `[tool.pytest.ini_options]`; verify pytest still works without it

## Deviations from Plan

None — plan executed exactly as written. No files were deleted.

## Self-Check: PASSED

- CLEANUP-PLAN.md created: FOUND
- Commit 4e3a2de: FOUND
- Line count >= 20: 158 lines

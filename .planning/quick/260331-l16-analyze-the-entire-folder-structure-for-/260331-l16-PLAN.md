---
phase: quick
plan: 260331-l16
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260331-l16-analyze-the-entire-folder-structure-for-/CLEANUP-PLAN.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every file and directory in the repo has been examined for obsolescence"
    - "Replit-specific leftovers are identified with explanation"
    - "Other legacy/dead artifacts are identified with explanation"
    - "User can review a single document listing all candidates for deletion"
  artifacts:
    - path: ".planning/quick/260331-l16-analyze-the-entire-folder-structure-for-/CLEANUP-PLAN.md"
      provides: "Categorized list of files/dirs to delete with reasoning"
      min_lines: 20
  key_links: []
---

<objective>
Audit the entire repository for legacy, obsolete, and leftover files — especially Replit IDE artifacts — and produce a reviewable cleanup plan document.

Purpose: The project migrated from Replit and may contain stale config, dead scripts, unused lock files, or temp artifacts that add noise and confusion.
Output: CLEANUP-PLAN.md listing every candidate file/dir for deletion with categorization and reasoning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full repository audit and cleanup plan document</name>
  <files>.planning/quick/260331-l16-analyze-the-entire-folder-structure-for-/CLEANUP-PLAN.md</files>
  <action>
Perform a comprehensive audit of the entire repository tree. Run these commands to gather the full picture:

1. `find . -not -path './.git/*' | sort` — complete file listing
2. `find . -name '.replit' -o -name 'replit.nix' -o -name '.breakpoints' -o -name '.replit-*' -o -name 'replit*' -not -path './.git/*'` — Replit-specific files
3. `find . -name '*.bak' -o -name '*.tmp' -o -name '*.orig' -o -name '*.swp' -o -name '*~' -o -name '.DS_Store' -not -path './.git/*'` — temp/editor artifacts
4. `find . -name '.cache' -o -name '__pycache__' -o -name 'node_modules' -o -name '.next' -o -name 'dist' -o -name 'build' -not -path './.git/*' -type d` — build/cache dirs that should be gitignored
5. Check `.gitignore` to see what is already excluded
6. Look for dead CI configs (`.circleci/`, `.travis.yml`, `Procfile`, `render.yaml`, `railway.json`, `fly.toml`, `Dockerfile` if unused, `docker-compose*.yml` if unused, `vercel.json` if unused)
7. Look for stale scripts — any shell scripts or Python scripts in root that appear unused or one-off
8. Look for duplicate or superseded config files (e.g., multiple package managers, old env templates)
9. Check for empty directories
10. Cross-reference with the active tech stack (Angular frontend in `frontend/`, Python/FastAPI backend in `backend/`) to determine what is genuinely in use vs. leftover

Produce CLEANUP-PLAN.md with these sections:

```
# Repository Cleanup Plan

Generated: {date}

## Summary
{Brief overview: total candidates found, categories}

## Category 1: Replit Leftovers
| File/Dir | Reasoning |
|----------|-----------|
| ... | ... |

## Category 2: Temp/Editor Artifacts
| File/Dir | Reasoning |
|----------|-----------|

## Category 3: Dead CI/Deploy Configs
| File/Dir | Reasoning |
|----------|-----------|

## Category 4: Build/Cache Dirs (should be gitignored)
| File/Dir | Reasoning |
|----------|-----------|

## Category 5: Stale/Unused Scripts
| File/Dir | Reasoning |
|----------|-----------|

## Category 6: Other Legacy/Obsolete
| File/Dir | Reasoning |
|----------|-----------|

## Recommendation
{Priority order for cleanup, any files that need closer inspection before deleting}

## Files Confirmed Safe (NOT for deletion)
{List any files that looked suspicious but are actually needed, with explanation}
```

Be thorough but conservative — when uncertain, put the file in "needs closer inspection" rather than recommending deletion. Include the full relative path for every candidate.
  </action>
  <verify>
    <automated>test -f ".planning/quick/260331-l16-analyze-the-entire-folder-structure-for-/CLEANUP-PLAN.md" && wc -l ".planning/quick/260331-l16-analyze-the-entire-folder-structure-for-/CLEANUP-PLAN.md" | awk '{if ($1 >= 20) print "PASS"; else print "FAIL: too short"}'</automated>
  </verify>
  <done>CLEANUP-PLAN.md exists with categorized file/dir candidates for deletion, each with reasoning. No files have been actually deleted.</done>
</task>

</tasks>

<verification>
- CLEANUP-PLAN.md exists and contains categorized cleanup candidates
- Document covers Replit leftovers, temp files, dead configs, stale scripts, and other legacy artifacts
- Each candidate has a file path and reasoning
- No files were actually deleted — this is a review document only
</verification>

<success_criteria>
User can open CLEANUP-PLAN.md and see every legacy/obsolete file candidate organized by category with clear reasoning, enabling an informed decision on what to delete.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-l16-analyze-the-entire-folder-structure-for-/260331-l16-SUMMARY.md`
</output>
